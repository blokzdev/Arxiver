# HUMAN.md — Co-Founder / human-in-the-loop ledger

Arxiver is built by Claude (Opus 4.8, Ultracode mode) running the autonomous loop in [`CLAUDE.md`](CLAUDE.md):
Claude orchestrates planning Workflows, runs a **first-person adversarial validation pass** over every
plan, self-approves subphases within an approved phase, and ships one PR each (green local `./gradlew
build`, green CI, self-merge on green). **The standing blocker is phase-plan approval, not merge.**

This file is the async channel back to the human Co-Founder. It is **not** a status board (`ROADMAP.md`
owns progress) and **not** a fact store (`.claude/memory/` owns durable facts). It holds only what a
human needs to *see or do*. **Skim §2 first — that's what's waiting on you.** To override any §1 decision,
just say so: Claude updates the row, notes the reversal, and threads the change through spec + code in one
commit. Rows are added in the same commit as the work that created them, and resolved (not silently
deleted) when met.

Legend: `OPEN` (waiting on you) · `FYI` (logged for awareness, no action) · `OVERRIDABLE` (Claude decided; reverse anytime).

---

## §1 — Decisions & judgment calls (Claude decided; override anytime)

| Date | Decision | Why | Override |
|---|---|---|---|
| 2026-07-12 | **Phase P-Ambient started (you approved it) — building PA.1 (the on-sync digest notification); a few product defaults + one dropped feature worth your eye.** (1) **The "streak" is DROPPED, not deferred** — there is no honest per-user engagement signal in the schema (saves/sync timestamps are written by background workers with no user present; `markSeen` has zero callers), so a streak would reward *the app running*, not *you showing up* — a dark pattern at odds with the calm local-first ethos. A real one needs a net-new `daily_activity` store; parked to backlog, never faked. (2) **Digest defaults:** opt-in **OFF** until you flip a Settings toggle (which triggers the app's first-ever `POST_NOTIFICATIONS` request), **at most once/day** even at a 3h sync cadence, **no onboarding prompt** (discoverable in Settings), `VISIBILITY_PRIVATE` so paper titles never render on the lockscreen, body = top-3 titles + "N more". (3) **Exactly-once accounting via a tiny additive `inbox_items.digested_at` column (migration v15→v16)** rather than a preferences watermark — it's crash/partial-pass resilient (chose the more-robust design). | Streak-drop is the ethos call; the digest defaults are the calm-by-default posture (under-notify beats spam); the extra column buys correctness a watermark can't (a stopped scoring pass can't lose a paper from a later digest). | Want the gamified streak (accept the new state)? A different daily ceiling / quiet-hours / user-tunable frequency? An onboarding prompt instead of Settings-only? A richer or barer notification? Say the word. |
| 2026-07-11 · FYI | **Red-line audit (Opus 4.8 workflow) — the perimeter is INTACT.** After all the recent P5/multi-source shipping I ran an adversarial 6-hunter audit (tokens · backup wall · egress allowlist · arXiv rate limiter · telemetry · repo secrets), each finding adversarially refuted before it could survive. **Zero confirmed violations** — every line clears with a named guard (secrets keystore-only via TokenVault/AiKeyVault; AllowedHosts fail-closed per redirect hop, `storage.googleapis.com` absent + pinned-rejected; one shared `ArxivRateLimiter` on every arXiv call; crash reporter is local-file-only, no telemetry deps; signing via env/CI secrets, `local.properties` untracked). I personally re-verified the one actionable finding and shipped it: a **backup-wall blocklist hardening** (the descriptor-walk test didn't have tokens for `head_bias`/`score_at_label` + the sibling `relevance_model` columns — not a leak today, but a future-regression blind spot; now closed for the whole never-exported row). **One defense-in-depth follow-up recorded, NOT built** (see §3): the egress host-gate is enforced by DI wiring + targeted tests, not one blanket structural test like the backup wall has — no exposure today (every bare OkHttp client hits a pinned/user host), so it's tracked for a future hardening pass, not gold-plated in now. | An audit after a big shipping run is cheap insurance on a privacy-first app whose red lines are load-bearing; the confirmed fix is adoptable-directly test-correctness; the INFO item was deprioritized to avoid over-building a structural test whose exemption model needs its own design. | Want the egress structural guard pulled forward now? Say so and I'll design it (§3). |
| 2026-07-11 | **P5.5 shipped — the "Likely relevant" section no longer flickers at the calibration boundary.** Before, a profile sitting right on the fit/no-fit line could show the calibrated section on one background pass and the plain 0.55 section on the next, and back again. Now a downgrade is **confirmed over two consecutive failed refits**: the first failure keeps your calibrated cut for one more pass, only a second in a row drops back to 0.55 — a fresh successful fit always applies immediately. One nuance worth knowing: "two passes" counts *completed* eval passes, so if a background pass is killed mid-run (Android does this), the downgrade just waits — it can never happen early or leave the row in a bad state. Needed a tiny DB column (`consecutive_null_fits`, v14→v15) because an in-memory counter would reset on those same process kills and could strand a stale calibration forever. | Flicker at a boundary is the classic hysteresis case; asymmetric two-pass-confirm (instant up, deliberate down) removes the flicker without ever *hiding* a real downgrade for more than one extra pass. The in-memory shortcut was rejected precisely because process death defeats it. ROADMAP P5.5 + decision log; SPEC-SEARCH/SPEC-DATA. | Want a stricter confirm (3 passes) or instant-down again? One constant. |
| 2026-07-10 | **P5.3 shipped — the "Likely relevant" cut is now per-user calibrated; two product constants worth your eye.** (1) **The section boundary means p = 0.5** ("more likely relevant than not") once your profile clears the calibration floor (≥50 labels + ≥10 effective negatives); below the floor it stays EXACTLY the old 0.55, so a fresh install is byte-identical to before. (2) **The section is now top-k = 10** above that cut (your stable-count call), so a hyperactive feed day can't flood it. Also: dismisses/thumbs now record the score you saw at label time (`score_at_label`, D14 from the refined plan) — pure future-eval bookkeeping, never shown. | A hardcoded 0.55 means nothing across users/segments once scores blend signals; the calibrated p=0.5 point is the only threshold with a stable meaning. Platt is monotone, so ordering never changes — only where the header sits. | Say the word to move p_min (0.5 → stricter 0.6/0.7), retune k=10, or drop the top-k cap back to "everything above the cut". |
| 2026-07-07 | **You can now send non-arXiv papers (chemRxiv/bio/med) to Claude routines — the standing PS.1/PS.2 carve-out is resolved (Phase P-Dispatch).** Two things to know. (1) **Accepted residual — the first non-arXiv dispatch can break a routine that hard-codes `arxiv_id`.** Payloads stayed **additive** (`arxiver/v1` unchanged; arXiv dispatches byte-identical); a non-arXiv paper carries `source`/`native_id`/`url` with `arxiv_id`/`abs_url`/`version` structurally absent. If one of *your* routines reads `arxiv_id` as a required field, the first chemRxiv paper you send it reads null there. The dispatch envelope prose + the in-app "copy starter instructions" teach the `arxiv_id`-vs-`source` discriminator, but a routine you hand-wrote before P-Dispatch won't know it until you update that routine. (2) **`deep_dive` on a non-arXiv paper is honestly degraded, not blocked** — the payload flags `pdf_fetchable: false` (chemRxiv PDFs are Atypon cookie-walled; bio/med are conservatively marked too), so the routine works from the abstract; the action stays enabled. | Additive-over-a-`v2`-bump is the right call for a single-user app — a v2 cut would strand every existing arXiv routine for zero capability additive can't deliver. The residual is "update your own routine when you start sending it non-arXiv papers," recorded so it's a conscious choice, not a silent break. ROADMAP Phase P-Dispatch + decision log; SPEC-CLAUDE-BRIDGE §4.1. | Want a smarter `pdf_fetchable` (mark bio/med fetchable) or the `deep_dive` starter template softened? Both are tracked backlog — say the word. Prefer a hard `arxiver/v2` cut over additive? Reversible, but it breaks existing arXiv routines. |
| 2026-07-06 | **P4.2 thumbs — two UX calls (you already chose row-level placement).** You picked **row-level** thumbs (on each Today inbox cell) over detail-screen. On top of that I made two adopted judgment calls: (1) **thumb-down is a SOFT "less like this"** — it teaches the ranker to demote similar papers next pass but **keeps the paper in the inbox**, unlike a dismiss (which removes it); (2) **no snackbar on a thumb tap** — the filled/outline icon (with state-aware TalkBack descriptions) is the confirmation. | (1) Thumbs are a "more/less like this" *preference* signal that should be orthogonal to *triage* (save/dismiss) — collapsing thumb-down into dismiss would lose the "I still want to see it, just less of this" case. (2) A snackbar per tap on a self-evident visible toggle is noise; the icon state already communicates it (this dropped the planned `today_snackbar_vote_*` strings). ROADMAP P4.2 + decision log. | Want thumb-down to also remove the paper (dismiss-like)? Or a confirmation snackbar per vote? Or the whole control moved to the detail screen instead of the row? Say which and it's a small change. |
| 2026-07-06 | **Phase P4 approved + a model ruling: Rocchio two-sided centroid, NOT an on-device classifier.** The inbox-ranker deepen (dismiss-as-negative + follows-cold-start + explicit thumbs) uses a two-sided Rocchio centroid: it reuses the shipped `KMeans`, stays deterministic, keeps the score clamped to `[0,1]` (so the 0.55 "Likely relevant" cut + `ScoreBar` are untouched), and is **bit-identical to today's ranker when you have no dismisses** — a purely additive deepen. Also note: my validation found the basic ranker **already ships and runs live**, so this is a deepen, not the greenfield build the request assumed (the stale HUMAN §3 row is self-healed below). | The P4 ROADMAP line literally says "logistic-regression / linear-SVM", but a classifier breaks the score scale (forces re-deriving the 0.55 threshold + `ScoreBar`), is non-deterministic without care, and is unstable at realistic class imbalance (few dismisses vs many saves). Rocchio is the safer additive first step, and the classifier *wants* the durable `paper_feedback` label table this phase builds anyway — so it's deferred to a tracked backlog follow-on, not dropped. ROADMAP Phase P4 + decision log; SPEC-SEARCH §5 (rewritten in P4.1). | Want the full on-device classifier now (accepting the threshold/ScoreBar rework + extra device-verify)? Say so and I'll pull the backlog "P4.x — classifier" forward. |
| 2026-07-04 | **Removed the Settings -> Chat history link** (PC.3): the promoted Chat tab is now the single entry point to chat history. | Two entries with divergent Back semantics (a stacked Settings screen vs a top-level tab) shouldn't coexist and confuse the back stack. | Want the Settings link back? Re-add a `SettingsLink` navigating to the Chat tab - trivial and reversible. |
| 2026-07-04 | **Explore merge narrows the category-directory discoverability** (PC.2): the taxonomy moved from an always-one-tap Browse tab to Explore's Library-scope blank-query resting state. | The approved merge folds two tabs into one to make room for Chat; the taxonomy has to live inside Explore. Fresh opens + the Today CTA land on it, but a mid-session arXiv-scope user has no signpost back. | If this bites in use, a persistent "categories" affordance (always reachable, incl. from the arXiv scope) is the recorded backlog fix - say the word and it moves up. |
| 2026-07-04 | **The unbuilt "recent queries persisted" Search spec bullet was dropped** (PC.2 SPEC-UI rewrite) rather than carried into Explore. | It was aspirational and never implemented; keeping a dead spec line invites confusion. | Want recent-queries? It becomes a real backlog item on Explore, not a silent spec ghost. |
| 2026-07-04 | **`windowSoftInputMode=adjustResize` set activity-wide** (PC.1). | The new full-screen conversation needs the composer to rise above the keyboard; the M3 modal sheet handled IME implicitly, a Scaffold does not. adjustResize + `imePadding` is the standard Compose pairing. | Affects every screen's IME behavior — if any existing input regresses (VERIFICATION M-PC1-1 checks the known ones), revert to `adjustPan` on that finding and re-scope. |
| 2026-06-27 | **Personal adversarial-validation step is now part of the loop.** After any planning Workflow, Claude verifies the plan's load-bearing claims against the actual code, attacks the riskiest forks, hunts loose ends, and refines the plan to its strongest in-scope form *before* self-approving. | Agents draft; Claude is the accountable owner. On PH.3 this pass corrected 3 real defects in the Workflow's plan and caught a malformed-fixture flaw before anything shipped. Canonical in `CLAUDE.md` §Operating model. | Tell Claude to trust Workflow output directly, or change what the pass gates on. |
| 2026-06-27 | **P-HTML source chain:** native `arxiv.org/html` → `ar5iv.labs.arxiv.org` → existing PDF viewer (never-strand floor). | Primary-source verified: native covers ~Dec-2023+, ar5iv covers pre-2023 on the same LaTeXML pipeline, PDF already works. `docs/SPEC-P-HTML.md` §2. | Re-rank/drop a source in SPEC-P-HTML §2. |
| 2026-06-27 | **No host-keying on the arXiv rate limiter** — the whole arXiv fetch group (Atom + PDF + HTML) shares one global ≥3s `ArxivRateLimiter`. **⟶ REVERSED 2026-07-05 (P-Sources, your explicit nod):** non-arXiv PDF/asset hosts get their OWN ~1.2s polite mutex; arXiv KEEPS the ≥3s singleton (a structural test enforces arXiv-host ⇒ the ≥3s singleton — no per-call bypass). | Original rationale ("zero red-line gain") held *while every host was arXiv*; multi-source PDFs (chemRxiv/bioRxiv) must not eat arXiv's 3s slot or serialize behind arXiv traffic. Surfaced as an explicit red-line reversal (like the PT.3 S2-gating one), not adopted silently. SPEC-P-SOURCES §5. | Revert to the global ≥3s for all hosts. |
| 2026-07-05 | **PS.1 shipped — chemRxiv first-class (import → PDF → organize → cite/share).** One judgment call worth your eye: **send-to-Claude (routine dispatch) is now hidden for a non-arXiv paper**, and `DispatchRepository` filters chemRxiv papers (and non-arXiv corpus neighbors) out of any routine payload. Routine payloads are arXiv-shaped (SPEC-CLAUDE-BRIDGE: eprint id + arxiv.org URL); dispatching a chemRxiv paper would mint a mangled arxiv.org URL. So **you can't currently send a chemRxiv paper to a Claude routine** — source-aware payloads are scoped to PS.2 (they touch the payload schema + golden files). This also fixed a crash I found that the planning judges missed: multi-selecting a chemRxiv paper for dispatch from the Library would have crashed. | The arXiv-only gate is the safe, correct interim (a chemRxiv paper in an arXiv-shaped payload is wrong, not just unstyled). Everything else about a chemRxiv paper is first-class: read its PDF, organize it, cite/share it (DOI link), search it. ROADMAP PS.1 row + Decision log. | If sending non-arXiv papers to routines matters before PS.2, say so and I'll pull the payload source-awareness forward. |
| 2026-07-06 | **PF.3 shipped — new-source follows (Research Square/SSRN/Preprints.org/PsyArXiv) + a source/category follow picker.** Two judgment calls worth your eye. (1) **Unfollowing now also removes that feed's already-arrived inbox items** — previously an unfollow left them in the Today inbox. I changed this so a follow's inbox rows are cleaned in the same op as the unfollow (else, as multi-source follows grow, rows would dangle on a deleted follow); it also applies to arXiv unfollows. (2) **New-source PDFs stay "open in browser"** — researchsquare.com / ssrn.com / preprints.org / osf.io are NOT on the egress allowlist, so their papers are discover-and-read-metadata + external-open only (same as chemRxiv's cookie-wall). Also: the OpenAlex "category" is a **Field** (26 top-level; I live-verified the ids from a residential IP — a design agent had Chemistry mapped to the wrong id, which would have silently broken every chem follow). The picker + all category lists are hardcoded (zero network on open — the OpenAlex credit budget). | (1) Clean unfollow semantics + prevents orphaned inbox rows as sources multiply; a paper you still want is saved/imported, not left in the triage queue. (2) Fails closed on un-vetted publisher hosts (red line); each host needs a per-source cookie-wall/redirect check before in-app PDF. ROADMAP PF.3 row + decision log; `docs/SPEC-P-FEEDS.md` §6. | Want unfollow to KEEP arrived inbox items (revert the cleanup)? Or want a specific new-source PDF host allowlisted for in-app read (say which — I'll do the per-host verification first)? Say the word. |
| 2026-07-06 | **Phase P-Feeds APPROVED (your steer) — a hybrid in-house multi-source engine; PF.0 shipped.** Two things you should know. (1) **Your shipped chemRxiv search (PT.4) is broken in production** — `chemrxiv.org`'s API is Cloudflare-gated to scripted clients (I proved it from a residential IP: curl AND the app's real OkHttp both get a synthetic 404; every open-source wrapper hit the same wall and gave up). The endpoint is correct — it's a CF gate, not a bug. **P-Feeds fixes chemRxiv discovery** by going through **OpenAlex** (un-gated), which re-serves chemRxiv + bio/med + arXiv + sources we lack. But **chemRxiv in-app PDF stays "open in browser"** — its PDF url 301-chains to the Atypon cookie-wall (verified), so we degrade gracefully rather than fight it. (2) **Architecture you chose: hybrid, not wholesale OpenAlex** — native for arXiv + bioRxiv/medRxiv (`api.biorxiv.org`: fresher, un-metered, native categories), OpenAlex only for chemRxiv + new sources (Research Square/SSRN/…). Two new egress hosts (`api.openalex.org`, `api.biorxiv.org`) — **approved with the phase**. OpenAlex is metered (2026), but per-device the free budget is ample; a **BYOK OpenAlex key** (PF.4) is the power-user escape hatch. | The hybrid keeps control + freshness where native APIs are clean, and uses OpenAlex only where we genuinely can't reach a source ourselves (chemRxiv-CF, API-less new ones). ROADMAP P-Feeds + decision log; `docs/SPEC-P-FEEDS.md`. | Narrow the scope (PF.0–PF.2 only, skip new sources/BYOK), or reverse the hybrid call — say the word. |
| 2026-07-05 | **PS.2 SHIPPED — bioRxiv/medRxiv importable + readable, S2's other OA results read-only.** Two things worth your eye. (1) **Partial in-app coverage, stated honestly:** a bioRxiv/medRxiv paper from a Semantic Scholar search is importable-and-readable-in-app **only when S2 hands back a direct `www.biorxiv.org`/`www.medrxiv.org` PDF URL**; when S2 gives a `doi.org` or publisher-host OA URL instead, the paper stays **read-only** (cite + open-in-browser), never rendered in-app. I chose to **store S2's OA URL verbatim (R0)** and host-gate it, rather than synthesize a `<doi>vN.full.pdf` URL (the planned R1) — synthesis needs a version the search API doesn't give and guesses at the host; storing what S2 actually returns is smaller and can't silently 404. So coverage is real but not total, and I'd rather say that than overclaim. (2) **You still can't send a bio/med (or chemRxiv) paper to a Claude routine** — same arXiv-only payload gate as PS.1; I closed the PS.1-deferred bug where an all-non-arXiv multi-select *dispatch* would POST a paperless envelope (now a clean "no arXiv papers in the selection" message). | R0 is a correctness/scope-honesty win (adopted directly per your "choose the better design" directive); host-gating fails closed on any arbitrary publisher host (red line). Full detail in ROADMAP PS.2 row + decision log; SPEC-P-SOURCES §3 R-table updated. | Want bio/med papers sendable to routines (source-aware payloads), or the R1 URL-synthesis built to widen in-app coverage? Both are tracked backlog — say the word. |
| 2026-07-05 | **Phase P-Sources APPROVED (your steer: "approve and include PS.2, take your time — make the implementations and related surfaces rich and sophisticated").** Committed cut = **PS.0** (source-identity abstraction + additive `MIGRATION_6_7`, dark) + **PS.1** (chemRxiv first-class: import → PDF → organize) + **PS.2** (bioRxiv/medRxiv PDF import). The central architectural call — **keep the opaque `papers.id` string PK, add additive `origin`/`native_id` columns, re-key ZERO rows** — is validated at file:line (`PaperEntity.kt:17` opaque PK; the scariest migration is *designed out, not attempted*). Per your "rich and sophisticated" steer I am NOT taking the bare-minimum cut — the reader/detail/organize/import surfaces get first-class multi-source treatment (guarding against gold-plating that isn't genuine value). | Closes the read-only gap P-Tools created (the §3 generic-import carve-out); you relaxed the "demand-gated minimal" framing toward richness. Governed by `docs/SPEC-P-SOURCES.md`. | Re-scope (narrow to PS.0+PS.1, or add PS.3 follows/feeds) anytime. |
| 2026-06-27 | **Semantic Scholar's 1.2s self-spacing mutex is the sole documented exception** — it stays on the bare (un-gated) client. **⟶ SUPERSEDED 2026-07-04 (P-Tools PT.3): S2 moves onto the `@ArxivClient` gated client** (the mutex stays; see the P-Tools row below). | Different API governance; `api.semanticscholar.org` is already allowlisted if it ever migrates onto the gated client. SPEC-P-HTML §7. | Direct S2 onto the gated client (now being done in PT.3). |
| 2026-06-27 | **`reader.css` is inlined into one `<style>` from a `:core:ai` resource; the reader is a natively-scrolling full-screen page** (no `file://` `<link>`, no `arxiver://height` self-size script). | A `file://` `<link>` can't load under the reader origin's `allowFileAccessFromFileURLs=false`; the self-size script is for inline answer blocks, not a full-screen destination (both were dead code the validation pass removed). SPEC-P-HTML §9. | N/A unless the reader stops being full-screen. |
| 2026-06-27 | **`core/ai/src/test/resources/phtml/` fixtures are trimmed, well-formed excerpts of real arXiv HTML** (`2412.19437`, `2510.04905`, `1706.03762`). | Goldens need real LaTeXML markup. The HTML edition inherits each paper's arXiv license and is **display-only** — Arxiver never re-hosts or exports rendered HTML (SPEC-P-HTML §1/§11). **Licensing FYI:** these are short third-party research-fixture excerpts; if the repo ever goes public, confirm that's acceptable or swap for synthetic. | Flag if the corpus should be synthetic-only or license-annotated. |
| 2026-07-03 | **Qwen readiness hotfix (PA.6) is structural, not a one-liner.** Your device report ("Qwen downloaded but 'No AI provider set up'") was a stale hand-enumerated readiness seam in DI. Instead of adding `\|\| qwen.isReady()`, the resolver now delegates to `OnDeviceProvider.isReady()` — the same engine list that serves chat — so a future engine can never be forgotten again; a structural test makes a revert a red build. Also shipped in the same PR: a **low-disk crash guard** (first on-device turn on a nearly-full phone previously hard-crashed the app — native SIGABRT — now a graceful storage error) and honest settings copy (the screen claimed on-device "arrives in a later update"). | Kills the bug class, not the instance; the incident post-mortem is in the ROADMAP decision log (2026-07-03). | Ask for the minimal one-liner instead, or reshape the guard's free-space bound. |
| 2026-06-27 | **PH.4 reader build choices:** the ViewModel exposes a **theme-free `ReaderDocument`** and the screen derives the final HTML in `remember(doc, theme)` (light/dark for free, VM stays Compose-free); MVP loads with **`baseUrl = null`**; **"Read PDF instead" is an always-present toolbar action** (the never-strand floor is one tap from every state). | Smaller, more testable surface than the blueprint's VM↔theme handshake + sandbox-relocation (the adversarial pass dropped both as over-builds). SPEC-P-HTML §9/§11/§12. | Reverse any of these in SPEC-P-HTML. |
| 2026-06-27 | **PH.5 figures use `data:`-base64 inline, NOT the SPEC's reserved virtual origin.** Pre-fetched figure bytes are base64-inlined into `index.html`; `baseUrl` stays `null`; both `blockNetworkLoads` and `blockNetworkImage` stay **armed**. | An https virtual origin is suppressed by `blockNetworkImage=true` → would have forced it **off** (disarming an egress control + needing your sign-off); `data:` keeps both blocks armed, adds no dependency, and the validation pass confirmed it. **No red-line escalation was needed** precisely because of this choice. SPEC-P-HTML §8/§12. | Switch to a virtual origin (would re-open the `blockNetworkImage` red-line question). |
| 2026-06-27 | **PH.5 figure caps are PROVISIONAL** (≤40 figures, ≤600 KB each, ≤4 MB total, 30 s deadline) and constructor-injectable. On the ≥3s arXiv limiter the 30 s deadline admits ~8–10 figures on a first online open; the rest stay figcaption placeholders. The shared limiter means a figure-heavy open **bounded-delays** (never starves) a concurrent FollowSync. | Real figure sizes/counts + low-RAM `loadDataWithBaseURL` behaviour are device-facts CI can't measure; these are conservative defaults. | Retune the caps after the §2 device session (no code change — they're DI params). |

| 2026-07-05 | **PT.4 SHIPPED — `search_chemrxiv` (new host `chemrxiv.org`), the last external source.** chemRxiv is the one new egress host in the phase, which **you pre-approved**. It runs on the same host-gated + consent-gated + egress-honest machinery as the others (its own 1.2s polite mutex, not the ≥3s limiter). One thing to know: **I could not reach the live chemRxiv API from here** (Cloudflare bot-gates datacenter IPs, same as CI), so I grounded the response shape against a real open-source Open Engage wrapper's parsing code rather than a live GET — the fields are conservative (a renamed key degrades to empty results, never a crash). A **live-GET confirmation on your device** is the one verification I can't run — it's logged as `VERIFICATION.md` §Q-PT11. chemRxiv hits are DOI-keyed, so they're **read-only** (no import) — which keeps the generic-import carve-out (§3) exactly where it was. | You pre-approved the host; the read-only choice avoids the generic-import schema decision; the wrapper-grounded envelope is the best available without a reachable API. ROADMAP PT.4 row + SPEC §7. | If chemRxiv search returns empty on your device, the live envelope differs from the wrapper's — send me one real `/items` JSON response and I'll pin the DTOs exactly. |
| 2026-07-05 | **PT.3 SHIPPED — Semantic Scholar search + S2 host-gate move + optional BYOK key.** Three things worth your eye: (1) the **S2 client moved onto the gated client** (the 2026-06-27 "S2 stays bare" call, reversed as pre-planned) — this makes the `api.semanticscholar.org` gate actually fire; the 1.2s politeness mutex is preserved and CitationSync is byte-untouched (no ≥3s over-throttle). (2) A **latent bug fixed:** S2 was sending **no API key at all** (the key was captured at app-build time AND never wired) — now a per-request supplier, so a key you enter later is honored. (3) The optional S2 key gets a **Settings card** ("Semantic Scholar", no Test button — S2 backs a tool, not chat; free tier works keyless). The adversarial review also caught that my PT.2 disclosure code hardcoded "arXiv" — a new S2 tool would have said "searches your device" for a query that left, or "Searched arXiv" for an S2 egress; both fixed (each external tool now names its own host). Scope trimmed: S2 details/batch/recommendations → backlog (no MVP caller). | The host-gate move is a security tightening you pre-approved; the BYOK fix makes the advertised key actually work; the disclosure fix keeps the "what leaves the device" promise honest per host. ROADMAP PT.3 row + decision log + SPEC §7/§9/§10. | Prefer S2 wired but the key card hidden (vault-only)? Or want details/recommendations pulled forward? Say the word. |
| 2026-07-05 | **PT.2 SHIPPED — first external egress, two-gate consent, composer deck.** The load-bearing autonomous call: PT.2 added a **new `web_search_enabled` column** (`MIGRATION_5_6`) rather than reusing PT.1's `tools_enabled` for the web gate. Reusing it would have silently flipped every existing "library-on" conversation to "web-on," egressing queries to arXiv without consent — a red-line data-semantics bug. So `tools_enabled` stays the library gate; a separate column gates external. Also shipped: the composer deck (2 chips), execute-time refusal of a web-off external call before it egresses, egress-honest activity bubbles + confirm wording. **One accepted residual (safe direction):** re-importing an already-saved paper (a local check, no fetch) still shows the external "left your device" bubble label — over-disclosure at draft granularity, never under. | The anti-repurpose choice protects shipped users' consent; the residual errs toward over-disclosing egress, which is the safe side of a privacy line. ROADMAP PT.2 row + decision log + SPEC-P-TOOLS §8/§9. | Want the residual made exact (a result-granular bubble), or the web gate defaulted ON? Say the word. |
| 2026-07-05 | **PT.2 extended: a per-message tool deck in the composer (your steer) + "always choose the better design" is now an operating principle.** The PT.1 "Search my library" toggle moves out of the sheet chrome into per-message chips in the input deck (like ChatGPT/Claude) — a library chip + a web-search chip, applied per-message (the gate is already per-request), sticky/remembered, each with its own honest disclosure; external tools get their own consent (library-yes/web-no is possible). Designed once in PT.2 alongside the external tools. And your standing directive — *proactively surface AND adopt the better design, don't just execute the plan* — is now in `CLAUDE.md` §Operating model (step 3) + agent memory. | Matches user expectations from popular chat apps + gives finer control; the per-request machinery already exists, so it's mostly relocating the control. The principle is written into the loop so it holds across sessions/environments. ROADMAP PT.2 row + SPEC-P-TOOLS §9. | Want the composer chip sooner (a "PT.1.5" before PT.2)? Or a different persistence (per-message resets vs remembered)? Say the word. |
| 2026-07-05 | **"Search my library" is a per-conversation OPT-IN, default OFF (PT.1)** — your steer. The library tool attaches only when the user flips the surfaced toggle; its hint discloses the egress ("matching papers' abstracts are sent to the cloud model"), so the toggle IS the disclosure (the inline activity bubble still shows each query). Persisted on `chat_sessions.tools_enabled`. | Privacy-first + matches the app ethos: by default a chat sends only the scoped paper's content (existing RAG); the user opts in to let the model search their *other* saved papers. Discoverable + reversible. Resolves the disclosure-gap I'd flagged. SPEC-P-TOOLS §9/§11. | Prefer **default ON** (feature works out of the box, opt-out for privacy)? One-line flip (the UI seed for a new conversation) — say the word. Note: the external web tools got their **own** separate gate in PT.2 (`web_search_enabled`), so library-yes / web-no is possible — resolved. |
| 2026-07-04 | **P-Tools architecture: the tool loop lives ABOVE the provider** (a repo-driven `ChatToolLoop` in `ChatRepository.stream`); providers stay single-shot + gain three additive default-off fields + a `supportsTools` flag. | It's the only layer that owns persistence + the `NonCancellable` finalize + the limiter/AllowedHosts seam + the disclosure surface; a provider-owned loop can't rebuild the in-flight message list after process death and splits disclosure across `:core:ai`↔`:app`. All three judges ranked it highest. SPEC-P-TOOLS §2. | Ask for the loop inside each provider (Stance B) — reopens the process-death + disclosure-split trade-offs. |
| 2026-07-04 | **S2 client moves from bare → the `@ArxivClient` gated client (PT.3)** — reverses the 2026-06-27 row above. **✓ SHIPPED 2026-07-05:** the move landed as one DI qualifier; the gate now fires, the 1.2s mutex is preserved, CitationSync is byte-untouched. | A security tightening: `api.semanticscholar.org` is allowlisted but the interceptor never fired before, so the gate was aspirational. The 1.2s self-spacing mutex stays (S2 is exempt from the ≥3s arXiv limiter). SPEC-P-TOOLS §10. | Keep S2 bare (restores the 2026-06-27 decision). |
| 2026-07-04 | **`chemrxiv.org` added as a new egress host (PT.4)** — the one new host in the phase, **pre-approved by you**. | Cambridge Open Engage's search REST API (chemRxiv); own polite mutex, `@ArxivClient` gated, no off-host CDN redirect on assets. SPEC-P-TOOLS §10. | Drop chemRxiv from P-Tools (keeps the host set at the P-HTML five). |
| 2026-07-05 | **PT.0 shipped the tool-loop plumbing tool-free in prod + deferred the visible consent toggle & activity-bubble to PT.1.** My validation cut two over-builds: (a) the planned `core/claude PayloadBuilder` edit was spurious (that's the Routines dispatch envelope, unrelated to chat tools) — cut, so its golden stays green unedited; (b) a visible "Enable web search" toggle gates nothing until a real external tool exists (PT.2), and PT.0's echo tool is a **test fixture only** — so prod offers **no** tools yet. PT.0 lands the loop, migration (`tools_enabled` column), disclosure, and red-line guards; PT.1 wires the first real tool + the visible toggle + the activity bubble. | The `tools_enabled` column + repo seams ship now (schema stability); shipping a toggle that does nothing would be misleading UI. The loop is fully CI-tested (SSE reassembly, cap, dangling-guard, byte-identity) — device verification rides PT.1's first real tool. | Want the echo tool wired in prod for a device smoke-test of the loop, or the toggle shipped early? Say so. |
| 2026-07-04 | **Tool consent is PER-CONVERSATION, not per-call** (an "Enable web search for this chat" toggle) + a persistent inline **activity log** as the real disclosure surface for mid-loop queries; local `search_my_library` is zero-egress and needs no consent. | A per-call gate would be unusable for an agentic loop that makes several calls per turn; the confirm can only show the *initial* request, so the model-minted queries that egress mid-loop are disclosed live in the transcript (query + source + `egress` flag). SPEC-P-TOOLS §9. | Ask for a per-call confirm, or an always-off default. |

**Approved by you during P-HTML planning (2026-06-27), now built:** add `ar5iv.labs.arxiv.org` as a new
network host · strip images in the MVP (figcaption placeholders; pre-fetch → PH.5) · additive "Read HTML"
button (augments, never replaces, the PDF entry) · fix the pre-existing PDF rate-limit bypass in its own PR (PH.2).

**Greenlit by you for P-Tools (2026-07-04), now being built:** full Semantic Scholar (search + details/batch +
recommendations; bioRxiv/medRxiv/PubMed via S2's index) · a robust `chemrxiv.org` host implementation (PT.4) ·
an optional free S2 API key as BYOK (`EncryptedSharedPreferences` via `AiKeyVault`) · plan-approval **delegated**
to Claude ("proceed with planning and implementing maximally") — so P-Tools runs autonomously on the loop.

---

## §2 — Needs from you (the loop can't do these)

| Status | Item | Detail |
|---|---|---|
| 2026-07-10 | **The one number that decides P5.4: your real-profile "Ranker health" card** — and a strong *advisory* lean toward NO-GO from an emulator experiment. On a debug build, give the app ~2 weeks of natural triage (or one deliberate session: ≥15 thumb-downs/dismisses on papers you genuinely don't want, plus your normal saves), let a sync run, then read Settings → Ranker health (debug) and tell me the per-segment AUC / P@10 lines. **What I did this session (Ultracode workflow + my own adversarial validation):** I couldn't close this gate for you — a workflow's two independent validity passes both concluded that *no emulator-synthetic profile can close it* (my synthetic labels are a different beast from your organic preference, and they're partly baked into the same embeddings being ranked). But I could do better than the useless diffuse AUC-0.50 profile: I built a realistic **coherent** profile on the emulator (interest = LLM-agents/reasoning/RL; hard-ish negatives = vision/image/diffusion — a sibling ML subfield) and ran the real harness. **Result: Rocchio AUC 0.89 [0.81–0.97], P@10 1.00** — i.e. on a realistic coherent profile the *existing* ranker already nails the top-10, which is exactly the "Likely relevant" section a learned head would try to improve. So the recorded rule's "healthy AUC + P@10 near ceiling → no-go" branch is met *in simulation*. **I deliberately did NOT build the learned head to test this further** — that would be building most of P5.4 just to decide whether to build P5.4, and it still couldn't close the gate. **What's left for you:** your real numbers are the sole gold gate. Healthy (like the sim) → P5.4 closes no-go and Phase P5 checkpoints; mediocre precision on sufficient data → I build the gated head. (Details: VERIFICATION.md B2b; the disposition is logged in ROADMAP P5.4.) |
| OPEN | **Device-verify P4 two-sided inbox ranking** (`VERIFICATION.md` §B2a) — needs real on-device embeddings. What hardware confirms: dismissing papers on a topic demotes *similar* inbox papers below the 0.55 "Likely relevant" cut on the next scoring pass (the dismiss signal now teaches the ranker, not just hides), an undone dismiss removes that influence, and a follows-only user (<10 library papers) gets a scored ordering instead of pure recency — with a sanity check that the "Likely relevant" section isn't the entire inbox in cold start. All on-device, zero egress. None block the merged code. | `VERIFICATION.md` §B2a |
| OPEN | **Device-verify P-Tools live tool-use** (`VERIFICATION.md` §Q-PT1–6) — needs a real cloud key. The loop/registry/migration/disclosure are fully CI-tested; what hardware confirms: a live `search_arxiv`/`get_paper`/`import_to_library` round-trip (fragmented tool-call SSE reassembles), the **import→reader loop-closer** (an imported paper opens in the P-HTML reader by id, no re-fetch), the **two-gate consent** (library-yes/web-no really blocks arXiv egress; a web-off external call is refused with no egress), per-message chip stickiness across process death, and ≥3s spacing on a multi-call turn. None block the merged code. | `VERIFICATION.md` §Q-PT |
| OPEN | **Device-verify Phase P-Chat** (all of `VERIFICATION.md` §M-PC1/§N-PC2/§O-PC3/§P-PC5 in one session). Key checks CI can't run: the live **v3→v4 upgrade** (existing chats survive, the 2026-07-04 ghost bubble is gone, pin + rename persist across restart), the **4-tab bar** (Today·Explore·Library·Chat) label fit at 1.3× font scale, IME/rotation on the full-screen conversation, TalkBack on the new actionables, and a **backup export/import round-trip** confirming no chat content leaks. None block the merged code; they confirm the surface on hardware. | `VERIFICATION.md` §M-PC1…§P-PC5 |
| OPEN | **Device-verify the P-HTML reader — PH.4 is now shipped, checks `VERIFICATION.md` §M (M2–M8) are waiting.** A live native ≥2024 paper **and** a pre-2023 ar5iv paper render end-to-end (M2/M3); **airplane-mode** render from cache with **zero egress** (M4); **native MathML** measured with a stress sample (display eq + matrix + stretchy delimiters + scripts) on **Pixel_3a_API_34 AND an API-26/27 old-WebView AVD** (M5) — that verdict decides whether PH.8's bundled MathML fallback is needed; fallback-to-PDF + back-stack (M6); cross-ref + external-link confirm (M7); TalkBack reads it (M8). CI can't run a device; these never block `[x]` but must be observed before the reader is trusted. | `VERIFICATION.md` §M |
| RESOLVED 2026-07-04 | ~~Provision an API-26/27 old-WebView AVD.~~ **Unneeded — PH.8 closed no-go**: the M5 measurement showed native MathML adequate, and System WebView is Play-updated on every API-26+ device, so the frozen-WebView cohort the AVD would have simulated is a museum piece already served by the PDF fallback. Reopens only with evidence of a real frozen-WebView user cohort (ROADMAP PH.8 row). | ROADMAP decision log 2026-07-04 |
| OPEN | **Ratify the PH.5 figure caps on device** (`VERIFICATION.md` M-PH5-4). Open a figure-heavy/survey paper + a near-max single image on the low-RAM API-26 target; confirm `data:`-laden `index.html` renders without truncation and memory is acceptable; record measured size/heap and **retune the caps** (they're DI params — no recompile). Also exercise M-PH5-1…3,5,6 (figures render with both network blocks armed, airplane-mode zero egress, negative-egress + poisoned-cache re-gate, degrade, first-paint/no-starvation). | `VERIFICATION.md` §M (M-PH5-*) |
| RESOLVED 2026-07-04 | ~~Re-verify Qwen-only Ask on your real device.~~ **Confirmed by you: Qwen works on the original device** — K20 closed `[x]` with a dated Verification-log row. | `VERIFICATION.md` K20 |
| OPEN (non-blocking) | **Give a real contact email/URL** for the polite-pool identifiers. (a) arXiv UA — requests send `Arxiver/<version>`; etiquette wants a reachable `(+<contact>)`. (b) **OpenAlex `mailto`** (P-Feeds) — placeholder `arxiver@blokz.dev` in `AppModule.OPENALEX_MAILTO`; OpenAlex doesn't verify it but etiquette wants a real one. Claude wires whatever you publish. | SPEC-P-HTML §7 · SPEC-P-FEEDS §3 |
| FYI | **Releases/tags are maintainer-only** — tag pushes are blocked from cloud sessions; you cut `v*` tags via the GitHub UI (fires the signed-release workflow). Signing secrets are held offline by you; Claude never asks for or locates them. | memory `release-engineering-state`, `gh-secret-crlf-gotcha` |

---

## §3 — Open questions & carve-outs (awaiting a steer; non-blocking)

| Status | Item | Default Claude is taking |
|---|---|---|
| ▶ READY 2026-07-12 (P-Prove) — one device session, owner-only | **The P-Prove perf/verification instrument is built + merged (PP.1–PP.5a, all CI-green).** What it buys you: a flake-free CI perf tripwire (PP.1), a Macrobenchmark module + Baseline-Profile pipeline (PP.2), an auto-seeding benchmark corpus (PP.3a), the tracing seam + StartupBenchmark/FrameTimingBenchmark/SearchTraceBenchmark/BaselineProfileGenerator (PP.3b), a cold-start deferral (PP.4), and a curated 30-query golden relevance set (PP.5a). **Now it needs ONE device session on the Samsung S20** (not the x86_64 emulator — perf numbers are host-CPU/thermal-dependent) to turn "green build" into "verified": run the runbook in **VERIFICATION.md §D** (D1–D4) + **§C C3** + **§E E1–E3** + **A1**. **Two things are yours alone:** (1) **provision the BGE model on the device first** or SearchTraceBenchmark/D2 fail loudly by design (Finding #1); (2) **CHECKPOINT 6 needs ≥2 live catalog-template routines with a real TokenVault token** (G1/G2/H3 — never fabricated). PP.5b (the C3 eval harness) is intentionally left to build *with the device in hand* rather than shipped unverified. Only when D1–D4/C3/E/A1 are green AND G2 runs do CHECKPOINT 5 + 6 + P5 close together — and P5.4 (the gated learned head) gets its real decision. | Instrument shipped autonomously; the device session + real-routine run are yours (nothing else blocks). Ping me to build PP.5b's harness live during the session, or to walk the runbook. |
| ✅ (a) RESOLVED 2026-07-12 · (b) recommendation stands · (c) shipped | **(a) APPROVED — Jetpack Glance for PA.2.** The Co-Founder green-lit adding `androidx.glance.appwidget` (+ other deps as needed) — PA.2's home-screen widget is unblocked; the dep is added when PA.2 is built, not before. **(b) PA.3 recommended CUT:** a `to_read` "reading queue" shelf can't honestly be called "unread/waiting" (nothing auto-advances the status) and duplicates the Library tab — recommend cutting it and folding an *honest* "Continue reading" into the queued **P-Read** phase (which owns the reading-position substrate). Recommendation stands; no objection recorded. **(c) Metered-only reality:** scoring (hence the digest + widget) runs only on **unmetered/Wi-Fi**, so a cellular-only user sees an empty digest until Wi-Fi — shipped the "requires Wi-Fi sync" hint on the opt-in toggle (PA.1b); it's inherent (scoring needs the embedding pass), not a bug. | (a) Glance approved — added at PA.2 build time; (b) cut PA.3, defer honest continue-reading to P-Read; (c) shipped with the Wi-Fi hint + disclosure. Say the word to ship the "Recently saved" shelf anyway or change the metered posture. |
| DECIDED 2026-07-12 (P-Prove PP.3a) — autonomous, overridable | **Benchmark corpus seeding is OFF in `debug`, not just `release`.** The PP.3 perf benchmarks run on the plugin's non-debuggable `benchmarkRelease` variant, so a debug build never needs the synthetic corpus — and seeding it would inject ~5,000 fake "Seeded Paper" rows into *your real `arxiver.db`* on every debug launch (idempotently sticky until you clear app data). So `ENABLE_TEST_CORPUS` is `true` ONLY on the two ephemeral benchmark variants (`benchmarkRelease`/`nonMinifiedRelease`); `release` **and** `debug` stay `false` (verified in the generated `BuildConfig`). Net effect: your dev builds stay clean. | Kept debug OFF. Say the word if you'd rather a debug build auto-show the seeded feed for manual perf eyeballing (one-line flip). |
| PRECONDITION 2026-07-12 (P-Prove PP.3b/PP.5) — device session, owner-only | **The BGE model must be provisioned on the S20 before the D2 search / D4 profile benchmarks.** On a fresh benchmark install the pinned BGE ONNX model isn't downloaded, so the semantic search leg is skipped and `SearchTraceBenchmark` would silently measure keyword-only (not the true <300 ms D2 that includes the ~100–200 ms `embedQuery` JNI). PP.3b builds `SearchTraceBenchmark` to *assert model-ready* so a mis-provisioned run fails loudly rather than reporting a bogus number — but you must let the model finish downloading on the test device first. Recorded now so the one device session isn't wasted on an empty index. | Building the loud-fail guard; the model-download step is yours at device time (will be in the VERIFICATION.md runbook). |
| FYI 2026-07-11 (red-line audit) — defense-in-depth follow-up, tracked not built | **Egress host-gate has no blanket structural test** — unlike the backup wall (which has the descriptor-walk guard) or the rate limiter (`NoDirectNewCallStructuralTest`), the `@ArxivClient` host-gate is enforced by DI-qualifier wiring + targeted tests, so a *future* new bare `OkHttpClient().newCall(...)` to an un-allowlisted host wouldn't trip a test. **No exposure today** — all four current bare clients hit hardcoded/pinned/user-URL targets (Anthropic, Gemini, ModelDownloader→huggingface, RoutineTrigger). A symmetric structural guard (assert every OkHttpClient is either gated or in a documented exemption set, mirroring the `NoDirectNewCall` exemption pattern) would close it, but it needs its own small design (structurally detecting client construction + the exemption model), so I didn't gold-plate it into the audit's fix PR. | Not building unless you want it; recorded so a future hardening pass has the target. Say the word to pull it forward. |
| OPEN — carve-out (P5 refinement, 2026-07-10) | **A Settings "reset personalization" control.** The ranker now accumulates durable per-user state (feedback labels, the fitted calibration/λ in `relevance_model`). There is no user-facing way to wipe it and start fresh — e.g. after a research-interest pivot, or handing the device to someone else. Cheap to build (delete `paper_feedback` + `relevance_model` + re-run the worker), but it's a user-visible destructive control, so it deserves your placement/wording call. | Not building until you want it; the state is local-only and excluded from backups either way. |
| FYI 2026-07-10 (PE.3) — three deliberate scope calls, each overridable | **(a) External search is un-paginated v1** — 25 top results per submit, no load-more. Pagination on a metered (and BYOK-billed) API needs an explicit "Load more" tap design, deferred with a breadcrumb. **(b) The online source resets to arXiv each session** (it survives rotation/process death via SavedStateHandle, but is not persisted across sessions — a DataStore preference is the carve-out). **(c) The bio/med lag hint ships as a hedge** ("may lag the native feed by a few days") — the measured figure was ~1–3 days on one day's sample; if you want a number in the UI it needs a dated re-measurement first. | Un-paginated + arXiv-default + hedged copy. | Say the word to add "Load more", persist the last-used source, or ship a measured lag figure. |
| FYI 2026-07-10 (PE.1) — **your "keep all 26" call was right; my three subsets were wrong** | **I measured every source's real discipline mix instead of guessing.** A live OpenAlex census (`group_by=primary_topic.field.id`, 2024+ works) overturned all three curated subsets I'd proposed: chemRxiv's "chemistry-adjacent" nine silently dropped **Computer Science (4.68%)** and **Medicine (3.85%)** — comp-chem and med-chem are real; PsyArXiv's obvious three dropped **Medicine (9.04%)**, i.e. clinical psychology; and **SSRN was badly wrong** — the intuitive {Econ, Business, Social, Decision} covers just **27.9%** of recent papers and omits **Engineering, its #1 field at 21.87%** (post-Elsevier SSRN is a broad STEM firehose; the law/finance model only holds all-time). Meanwhile **your instinct on the megajournals was confirmed**: Research Square and Preprints.org have genuinely flat distributions (top-10 covers only 80–84%; every field >0.15%), so curating them really would have hidden content. The junk you saw is confirmed noise: chemRxiv Dentistry **0.04%**, Arts & Humanities **0.10%**, Veterinary **0.02%**. | Adopted an objective rule — **show a field iff it carries ≥1% of that source's papers since 2024** — and curated lists now lead with the source's dominant discipline instead of alphabetically. This is the one place I made a taste call: chemRxiv now offers Computer Science and Medicine. | Say the word to tighten the 1% floor or drop the two chemRxiv additions. |
| ✅ FIXED 2026-07-10 (PE.1b) — **a source you could follow delivered almost nothing**  | **PsyArXiv is 98.97% DOI-null** (5,498 of 5,555 works) because it's OSF-hosted, and our ingest does `bareDoi() ?: return null` — so a PsyArXiv follow **silently discards ~99% of its papers**. Worse, PFP.3's new health hint would then show "No new papers recently" under that follow, blaming the *feed* for the *app's* drop. chemRxiv is 0.00% DOI-null, so this is specific to OSF. (Two related OSF quirks: PsyArXiv is ~100% `pdf_url`-null, and its `publication_date` is a re-harvest artifact — 99% dated 2025 — so our date windowing misbehaves for it.) | Fixing in **PE.1b**: identity falls back to the OpenAlex work id when there's no DOI, and the paper gets a canonical landing URL (rides PE.2's migration). Not fixed in PE.1 — that PR is vocabulary only, and I'd rather ship the fix with the schema change it needs than bolt it on. | Nothing needed; flagging because it means PsyArXiv has been quietly broken since PF.3 shipped it. |
| OPEN — product call (P-FeedPolish item-3, deferred 2026-07-07) | **Multi-source category badges — deferred as vacuous-as-specced.** The idea was to show, on the arXiv taxonomy grid, which sources a category is followed on. But arXiv grid codes (`cs.LG`) and non-arXiv follow values (OpenAlex `fields/16`, bioRxiv `neuroscience`) are **disjoint vocabularies**, so a per-source badge resolves to `{arXiv}` or `{}` forever — it can never light up a cross-source badge. A genuinely useful version needs a **discipline crosswalk** (arXiv group ↔ OpenAlex Field ↔ bio/med category) that doesn't exist and is a real research effort to build/maintain. The **PFP.2 "Following" manage screen** already meets the underlying "what do I follow, and where" need. | Not building badges; the manage screen covers the need. If you want the grid to reflect cross-source follow state, say so and I'll scope the discipline-crosswalk as its own phase (it's non-trivial). |
| FYI 2026-07-07 (PFP.3) | **Follow health hint — threshold + copy + color are UX calls, overridable.** The manage screen shows a soft "No new papers recently" subtitle when a follow's last **4** syncs delivered nothing (`EMPTY_STREAK_WARN`). I raised it from the plan's 3 → **4**: the streak counts sync *events* not wall-clock, and `syncNow` injects extra events, so 3 (≈18 h at the 6 h cadence) false-positives on normal niche-feed/author lulls. Copy is deliberately time-agnostic + gentle (muted `onSurfaceVariant`, not an error color) so a residual false-positive is harmless. | Threshold 4, gentle copy, muted color. Say the word to retune the number, reword the copy, or scope the hint to non-arXiv/non-author follows. |
| FYI 2026-07-07 (PFP.3) | **The health streak is a LOCAL signal — deliberately excluded from backup.** `arxiver-backup/v2`'s `BackupFollow` maps only `type/value/label/origin`, so `empty_sync_streak` is never exported; a **restored** follow starts at streak 0 (a fresh device re-earns the signal from real syncs). Correct by design — the streak reflects *this install's* sync history. Noting it so a future "why did my quiet-hint reset after restore" is already answered. | Streak stays out of backup. No action needed. |
| FYI 2026-07-07 (PFP.2) | **The "Following" screen shows ALL follow types, incl. arXiv author/query follows** — not just the non-arXiv category follows the picker creates. Rationale: it's the only surface where an arXiv **author** or **saved-search** follow can be removed at all (the taxonomy grid only toggles category follows), so scoping it to non-arXiv would leave those follows unmanageable. Grouped by source, arXiv first. | Showing all types by default (removal is the only action; no per-type gating). Say so if you'd rather the screen hide arXiv follows or split into tabs. |
| RESOLVED-FOR-SCHEMA 2026-07-05 (P-Sources) | ~~**Generic (non-arXiv) import from S2/chemRxiv.**~~ **chemRxiv (PS.1) + bioRxiv/medRxiv (PS.2) are now first-class: import → read its PDF → organize → cite/share.** The schema decision landed in PS.0 (opaque PK + `origin`/`native_id`). S2's other (non-bio/med) OA results are **read-only + external-open** (host-gated, PS.2). **Still deferred:** a universal arbitrary-DOI importer (needs Unpaywall/Crossref resolvers → backlog capstone) and the R1 `<doi>vN.full.pdf` URL-synthesis that would widen bio/med in-app coverage (backlog). | chemRxiv + bio/medRxiv done; arbitrary-DOI + R1-synthesis stay deferred. Say the word to reprioritize. |
| ~~RETIRED 2026-07-10~~ (P-Explorer PE.2h) — **the premise was wrong** | ~~**Allowlist `assets.chemrxiv.org` so chemRxiv PDFs read IN-APP?**~~ **That sub-domain does not exist (NXDOMAIN).** The PE.2h host probe established that chemRxiv's real PDFs are served from **`chemrxiv.org`**, which has been **allowlisted all along** (`AllowedHosts.kt` already carries the correction). The `assets.chemrxiv.org` strings in `AllowedHostsInterceptorTest` are *synthetic off-host placeholders*, not evidence of a real host — that's what misled the original row. **This decision was never actionable and is closed, not deferred.** The real reason chemRxiv doesn't read in-app is different (see the successor row below). | Nothing to allowlist. No action was ever possible. | — |
| OPEN — successor to the row above (revisit after P-Explorer, per your steer) | **Read chemRxiv PDFs in-app?** The true blocker is an **Atypon cookie-wall**: a cold fetch bootstraps an anonymous session via `chemrxiv.scienceconnect.io` (not allowlisted), and the shared PDF client holds **no `CookieJar`**, so we land on an HTML challenge that `PdfDownloader`'s `%PDF` guard correctly rejects → graceful open-in-browser. Making it work is a **two-change project**: add a CookieJar to the egress-gated client (new *stateful* behavior) + allowlist one publisher host. It also brushes chemRxiv's robots.txt, which disallows the `/action/*` bootstrap path. | **Browser-only** — today's shipped behavior is *correct*, not a bug. Parked at your instruction until P-Explorer ships. | Say "do chemRxiv in-app" and I'll do it behind a device check (CI can't verify — datacenter IPs get a blanket Cloudflare 403). |
| DECIDED 2026-07-10 (P-Explorer PE.2h) — **your call, authorized pending a device check** | **Research Square in-app PDF read.** The only clean new-egress candidate: both hosts are publisher-owned (`www.researchsquare.com` → `assets-eu.researchsquare.com`), content is CC BY, and no ToS/robots conflict. The catch: the PDF redirects **off-host to a regional shard**, and our allowlist is exact-match + enforced **per redirect hop** — so a shard we didn't list fails closed and looks broken. | Authorized in principle; **not added yet**. A device check must first record *which* `assets-*` shard your phone actually lands on (`VERIFICATION.md` §Q-PE-RSQUARE). Then one PR adds the exact hosts + a per-hop-redirect test. | Run the device check and tell me the shard; or say "just add assets-eu" to accept the region risk. |
| DECIDED 2026-07-10 (P-Explorer PE.2h) — **rejected on red-line grounds; FYI, no action needed** | **PsyArXiv/OSF, SSRN, Preprints.org stay browser-only — and PsyArXiv is the one I'd refuse even if asked.** OSF's only working PDF path terminates at **`storage.googleapis.com`**: multi-tenant Google Cloud Storage where the bucket lives in the URL **path**, not the hostname. Exact-host allowlisting therefore **cannot scope it to OSF** — granting it would open egress to *any* GCS bucket, a materially broader surface than every existing allowlist entry, and the `%PDF` guard cannot undo a widened network surface. SSRN's ToS **bans automated queries** (Cloudflare-challenged); Preprints.org **edge-denies** non-browser clients. Fetching either would mean circumventing an access control. | Browser-only, permanently for OSF. A structural test (`PdfAccessAllowlistTest`) now pins these hosts as un-allowlistable so a future "just add the host" change must confront this reasoning. **I also rejected spoofing a browser User-Agent** to get past bio/medRxiv's UA rules — their robots.txt welcomes an honest `Arxiver/…` agent, and a blocked honest UA means "no". | Override only with an explicit instruction; I'd argue against the GCS grant. |
| WIN 2026-07-10 (P-Explorer PE.2h + PE.0) — **no new egress needed** | **bioRxiv + medRxiv PDFs read in-app** — their hosts were *already* allowlisted, and a direct probe confirms `200`, **0 redirects**, `application/pdf`, body starting `%PDF`. The app was simply *lying about them*: `isPdfFetchable()` was `ref is ArxivRef`, which is **tautologically false** at its only call site, so every Claude routine was told a bioRxiv PDF was unfetchable. PE.0 fixes it with an evidence-backed per-source tier. The `<doi>vN.full.pdf` synthesis the backend already performs matches the pattern the probe validated. | Shipped in PE.0. Closes the recorded P-Dispatch `pdf_fetchable` deferral. One device confirmation queued (`VERIFICATION.md` §Q-PE-BIORXIV). | Nothing needed — this is a free win the audit surfaced. |
| RESOLVED 2026-07-06 (PF.3, via `sqliteMode=NATIVE`) | **The `:app` Robolectric unit suite flakes intermittently** — a Room **InvalidationTracker background refresh** hitting a closed connection (`Illegal connection pointer … @Room Invalidation Tracker Refresh`), surfacing cross-test as `UncaughtExceptionsBeforeTest` (a leaking suite's background refresh throws *after* its `db.close()`, landing in the *next* test's `runTest` startup — a victim like `FilteredPapersViewModelTest` passes in isolation but flakes in the full suite). PS.2 added **synchronous** query/txn executors to all 19 Room suites, which **greatly reduced** it. **Correction (PF.2, 2026-07-06):** it is NOT fully eliminated — it recurred once on the PF.2 full-suite run (green on a plain re-run). **Root cause + real fix (PF.3):** the `Illegal connection pointer` is a Robolectric **LEGACY SQLite-shadow** artifact; switching to **NATIVE** SQLite (a `robolectric.properties` per Room-testing module) removes the shadow pointer bookkeeping the refresh races → `:app` went **3/3 green after ~5 consecutive flakes**. First ruled out `InstantTaskExecutorRule` (ineffective — Room 2.7's refresh is a coroutine, not the ArchTaskExecutor path) and confirmed `forkEvery(1)` was already set (intra-class leak). | Fixed at root cause, no re-kicks needed going forward. Memory + backlog updated. | Nothing needed from you; the CI flake that occasionally hit P-Sources/P-Feeds PRs is now gone. |
| RESOLVED 2026-07-06 (→ Phase P4) | ~~**On-device inbox relevance ranking.**~~ **Corrected + promoted.** The premise here was **stale**: the positive-only ranker ALREADY ships and runs live — `EmbeddingWorker.scoreInbox` computes k-means centroids over library saves and writes `inbox_items.score`; the Today inbox renders a "Likely relevant" (≥0.55) section; `SyncScheduler` runs it every 6h + on every manual sync. So the `score` column is **not** unused. What was genuinely missing — the **dismiss** signal (thrown away) and a cold-start for **follows-only** users (<10 library → pure recency) — is now the approved **Phase P4** (Rocchio two-sided ranker + durable feedback + explicit thumbs), all still on-device/zero-egress. | Building it as Phase P4 (P4.0 shipped dark). | Steer the model choice (Rocchio vs the deferred classifier — §1 P4 row) or, when P4.2 lands, the thumbs UI placement. |
| RESOLVED 2026-07-05 | ~~**Pre-turn RAG coexistence with tools.**~~ **Decided at PT.1: Option B — keep pre-turn RAG; `search_my_library` AUGMENTS it** and cites in prose `arXiv:<id>`, so the `[n]` excerpt-citation contract is byte-untouched. Option A (suppress RAG + re-source `[n]` from tool results) needs a mid-loop citation channel → ROADMAP backlog. | SPEC-P-TOOLS §11 |
| OPEN | **PH.8 bundled MathML fallback — build it?** Gated on the §2 device measurement of native WebView MathML on old WebViews. | Ship native-only; decide after the device verdict. |
| OPEN | **Re-enable R8 shrinking?** Disabled after two obfuscation casualties; ~no benefit for a sideloaded single-user app. | Stays OFF until keep-rules are audited + the signed APK is device-smoked. v2 backlog. |
| FYI | **P-HTML MVP scope cuts** (deliberate, tracked): offline images → PH.5 · TOC/section-jump → PH.6 · find-in-page + selection→Ask → PH.7 · bundled math fallback → PH.8. **Out of phase entirely:** full-text HTML search indexing, bulk corpus prefetch, save/export of rendered HTML. | Elevation, not MVP — raise one to re-prioritize. |

---

## §4 — Setup & environment (pointers; truth lives in `.claude/memory/`)

No separate `SETUP.md` — duplicating env facts would create a drift surface the memory harness forbids.

- **Local build JVM = JDK 17** (matches CI; JDK 21 causes a Windows daemon jar-lock + a test flake). → memory `local-build-jdk17`
- **Windows Gradle jar-lock recovery** (`classes.jar … used by another process`): `./gradlew --stop` + force-remove the locked intermediate. → memory `windows-gradle-jar-lock`
- **How changes land:** `main` is protected, PR-only; CI's required check runs on `pull_request` — open a (draft) PR early. → memory `repo-landing-process`
- **Emulator / SDK / AVD / JDK-17 paths + reboot procedure**, `Pixel_3a_API_34` `[E]` target → the local `local-emulator-verification-env` memory (the API-26/27 old-WebView AVD in §2 is the one gap).

---

*Maintained by Claude under the `CLAUDE.md` loop. Override any §1 decision by saying so.*
