# VERIFICATION.md — on-device verification ledger

Cloud CI builds and runs the JVM/Robolectric suite, but it **cannot run the app on a
device**. Several things are only observable on real hardware (signed-APK behavior,
background workers, on-device ML, performance, TalkBack), and Arxiver has already shipped
bugs that CI couldn't catch (R8 obfuscation crashed onboarding in v1.1.0 and v1.1.1). This
file is the single batched checklist of everything that needs a human + a device, plus a
dated log of what's actually been observed.

**Status of CI-side work lives in `ROADMAP.md`; status of device-side work lives here.**

## How to use this
- When you do a device run, work the checklist for the **current target build**, set each
  item's status + the build/date, and append a dated entry to the Verification log.
- A `[needs-user/device]` or `[needs-user]` task in ROADMAP is **mirrored here** as the
  concrete thing to check — ROADMAP says "blocked on device", this says "here's how".
- `[x]` in ROADMAP never depends on a device check (CI green is the gate); device checks
  ride here instead so they're never lost.

Legend: `[ ]` pending · `[x]` verified on device (build/date) · `[E]` verified on
emulator (build/date) · `[~]` partial · `[!]` failed (with pointer) · `[-]` superseded / n/a

> **`[E]` (emulator-verified)** was introduced 2026-06-23 when an x86_64 emulator (API 34)
> + autonomous adb/screenshot driving was set up locally. The emulator fully covers most
> non-hardware surfaces (UI/UX, workers, insets, feedback/swipe, deep links, embeddings,
> RAG, notifications). It is **not** authoritative for: Gemini Nano (flagship AICore only),
> real-device performance numbers (D1–D3), the hardware-backed Keystore (J1), or arm64
> Gemma throughput — those keep needing the S20 / a flagship and stay `[ ]` until then.

**Current target build: v2.0.2 (versionCode 7).** Reconciles the stale build identity
(the build self-IDed as 1.2.2/vc6 while tags were already at v2.0.1). v1.2.0 verified
end-to-end on a physical S20 (2026-06-14). **2026-06-23: first emulator verification
session** (API 34 x86_64) cleared the §I field-fixes and a large slice of §L/§F/§A/§H —
see the `[E]` items and the Verification-log. §I re-checks now pass on the emulator.

---

## A. Release smoke / first run
- [x] **A1 Install from scratch** — sideload the signed APK on a clean device. _Verified v1.2.0 (2026-06-14)._
- [x] **A2 Onboarding → Today** — follow ≥2 categories, tap "Start reading", land on **Today** with no crash. _Verified v1.2.0 (2026-06-14) — first build to clear the R8 crashes._
- [x] **A3 Onboarded flag persists** — reopen stays on Today, not onboarding. _Verified v1.2.0 (2026-06-14)._
- [x] **A4 Crash reporter** — an uncaught exception saves a trace; next launch shows a copyable dialog. _Verified v1.1.1 (2026-06-14): user copied the protobuf trace. From v1.2.0 traces are de-obfuscated (real names)._
- [E] **A5 Zero crashes in normal flows** — exercise each tab without a crash. _(PRD §7.4)_ _v2.0.2 (emu 2026-06-23): tab-by-tab sweep Today/Browse/Search/Library/Settings/Detail/PDF/Connections — zero `AndroidRuntime:E` in logcat. (Ask/Claude surfaces ride V4/V5.)_

## B. Background sync & workers
> The `@HiltWorker` factories were R8-stripped v1.0.0–v1.1.1; v1.2.0 (minify off) is the
> first build where they run — confirmed by B1.
- [x] **B1 FollowSyncWorker** — followed categories populate the Today inbox after first sync. _Verified v1.2.0 (2026-06-14): fetching new articles works._
- [E] **B1a Non-arXiv follows (P-Feeds PF.3; UI now the PE.4 unified directory)** — open Explore → the RSS/feed action lands on the **Browse-&-Follow directory** (Library resting state; arXiv expanded as a peer row). Expand bioRxiv/medRxiv and follow a native category; expand chemRxiv/SSRN and follow a curated Field + a whole-source follow. After a sync each lands papers in Today; a non-arXiv-only follower sees the inbox fill + skeletons (NOT "you follow nothing"). Opening the picker issues zero network requests. A new-source paper's PDF opens in the browser (host-gated), not in-app. Unfollowing removes that feed's inbox rows. _2026-07-10 (emu 5556): the RSS action opened the unified directory with arXiv as a peer row ("2 followed") beside all seven preprint sources; expanded bioRxiv → followed **Bioinformatics** (`category|bioinformatics|biorxiv`) and SSRN → followed **Engineering** + **All of SSRN** (`category|fields/22|ssrn` + `category||ssrn`). One sync landed **20 bioRxiv papers** in Today (`papers.origin=biorxiv`); the directory opened with zero network (structural). Browser-vs-in-app PDF split confirmed under B1e. Remaining: the explicit zero-network-on-open trace + medRxiv/chemRxiv legs._
- [x] **B1f Research Square PDF shard (P-Explorer PE.2h) — gates an egress decision.** _Verified 2026-07-10 from the Co-Founder's own Windows 11 box (same NAT/ISP egress a phone on that wifi uses; the shard is chosen by **egress geography**, not by the device, so an emulator would add nothing here — its traffic NATs through the same host)._ Probed 3 real OA PDFs resolved live from `api.openalex.org` (SID `S4306525896`), fetched with the app's **own** User-Agent `Arxiver/0.1 (…)` — never a browser impersonation. **Result: all three land on `assets-eu.researchsquare.com` → `http=200`, `content-type: application/pdf`, body begins `%PDF`.** Chain: `www.researchsquare.com` → (`/article/rs-*/vN.pdf`) → `assets-eu.researchsquare.com`; 1–2 hops. **Shard is stable across papers.** ⇒ The two exact hosts to allowlist are `www.researchsquare.com` + `assets-eu.researchsquare.com`. **Residual (recorded, not hand-waved):** `-eu` is a *regional* shard — on cellular/roaming a different shard would **fail closed** (per-hop exact-match) and degrade gracefully to open-in-browser, never break. Re-check if the Co-Founder relocates.
- [E] **B1g Multi-source Explore search (P-Explorer PE.3)** — Explore → Online scope: the leftmost **source chip** opens a radio sheet (arXiv default; followed sources ★ near the top; SSRN/RS/Preprints.org/PsyArXiv captioned "Papers open in browser"; bio/med carry the lag hint). TalkBack reads the sheet title as a heading and rows as radio buttons. Pick chemRxiv, search "catalysis" → results appear after ONE submit; **airplane-mode check: opening the sheet + typing fires zero network** (only submit errors). Field chips narrow server-side (still one call per submit). An SSRN hit renders a short card (no abstract) and its detail shows "No abstract available from SSRN" + **Open in browser** (no PDF spinner); a bio/med hit's PDF opens IN-APP. Tap a result → detail loads under the persisted row; save one → it lands in Library; multi-select → save/Organize/Dispatch all work on external hits. Switching source clears results and does not auto-search. _2026-07-11 (emu 5556): the source chip opened the **"Search source"** radio sheet exactly to spec — **arXiv** selected with a **★** (a followed source, sorted to top); **chemRxiv / Research Square / SSRN** each captioned "Papers open in browser"; **bioRxiv / medRxiv** each "May lag the native feed by a few days". Picked **chemRxiv** → chip + hint ("Search chemRxiv by keyword") + subtitle ("Keyword search · chemRxiv (via OpenAlex)") updated and the field chips switched to the **chemRxiv-specific** vocab (Chemistry / Materials Science — the PE.1 curated set, not the generic 26-Field list), and the prior results **cleared with no auto-search**. Typed "catalysis" → **ONE submit → "Top 25 results"** (first: "Digital Catalysis Platform (DigCat)…", Di Zhang/Hao Li, a **Materials Science** category chip, 19 Dec 2024) — live via `api.openalex.org` (reachable from this residential-IP emulator; the datacenter Cloudflare gate only bit CI). **Airplane-mode legs all seen:** with ✈ on, the source sheet **opened offline** (full source list rendered from the local `PreprintSourceRegistry`, zero network); switching source offline worked and cleared results; a submit resolved to an honest **"You're offline. Cached papers remain available." + Retry** (graceful, no crash, no silent call) — the behavioral half of the metering contract the `SearchViewModelTest` request-counting pins (switch=0, typing=0, submit=1). Harness profile untouched (no saves/follows). **SSRN card + detail leg also closed (same day):** the full source list scrolled to show all 8 sources with correct captions (chemRxiv/RS/**SSRN**/**Preprints.org**/**PsyArXiv** "Papers open in browser"; bio/med lag hint); picked **SSRN** → SSRN-specific field vocab (**Engineering**/**Social Sciences**), "governance" → ONE submit → live **Top 25** (Kaufmann/Kraay "Governance Matters VII", La Porta "Investor Protection and Corporate Governance") rendering as **short cards with no abstract** + category chip + date; opened the first hit → detail showed the **SSRN** source badge, **"No abstract available from SSRN"**, and **"Open in browser" (NO PDF spinner)** + Connections + DOI `10.2139/ssrn.1148386`. The viewed hit persisted only a bare `papers` row (origin=ssrn), never a library/inbox/feedback row — deleted afterward to keep the harness pristine. **Outstanding (would pollute the ranker harness or need aural TalkBack): the save/multi-select/Organize/Dispatch-on-external-hits legs and the TalkBack heading/radio semantics** (present in code — `role = Role.RadioButton` + heading — not driven aurally this pass)._
- [E] **B1e bioRxiv/medRxiv PDFs read IN-APP (P-Explorer PE.0)** — _network contract already verified 2026-07-10 from the local box: `https://www.biorxiv.org/content/10.1101/2021.10.04.463034v1.full.pdf` → `http=200`, **0 redirects**, `application/pdf`, body begins `%PDF`. Remaining is the device-bound half:_ follow a bioRxiv/medRxiv category, open one of its papers from Today, tap through to the PDF — it must render **in-app**, not bounce to the browser. Confirm a transient 403 is rare and recovers on a retry. Also dispatch that paper to a Claude routine and confirm the payload now carries `"pdf_fetchable": true` (it wrongly said `false` before PE.0). _2026-07-10 (emu): a bioRxiv paper (10.1101/2024.03.09.583681, "Effective structure-aware protein alignment") opened from Today → **Read PDF rendered IN-APP** (the "bioRxiv preprint doi:…" watermark + full abstract/§1 visible), `topResumedActivity` stayed `dev.blokz.arxiver/.MainActivity` (no Chrome bounce), and logcat showed no AllowedHosts block — the honest-UA path to `www.biorxiv.org` works on device exactly as the local-box probe predicted. Device leg of B1e DONE; medRxiv leg + the routine-dispatch sub-leg still open._
- [E] **B1c Following-management screen (P-FeedPolish PFP.2)** — open Explore → the **Bookmarks** top-bar action → the "Following" sheet lists every follow grouped by source (arXiv follows appear under an **arXiv** group, not a fallback; author/query follows show a type label; a whole-source follow reads "All of <source>"). Tap unfollow on one → it disappears and that feed's already-arrived Today rows are gone on the next Today open (same lifecycle as the picker). Opening the sheet issues **zero** network requests. With nothing followed, the empty state's "Follow sources" button opens the picker. TalkBack reads each unfollow control ("Unfollow …"). _2026-07-10 (emu): the "Following" sheet grouped follows under **arXiv** / **bioRxiv** / **SSRN** headers (arXiv follows under an arXiv group, not a fallback); "All of SSRN" rendered as a whole-source row; tapping its unfollow (content-desc "Unfollow All of SSRN") removed it and it vanished from the enabled set. TalkBack labels present in the tree._
- [E] **B1d Follow health hint (P-FeedPolish PFP.3)** — follow a genuinely-quiet source (e.g. a narrow OpenAlex Field or a low-volume author). After the follow's sync delivers zero papers on **≥4 consecutive** syncs (force by triggering manual syncs, or a mis-mapped Field that always returns nothing), the manage screen ("Following" sheet) shows a soft **"No new papers recently"** subtitle under that row — muted `onSurfaceVariant`, NOT an alarming/error color. A follow that then delivers a paper clears the hint on the next sync. A follow whose sync **fails** (offline / 429 / 404) must **NOT** accrue the hint (failure ≠ empty). TalkBack reads the row + hint sensibly. Note: pre-v11 installs start every follow's streak at 0 (back-filled), so the hint can take a few post-upgrade syncs to appear. _2026-07-10 (emu): drove both arXiv follows (Machine Learning, Artificial Intelligence) to `empty_sync_streak=4` via repeated manual syncs — the "Following" sheet then showed the soft **"No new papers recently"** subtitle under exactly those two rows in muted (non-error) tone, while the freshly-followed Bioinformatics (streak 0, 20 papers) showed none. The failure-≠-empty and clears-on-delivery legs not separately forced this run._
- [ ] **B1b OpenAlex BYOK key (P-Feeds PF.4)** — Settings → AI providers → the "OpenAlex" optional-key card: enter a key → it shows "Connected" (masked, never read back), and OpenAlex-backed discovery/follows keep working (the key rides `?api_key=`); clear it → back to the free `mailto` pool. The key must never appear in a backup/export.
- [ ] **F6 "Emerging in your areas" shelf (P-Discover2 PD.3)** — Settings → toggle **"Show emerging areas"** ON
  (default OFF; no permission prompt — it posts nothing). With a warm profile (several followed arXiv categories,
  ≥~30 baseline papers, real recent activity), the next daily background sync computes the shelf and Today shows a
  calm **"Emerging in your areas"** section at the top: a human area name ("Robotics" not "cs.RO") + "more active than
  usual in your recent feed" + a few tappable driving papers. **Ethos checks:** no "hot"/🔥/count-badge/notification;
  never implies global popularity; the section is **absent** (not a broken empty rail) when nothing has cleared the
  bar — the common state. Airplane mode: the shelf still renders from the cache (zero egress). A conference-deadline
  week must NOT light up every category (the relative frame self-cancels a global surge). Toggle OFF → the section
  vanishes. All computed on-device from `published_at`; nothing leaves the device.
- [ ] **F5 "More like this" on any paper (P-Discover2 PD.2)** — open a paper from **Explore/Online search** (a
  non-library paper, so it has no precomputed `related_papers`): the "Related papers" section must **populate via the
  live cosine scan** (once the paper is embedded), not silently vanish. Open a brand-new/un-embedded paper → the
  section shows the calm **"not indexed yet"** line (not empty). A paper with an embedding but no on-device neighbors
  → **"No similar papers on your device yet."** All on-device (airplane mode still resolves neighbors); zero egress.
- [ ] **B1h Follow an author (P-Discover2 PD.1)** — open a paper's detail → tap an **author name** → the menu shows **"Follow author"**; tap it → the menu now reads **"Following ✓"** and it persists across a reopen (a `type=author, origin=arxiv` follow row; `au:"<name>"` query). The expedited one-shot sync fires, and that author's recent papers land in Today after it completes (on Wi-Fi/unmetered for the embedding leg). Unfollow via the menu (or the Following sheet) removes the follow + its inbox rows. Honest namesake caveat: a common name may pull a namesake's papers (documented, not a bug). TalkBack reads the author as "…tap to follow this author".
- [E] **B2 EmbeddingWorker** — runs on unmetered network; un-embedded papers get embedded (Settings shows the embedded count climbing). _v2.0.2 (emu 2026-06-23): after the first sync the worker ran and Settings shows "28 papers embedded locally"; chat retrieval returns real chunks, confirming the embeddings exist._
- [E] **B2b Ranker eval harness (P5.1) — debug build** — with ≥15 saves and ≥15 dismisses/thumb-downs on device, let the background embedding pass run (or trigger a manual sync on unmetered), then open Settings → the debug-only "Ranker health" card shows per-segment AUC/P@10 (or an honest "insufficient data" with ESS numbers) + the per-segment inbox score distributions. Airplane mode: ranking and the eval still run (zero egress). If the title-only segment's "above cut" is near 0% while rich is healthy, that is the starvation signal P5.2/P5.3 must fix — report the numbers. _2026-07-10 (emu 5556, v2.0.2+p5.3 debug): the card rendered live per-segment numbers after a real 61-label profile (Overall/Rich AUC 0.50 [0.38–0.64] · P@10 0.58; Title-only honestly "insufficient data (ESS 0/0)" — arXiv-only follows have no stripped abstracts; Rich inbox n=278 · p50 0.42 · p90 0.46 · above cut 0% · 64 labels evaluated). The AUC≈0.5 is a property of the deliberately-diffuse synthetic profile (pos = "anything non-vision"), not a pipeline defect — the REAL-profile numbers still need the Co-Founder's device._ **Coherent-profile readout, 2026-07-10 (emu 5556, +QW1 build pending):** the diffuse profile was replaced with a realistic **coherent MODERATE-difficulty** one — interest = LLM-agents/reasoning/RL (30 positives: 24 saves + 6 thumb-up), hard-ish negatives = vision/image/diffusion/3D/segmentation, a sibling ML subfield (35 negatives: 15 thumb-down w=1.0 + 20 dismiss w=0.3); constructed via `run-as sqlite3` with strictly-increasing class-interleaved past-millis timestamps, FK-checked, both `source` literals verified. One eval pass (Settings→Re-index) produced **Overall/Rich AUC 0.89 [0.81–0.97] · P@10 1.00**, calibration **fitted** (a=48.7, b=−20.2 ⇒ cut≈0.414, monotone/predictive — CI excludes 0.5), λ=0.3 selected+time-split-confirmed, 45+40 pooled held-out labels; Title-only still "insufficient (ESS 0/0)" (arXiv corpus has no stripped abstracts). **Reading:** on a realistic coherent profile the shipping shrinkage-tuned Rocchio ranker separates strongly and the top-10 (= the user-visible "Likely relevant" section) is at ceiling — **advisory NO-GO-direction** support for P5.4. **Scope (adjudicated):** this is *advisory only* — no emulator-synthetic profile can CLOSE P5.4's `[!]` gate (taxonomy-proxy labels are a different label-generating process from organic preference and are partly encoded in the bge geometry being ranked), so the Co-Founder's REAL-profile numbers remain the sole gold gate. Device now holds this coherent 65-label profile as harness fuel (superseding the diffuse 60)._
- [E] **B2c Calibrated threshold + top-k (P5.3)** — with a profile past the calibration floor (≥50 labels incl. ≥~10 real dismisses/thumb-downs), let a scoring pass run, then check Today: the "Likely relevant" section holds AT MOST 10 papers, and the debug Ranker-health card's numbers coexist with a sensibly-sized section (the cut is now the calibrated p=0.5 point, not 0.55). Below the floor, behavior must be indistinguishable from pre-P5.3 (exactly the 0.55 cut) **for a never-fitted profile OR after the downgrade completes** — under **P5.5** hysteresis a fitted profile's FIRST failed refit KEEPS the calibrated cut for one more pass; the 0.55 fallback lands on the SECOND consecutive null fit (see the P5.5 leg below). Dismiss a paper → the `paper_feedback` row records `score_at_label` (verifiable via a debug DB dump, not UI). _2026-07-10 (emu): ALL legs seen live — calibration fitted pass-1 (a=1.504, b=0.034 on 54+28 held-out) → "Likely relevant" = EXACTLY 10 rows with a score-tied 11th paper correctly excluded below "More from your follows"; pass-2's near-zero-slope refit tripped the a≤0 anti-predictive guard → NULL → the section vanished (p90 0.46 < the exact-0.55 fallback) — the below-floor golden, live; swipe-dismiss stamped `score_at_label=0.472970…` byte-identical to the displayed score. Note the flip-flop: a profile hovering at the guard boundary sees the section appear/disappear across passes — designed honesty, hysteresis noted as a refinement (ROADMAP P5 ledger)._ **P5.5 hysteresis (shipped 2026-07-11) — device leg:** on a fitted profile, force a below-floor refit (e.g. clear enough labels to drop under 50 / trip the a≤0 guard) and confirm the "Likely relevant" section **persists one extra pass** (the row keeps its a/b, `consecutive_null_fits`→1 in a debug DB dump); a SECOND consecutive below-floor pass drops it to the 0.55 regime (a/b→NULL, streak→0). A fresh fit at any point restores immediately. Note the two-pass-confirm counts **completed eval passes** — a worker cancelled/crashed between read and upsert skips the increment, so the downgrade can take more than two calendar passes, never fewer.
- [~] **B2a Two-sided inbox ranking (Phase P4)** — with ≥10 saved papers, dismiss a few inbox papers on one topic, trigger a sync, and confirm on the next scoring pass that papers *similar to the dismissed ones* drop below the "Likely relevant" cut while save-similar papers stay on top (the dismiss signal now demotes, not just hides). Undo a dismiss → its negative influence is gone next pass. **Cold start:** on a fresh profile with <10 library papers but ≥1 followed source, confirm the inbox shows a scored "Likely relevant" ordering (not pure recency); observe whether that section is a reasonable size (not the entire inbox). Everything stays on-device (airplane mode still re-ranks). Zero egress from ranking. _2026-07-10 (emu): demotion leg seen live — 20 thumb-downs on a vision/video cluster put every vision paper at the score floor (0.36–0.38) while the thumb-up cluster (agents/LLM) held the top (0.47–0.50); airplane ON + force-stop + relaunch rendered the full scored, score-ordered feed offline (279 scores from DB). Outstanding: the undo-erases-influence leg and the fresh-profile (<10 library) cold-start leg — this device has 43 positives._
- [E] **B2d Ambient digest notification (P-Ambient PA.1b)** — opt-in Settings toggle (default OFF) that requests `POST_NOTIFICATIONS` on 13+; a background (periodic, non-user-initiated) `EmbeddingWorker` pass then posts a local "N new likely-relevant papers" notification thresholded on the calibrated cut, exactly-once via `inbox_items.digested_at`, capped ≤1/day, `VISIBILITY_PRIVATE`, tap opens the app. Zero egress; the count never leaves the device. _2026-07-12 (emu 5556, throwaway build — `cmd jobscheduler run` can't target WorkManager's periodic job on this API, so `embedNow`'s digest-suppress was temporarily removed to force a non-suppressed pass; reverted + the shipped APK reinstalled after): **live migration v14→v16** ran on install (`user_version=16`, all 303 inbox rows `digested_at` backfilled NULL). Toggle rendered to spec with the "Requires Wi-Fi sync; nothing leaves your device" copy; enabling it (permission already granted from a prior model-download session) persisted `digest_enabled`. The DAO's eligible query returned **89 rows** at the live calibrated cut (0.414; a=48.7). A forced pass **posted the digest**: title **"89 new likely-relevant papers"**, `BigTextStyle` body listing the top-3 by score (all LLM-agents papers — the coherent profile's genuinely top-ranked rows, best-first) + **"…and 86 more"**, on channel `arxiver_digest` "New relevant papers" IMPORTANCE_DEFAULT; **exactly 89 rows got `digested_at` stamped** (exactly-once on device, matching the eligibility count). Tap **opened the app** (`topResumedActivity=MainActivity`) — landing on the last screen (Settings) on a WARM resume; a COLD start lands on Today (MainActivity's start destination) — forcing Today on warm-resume rides PA.2's richer deep-link (documented scope-down). `VISIBILITY_PRIVATE` is set on the notification (confirmed in `dumpsys`); the lockscreen-hide wasn't visually verified (the emulator has no screen lock set). Outstanding for a real device: the lockscreen-hidden render + the API-33 deny path (this device had the grant)._
- [ ] **B3 CitationSyncWorker** — nightly citation fetch fills a saved paper's Connections (references/citations).
- [ ] **B4 DispatchQueueWorker** — a dispatch queued while offline sends once back online.
- [ ] **B5 Sync cadence** — periodic sync respects the interval set in Settings.

## C. On-device ML
- [~] **C1 Model download** — pinned bge-small-en-v1.5 ONNX (~34 MB) downloads on unmetered with progress UI; checksum verified; survives delete/re-index in Settings. _v2.0.2 (emu 2026-06-23): the model auto-downloaded over the emulator network and loaded ("Model ready — hybrid search active"), implicitly checksum-passing. The progress UI + explicit delete/re-index round-trip were not driven this session (V2-followup)._
- [E] **C2 Embedding inference** — WordPiece tokenizer + ONNX session produce embeddings on device (unit-tested against reference tokenizations; on-device inference not yet run). _v2.0.2 (emu 2026-06-23): bge ONNX inference runs on the x86_64 emulator (28 papers embedded; chat retrieval returns relevant chunks). The "not yet run on device" gap is closed for x86_64; arm64 throughput still rides the S20._
- [ ] **C3 Hybrid search relevance (golden set)** — ≥80% top-5 hit over the golden fixture; needs on-device
  inference, so it lives here not in CI. **The fixture is ready (PP.5a):** `core/search/src/test/resources/golden_relevance.json`
  — **30 queries / 36 real landmark arXiv papers** (incl. 6 distractors), CI-gated for structure by
  `GoldenRelevanceStructuralTest`. **The C3 eval harness is device-session work (PP.5b, deliberately not shipped
  blind):** build an `ENABLE_TEST_CORPUS`-gated instrumented path (an `:app` androidTest or a debug entry) that
  (1) inserts the 36 papers + embeds each abstract via the REAL BGE `EmbeddingService` (never a JVM-faked embedding —
  that breaks the ONNX-free-CI red line), (2) FTS-indexes them, (3) runs each query through the full hybrid path
  (`LocalKeywordSearch` + `VectorIndex.topK` + `HybridFusion.fuse`), (4) counts a hit when the query's `expectedIds`
  land in the top-5, (5) asserts hit-rate ≥ 0.80. Prereq: the BGE model provisioned on the device (see §D). Record the
  measured hit-rate + per-query misses.

## D. Performance _(ROADMAP 5.4 / PRD F5.4 · Phase P-Prove)_
> The P-Prove Macrobenchmark harness (PP.1–PP.3b) is the instrument for D1–D4. **Run on the physical Samsung S20, NOT
> the x86_64 emulator** — startup/frame absolute numbers are host-CPU/thermal-dependent (the emulator is legitimate
> only for D4 profile *generation* + the CI compile). **Precondition (Finding #1):** the BGE ONNX model must be
> downloaded on the device before D2/D4, or the semantic leg is skipped and `SearchTraceBenchmark` fails its
> `semantic_active` assertion (by design — never a silent keyword-only number). The corpus is seeded automatically on
> the `benchmarkRelease`/`nonMinifiedRelease` variants (`ENABLE_TEST_CORPUS`); benchmarks gate on the "Seeded Paper"
> content anchor, not just `today_screen` (Finding #2). Record the exact device in every row.
- [ ] **D1 Cold start < 2s.** `:macrobenchmark` `StartupBenchmark` cold→Today, `CompilationMode.None()` **and**
  `Partial(baseline)` — record TTID + TTFD both ways (the profile delta is one shipped win). **PP.4 landed a second,
  profile-independent win:** the `onboarded` read + `CrashReporter.pendingCrash` are no longer on the first-frame
  main thread (splash-held async read + deferred crash effect), so the main thread is unblocked — expect the TTFD
  improvement here (TTID may barely move since the splash is simply held). Confirm both on the S20.
- [ ] **D2 Hybrid search < 300ms** — `SearchTraceBenchmark` reads the async `hybrid_search` section (INCLUDES the
  `embedQuery` JNI). PRD #2 says a 1,000-paper library; F5.4 says 5K — capture BOTH corpora and note which. Requires
  the BGE model provisioned (above).
- [ ] **D3 List scrolling at 60fps** on the Today feed — `FrameTimingBenchmark` (corpus seeded; gated on "Seeded
  Paper"). Record frame P50/P90/P99. If fling doesn't register, move the `today_screen` tag to the `LazyColumn`.
- [ ] **D4 Baseline profile** — `BaselineProfileGenerator` on `nonMinifiedRelease`; generate with
  `./gradlew :app:generateBaselineProfile -Pandroidx.baselineprofile.skipgeneration=false` (the repo pins
  `skipgeneration=true` for CI); commit `app/src/main/baseline-prof.txt`; confirm `profileinstaller` applies it AOT.

## E. Accessibility _(ROADMAP 5.5)_
- [ ] **E1 TalkBack walkthrough** — every screen's actionables are labeled and operable; swipe actions have custom-action equivalents (labels shipped during feature work; walkthrough not done).
- [ ] **E2 Font scale 1.3×** — layouts hold without truncation/overlap.
- [ ] **E3 Color-blind check** — StatusChip tones are distinguishable (each already carries a text label; confirm visually).

## F. Reader & content
- [x] **F1 PDF** — download + in-app reader, light and dark. _Verified v1.2.0 (2026-06-14)._
- [E] **F2 Deep links / share-in** — opening or sharing an arxiv.org URL resolves to the paper detail. _v2.0.2 (emu 2026-06-23): VIEW `https://arxiv.org/abs/2606.23678` → AIR paper detail; ACTION_SEND of an abs URL → SpatialClaw detail. Note: https auto-open routes to the browser by design (`autoVerify="false"` — arxiv.org isn't our domain, the user picks Arxiver or shares in)._
- [x] **F3 Tagging** — add/remove tags on a paper; persists. _Verified v1.2.0 (2026-06-14)._
- [x] **F4 Search (functional)** — local + arXiv search return results. _Verified v1.2.0 (2026-06-14); relevance/latency targets still pending (C3/D2)._

## G. Claude dispatch (live — needs the user's routines) _(ROADMAP 6.6)_
- [ ] **G1 Guided setup wizard** — create on claude.ai → connect URL+token (live validation) → opt-in verify ping against a real routine. (Fire-API contract itself was live-verified in dev, task 4.8.)
- [ ] **G2 End-to-end** — set up ≥2 catalog templates via the wizard and confirm real runs; record findings in SPEC-CLAUDE-BRIDGE.
- [ ] **G3 Privacy preview** — the confirm sheet previews exactly what leaves the device; tokens never appear (redaction is structurally tested — confirm visually).
- [ ] **G4 Non-arXiv dispatch (Phase P-Dispatch)** — the send-to-Claude action now shows on a chemRxiv/bioRxiv/medRxiv paper's detail screen. Sending one produces a well-formed payload: the confirm-sheet JSON has `source`/`native_id`/`url` and no `arxiv_id`/`abs_url`, and the envelope's paper line reads `chemrxiv:<doi>` (never `arXiv:null`). A **mixed** arXiv+non-arXiv selection carries both shapes (the arXiv row keeps `arxiv_id`/`abs_url`). A live run to a real routine is accepted, and a `deep_dive` on a cookie-walled paper degrades honestly (works from the abstract; `pdf_fetchable:false` present). The un-gated action renders in light/dark + TalkBack reads it. Builder/envelope shape + un-gate are CI-covered (`PayloadBuilderTest`/`DispatchEnvelopeTest`/`DispatchRepositoryTest`); this confirms the live round-trip.

## I. v1.2.1 / v1.2.2 field fixes — re-verify on device
- [x] **I1 Sync spinner clears** — Today top-bar spinner stops (reverts to refresh icon). _Verified v1.2.1 (2026-06-14): screenshot shows the refresh icon, no perpetual spinner._
- [E] **I2 No phantom empty row (lists)** — Today/Library/Search list ends cleanly. _v2.0.2 (emu 2026-06-23): scrolled Today to its true end (last paper card flush, no trailing divider/phantom row)._
- [E] **I3 No bottom blank strip (app-wide)** — **root-caused v1.2.2:** nested Scaffolds double-applied the bottom nav inset → blank strip above the bottom bar on Today/list screens (and the gesture-bar clearance on detail screens). Fixed via `consumeWindowInsets` on the NavHost + trimmed PaperDetail's redundant trailing padding. _v2.0.2 (emu 2026-06-23, gesture nav on): no blank strip on Today (list flush above the nav bar) AND on Paper Detail (metadata card ends with only the correct small gesture-bar inset)._
- [E] **I4 Add to collection** — from a saved paper's detail, add it to a collection; it appears under that collection (Library → Collections → open); toggling off removes it; survives relaunch; "New collection" from the picker works. _v2.0.2 (emu 2026-06-23): saved AIR paper → created "Reasoning" via inline "New collection" → paper appears in Library→Collections→Reasoning → survives relaunch AND survives the vc6→vc7 upgrade install. (Toggle-off removal is CI-covered by F1.3 DAO/VM tests.)_

## J. AI providers (v2 / P1) _(SPEC-AI-PROVIDERS)_
> CI covers the transports (MockWebServer SSE), the registry/settings ViewModel
> (pure-JVM with fakes), and the key-vault contract (Robolectric, in :core:ai).
> What's left for hardware: the **hardware-backed Keystore** path and **real keys**.
> The **Settings → AI → AI providers** screen is built (P1.1b); these need a real key.
- [E] **J1 Key vault on hardware Keystore** — a saved provider key survives app restart on a real device (Robolectric uses a software keystore; confirm the AndroidKeyStore-backed `EncryptedSharedPreferences` round-trips on device). _v2.0.2 (emu 2026-06-23): both Claude + Gemini keys survived an app relaunch (EncryptedSharedPreferences round-trip on the emulator's AndroidKeyStore). The **hardware-backed (TEE/StrongBox)** path specifically still wants the S20._ _(P1.1a)_
- [E] **J2 Claude test connection** — Settings → AI → AI providers, paste a real Anthropic key, Save, Test connection → "Connection OK"; a wrong key → "key rejected"; airplane mode → "offline". _v2.0.2 (emu 2026-06-23): a real Anthropic key → **"Connection OK"** against the live Messages API; a wrong key (the Gemini key pasted into Claude's field) → "Key rejected"._ _(P1.1b)_
- [E] **J3 Gemini test connection** — same with a real Gemini Developer API key. _2026-06-23 (P-Rich R0): with a valid `AIza…` key, Test still showed **"Couldn't reach the provider"**. **Root cause (real bug):** the pinned model `gemini-2.0-flash` is **deprecated** — `generateContent` returns **HTTP 404** "This model is no longer available", which the app maps to the generic connection error. So Gemini chat was fully broken regardless of key. **Fix:** `GeminiProvider.DEFAULT_MODEL` → `gemini-2.5-flash` (verified live: `gemini-2.0-flash` → 404, `gemini-2.5-flash` → 200). After the fix, Test connection returns "Connection OK". (The V2 `AQ.…` key was also the wrong **type** — a red herring; the deprecated model was the actual blocker.)_ _(P1.1b / P-Rich R0)_
- [E] **J4 Default provider persists** — connect two providers, pick a default (radio), relaunch → selection holds. _v2.0.2 (emu 2026-06-23): both providers connected, Claude set as default → after relaunch Claude's "Use as default" radio is still selected and chat resolves to Claude._ _(P1.1b)_
- [E] **J5 Gemma model download** — Settings → AI → AI providers → on-device card → Download model: runs on Wi-Fi only (unmetered), shows progress %, SHA-256 verifies, survives relaunch; Delete removes it. ~2.59 GB. A re-download after the F2 model swap also purges the stale `-web` file (no double-counting on disk). _v2.0.2 (emu 2026-06-26, **V3 6 GB AVD**): tapped Download → progress notification climbed 1%→100%; the downloader wrote `files/models/gemma-4-E2B-it.litertlm.part` and only renamed it to the final `gemma-4-E2B-it.litertlm` (**2,588,147,712 B = 2.59 GB**) after the SHA-256 check passed. **Bug found+fixed (this PR):** the download button advertised "~1.9 GB" (stale label left over from the F2 swap off the 1.87 GB `-web` build) → corrected to "~2.6 GB". Relaunch-survival + Delete + stale-sibling-purge legs not separately exercised._ _(P1.2b/F2)_
- [E] **J6 Gemma loads + generates** — after download, Test connection returns a reply and per-paper Ask streams real tokens (CPU/XNNPACK). F2 swapped `GemmaEngine.SPEC` to the standard CPU build `gemma-4-E2B-it.litertlm` (the `-web` build had no CPU decode graph → zero tokens, confirmed 2026-06-19). Confirm the standard build's CPU decode graph runs here. _v2.0.2 (emu 2026-06-26, **V3**): LiteRT loaded the model with `MainExecutorSettings: backend: CPU` and `Creating Gemma4DataProcessor` — **no `TF_LITE_PREFILL_DECODE not found … Skipping`** (the F2 zero-token signature is gone on the standard build). **Test connection → "Connection OK"** (a non-empty reply), and Summarize/Key-contributions presets streamed real grounded answers (below). The F2 fix holds: the standard CPU model emits tokens, not a blank._ _(P1.2b/F2)_
- [E] **J7 On-device works offline** — with the model installed, Test connection (and later chat) returns a reply in airplane mode; confirm **no network traffic** during on-device generation. _v2.0.2 (emu 2026-06-26, **V3**): with **airplane mode ON** (Wi-Fi disabled, the ✈ icon up), tapping the **Key contributions** preset produced a grounded, `[1]`-cited bulleted answer from Gemma (logcat `Creating Gemma4DataProcessor`, no `UnknownHost`/`ECONNREFUSED`/`Offline`). On-device generation is fully network-free._ _(P1.2b)_
- [E] **J8 RAM-floor gating** — on a <4 GB-RAM device the on-device card shows the "not enough memory" note and hides Download; on a ≥4 GB device Download is offered. Recommended-tier line reflects capability. _v2.0.2 (emu 2026-06-23): the **<4 GB branch** verified — at "Device memory: 1973 MB" the card shows "doesn't have enough memory" and hides Download. v2.0.2 (emu 2026-06-26, **V3 6 GB AVD**): the **≥4 GB branch** verified — the same AVD bumped to 6 GB (`hw.ramSize=6144` → "Device memory: 5931 MB") now **shows the On-device card + Download**, and the recommended-tier line reads "Recommended: Cloud (your key)" before download → "**Recommended: Gemma (downloaded)**" after (TierSelector picks GEMMA once `gemmaReady`)._ _(P1.2b)_
- [ ] **J9 Tier degradation** — delete the Gemma model (or clear keys) → recommended tier falls back down **Gemma→LIGHT (Qwen)→Nano→cloud→none** with the card updating live. Explicit step: with Qwen installed and keys cleared, delete Gemma → "Recommended" shows the LIGHT tier AND an Ask turn actually routes on-device to Qwen (the resolution leg, not just the label — J9 as originally written would have caught the 2026-07-03 resolver bug). _(P1.2b / PA.3)_
- [~] **J10 Nano availability** — on a Nano-capable flagship (Pixel 9/Galaxy S25), the on-device card shows Nano status; on a non-flagship it shows "not supported" and degrades to Gemma/cloud (the `genai-prompt` dep must not crash non-flagships). _v2.0.2 (emu 2026-06-23): the **non-flagship branch** is verified — the card shows "Gemini Nano (system): Not supported on this device", the `genai-prompt` dep does not crash, and the app degrades to cloud. The positive Nano-status branch is flagship-hardware-only (the S20 cannot do it either)._ _(P1.2c)_
- [ ] **J11 Enable Nano + generate** — when DOWNLOADABLE, "Enable Nano" downloads the feature (progress), then status → Available; Test connection returns a reply. _(P1.2c)_
- [ ] **J12 Preferred engine switch** — with **≥2 on-device tiers ready**, the preferred-engine chips appear **per ready tier** (e.g. Auto · Gemma · Qwen 0.6B light · Nano); switching changes which engine answers; Auto uses the default order (Gemma→LIGHT→Nano). Persists across relaunch. _(P1.2c / PA.3b)_

## K. Chat & RAG (v2 / P2) _(SPEC-AI-PROVIDERS §6, docs/P2-PLAN.md)_
> CI covers the chunker/retriever (pure), chunk + chat DAOs/migrations (Robolectric),
> `ChatRepository` (fake provider + in-memory DB), and the redaction golden tests. What's
> left for hardware: real on-device generation, streaming feel, privacy-preview fidelity,
> retrieval relevance over a real library, and history persistence. Seeded for P2; each item
> activates when its subphase ships.
- [E] **K1 Chunk embedding on device** — `EmbeddingWorker` chunk-indexes library papers on the unmetered job (eager backfill, bounded per run); `chunk_embeddings` fills (observable via `observeIndexedPaperCount`); a model change wipes mismatched chunks and re-indexes; the v1→v2 migration applies cleanly on an upgraded install. _v2.0.2 (emu 2026-06-23): Settings shows "Model ready — 28 papers embedded locally" (the bge ONNX model auto-downloaded and the worker backfilled), and chat retrieval returns real per-paper chunks (so `chunk_embeddings` is populated). The migration applied cleanly (the app runs end-to-end on the upgraded install). Formal model-change wipe/re-index rides V2-followup._ _(P2.1)_
- [E] **K2 Retrieval relevance** — `RagRetriever` returns on-topic chunks for a real library question, scoped to a paper and to a collection; the hybrid blend (semantic cosine + `chunk_fts` BM25) beats either leg alone on a keyword-y vs paraphrase query (sanity, not a golden set). _v2.0.2 (emu 2026-06-23): paper-scoped retrieval surfaced the AIR abstract as context `[1]` → a correct grounded answer; collection-scoped retrieval over "Reasoning" pulled context from **both** papers (`[1]`+`[2]`) and the answer distinguished them. **Bug found+fixed:** an inbox-only (un-embedded) paper retrieved nothing → ungrounded answer despite the sheet's "grounded in its abstract" — the `ScopeIndexer` Paper branch was a no-op; now binds `RagIndexer.indexPaper` + `AskViewModel` awaits indexing before retrieval (`AskViewModelTest` race test added)._ _(P2.1)_
- [E] **K3 Streaming render** — the `AskSheet` renders tokens incrementally as they arrive (no freeze-then-dump); the Stop button cancels mid-stream cleanly and leaves the partial answer. _v2.0.2 (emu 2026-06-23, **cloud/Claude**): per-paper Ask + collection chat both stream non-blank answers into chat bubbles (markdown rendered). v2.0.2 (emu 2026-06-26, **V3 on-device Gemma**): with prefer-on-device ON, the Summarize preset streamed a grounded, `[1]`-cited markdown summary ("Summary for a Researcher" — TailorMind / profile refinement / textual gradient / Retrieval-Augmented Style Control), and Key contributions a cited bullet list — **real on-device tokens, not a blank bubble** (the F2 S20 blank-bubble guard holds on the standard CPU build). On-device decode is slow on the emulated x86 CPU (tens of seconds) but correct._ _(P2.3 / F2)_
- [E] **K4 Privacy preview fidelity** — before a **cloud** call the `ChatPreviewBuilder` "what leaves the device" confirm shows exactly the system instruction + messages (retrieved chunks folded in) that will be sent; **no provider key** (header-only), no gated note chunks (`includeNotes` off); on-device generation shows **no preview and emits no network traffic** (confirm in airplane mode). _v2.0.2 (emu 2026-06-23): the cloud confirm shows the exact SYSTEM instruction + USER message with the retrieved `[1]`/`[2]` excerpts folded in, and states "Your API key is sent in a header, never in this body" — key structurally absent. v2.0.2 (emu 2026-06-26, **V3**): the **on-device no-preview/no-traffic leg** verified — an on-device (Gemma) turn streams **with no "what leaves the device" confirm at all** (vs. every cloud turn showing one), and generates in airplane mode with no network (J7), so nothing is sent off-device._ _(P2.2 builder / P2.3 sheet)_
- [E] **K5 Provider resolution + fallback** — the explicitly selected provider is used by default; turning on **prefer-on-device-when-ready** uses an on-device engine when ready; an unconfigured selection falls back (on-device first); no usable provider surfaces a clear "configure a provider" state (`NotConfigured`). _v2.0.2 (emu 2026-06-23): the selected provider (Claude) answered every turn; clearing both keys → send surfaces "No AI provider is set up yet… Configure a provider" (`NotConfigured`). The prefer-on-device toggle isn't offered on this AVD (no eligible on-device engine, by design). v2.0.2 (emu 2026-06-26, **V3**): the **"uses on-device when ready"** leg verified — with Gemma downloaded the toggle is offered; turning **Prefer on-device when ready** ON made an on-device (Gemma) engine answer every turn even though Claude was still the selected cloud default._ _(P2.2)_
- [E] **K6 Summaries** — the `AskSheet` summarize chip returns a useful paper summary grounded in the abstract (and notes if included). _v2.0.2 (emu 2026-06-23): the Summarize chip on an embedded paper returned a grounded, markdown-formatted "## Summary" (problem/approach/result) streamed from Claude._ _(P2.3)_
- [E] **K7 Collection (KB) chat** — the collection screen's "Chat" action opens KB chat that retrieves across the collection's papers (ensure-embedded indexes missing papers on open, shown by the "Preparing this collection…" note); adding/removing a paper changes what's retrievable. _v2.0.2 (emu 2026-06-23): "Chat with this collection" over a 2-paper collection retrieved context from both (`[1]`+`[2]`) → a synthesized cross-paper answer. (Both papers were already embedded, so no "Preparing…" note showed — correct.) **Cosmetic note:** the collection chat sheet still uses the per-paper labels ("Ask this paper"). Add/remove-changes-retrieval rides V2-followup._ _(P2.4)_
- [E] **K8 Chat history persists** — the Settings → "Chat history" list shows past sessions (paper title / collection name); sessions + turns survive app restart; tapping resumes the right conversation; delete removes it. _v2.0.2 (emu 2026-06-23): sessions listed with label + scope chip (Paper/Collection) + relative time; survived an app relaunch; tapping the AIR session resumed its exact Q&A; delete removed a session (4→3)._ _(P2.4)_
- [E] **K9 On-device STRUCTURED table validity** — with prefer-on-device ON and Gemma ready, a **comparison** question (the kind the STRUCTURED nudge targets) makes **Gemma E2B emit a well-formed Markdown table**, and the answer contains **no** LaTeX/Mermaid (the STRUCTURED tier forbids them). The RAG-budget half is proven headlessly (`ChatContextAssemblerTest` — the ~50-token nudge drops no chunk at the 4096 on-device window). _v2.0.2 (emu 2026-06-26, **on-device Gemma**, STRUCTURED tier): on the "Prompt Injection… Single and Multi-Injection Settings" paper with prefer-on-device ON, "**Compare the single-injection and multi-injection attack settings**" routed **on-device** ("On-device — nothing leaves your device", no cloud confirm) and Gemma streamed (~20–40 s on emulated x86 CPU) a grounded answer that — per the STRUCTURED exemplar nudge, in **Standard** mode — ended in a **valid, correctly-rendered 2×2 Markdown table** (`| Setting | Observation |` → Single-Injection / Multi-Injection rows, each citing `[1]`), **not raw pipes**. **Grounding held:** it cited `[1]`, honestly flagged that the abstract "does not provide a direct comparison," then tabled only the per-setting observations it could legitimately synthesize (no fabricated table). **No LaTeX/Mermaid** in the answer (STRUCTURED forbids them). Confirms the research prediction (~2B tables valid with a 1-shot exemplar) on our actual Gemma build, and that the nudge doesn't override grounding. Run on `emulator-5554` once the other local session's `dev.helpinghand.companion` agent vacated it for its own emulator (`HH_verify_api34`/5556)._ _(P-Atlas PA.2 / PA.0a)_
- [E] **K10 Qwen3-0.6B light tier CPU-loads + emits tokens (PA.0b/PA.3)** — the pinned `Qwen3-0.6B.litertlm` (614 MB, `litert-community/Qwen3-0.6B`) downloads + SHA-verifies, the `QwenEngine` loads it on the **CPU** backend, and a turn streams **non-zero tokens** (no zero-token F2 signature — the guard throws if it regresses). **Coverage note (2026-07-03):** the 2026-06-26 run had **Gemma coexisting**, so it verified in-provider Qwen routing (`preferredTier`) but **never Qwen-as-only-ready-engine resolution** — which is how the resolver seam bug (K12) shipped masked; tier-alone configurations are now mandatory for additive tiers (SPEC §7). _v2.0.2 (emu 2026-06-26, 6 GB AVD): the **PA.3b UI** showed the **Qwen 0.6B light** card with a Download button (device light-eligible at 5931 MB). Tapping Download ran a foreground-service download (no perms prompt — already granted from V3) that landed **exactly `614,236,160` bytes** at `files/models/light/Qwen3-0.6B.litertlm` (SHA-verified — `ModelDownloader` only renames `.part`→final on a hash match). **Dir-isolation proven on-device:** Gemma (`files/models/gemma-4-E2B-it.litertlm`) and Qwen (`files/models/light/…`) **coexist** — the Qwen download did NOT purge Gemma (the separate `models/light` dir works, validating PA.3a's deleteStaleSiblings catch). The Qwen card flipped to **Test/Delete**; the **Preferred engine** row rendered per-ready-tier chips (`Auto · Gemma · Qwen 0.6B light`). With **prefer-Qwen** selected, a per-paper Ask ("What is this paper about…") routed **on-device** ("On-device — nothing leaves your device", no cloud confirm) and **Qwen streamed a real grounded answer in ~20 s** — referencing the paper's actual content (language-based digital twins, elderly cognitive assistance, LLMs, the I-CONECT dataset, stylometric cues) — **non-zero tokens, not a blank bubble** (no F2 zero-token trap on the CPU build). PLAIN prose (no table — correct for 0.6B); a verbose "thinking-out-loud" style vs Gemma's cleaner output. **Bug found+fixed (this PR):** the Qwen download button reused the shared `ai_ondevice_download` string, mislabeling the size as "~2.6 GB" (Gemma's) → per-model `ai_ondevice_download_light` ("~0.6 GB")._ _(P-Atlas PA.3 / PA.0b)_
- [E] **K11 STRUCTURED `TABLE::` intermediate adherence (PA.4)** — with the shipped app-draws-the-structure prompt, Gemma E2B (the STRUCTURED tier) **reliably emits the `TABLE::` … `::TABLE` block** (one row per line, cells split by `~|~`, `[n]` citations inline) often enough that `StructuredTableTransform` renders a valid GFM table; otherwise it **degrades to a grounded bulleted list** (never a broken table). On a comparison question, confirm the rendered answer shows a real bordered table (or a clean grounded list), that `~|~`/`TABLE::` never leak into the displayed/persisted text, and that citations survive. Measure the `TABLE::`-adoption rate + the parse-confidence distribution on real arXiv chunks; retune `CONFIDENCE_FLOOR` (0.70) if warranted. If adoption is low (Gemma reverts to GFM pipes), the transform is a safe no-op and the fix is prompt iteration, not architecture. _v2.0.2 (emu 2026-06-27, `emulator-5554`): with prefer-on-device ON + Gemma the preferred engine, a comparison prompt ("Compare single-injection versus multi-injection in a table") routed **on-device** (no cloud privacy-confirm) and Gemma E2B **positively adopted the format → a valid GFM table rendered** — read from `chat_messages.content`: `| Setting | Observation |` / `| :--- | :--- |` / `| **Single Injection** | … [1]. |` — the `TABLE::`→GFM render K11 was waiting for, now observed on hardware (and a real bordered table shown in the sheet). **Real bug found + fixed (the value of device verification):** the same answer **leaked a raw `::TABLE`/`~|~` block** — Gemma opened a *second* table with the **closing** fence `::TABLE` and the body had no `TABLE::`, so `StructuredTableTransform`'s `if (!body.contains(OPEN)) return body` guard **returned early** and the machine sentinel reached the user. **Fix:** the gate now triggers on **any** sentinel token, a block opens on **either** fence (a stray `::TABLE` is salvaged into a real table), and a final scrub guarantees no bare `~|~` ever survives — pinned by **4 new `StructuredTableTransformTest` cases** reproducing the exact captured output (now 24/0/0). **Re-verified live after reinstall:** a fresh on-device comparison made Gemma **re-trigger the exact fence-confusion** (it again emitted a stray `::TABLE`/`~|~` block) — and the fixed transform **salvaged it into a second valid GFM table** (`chat_messages.content` persisted with **zero** `~|~`/`::TABLE`, vs the raw-sentinel leak in the pre-fix turn). A textbook before→after on the same device. The splice (FULL/PLAIN byte-identity) stays golden (`ChatRepositoryTest`)._ _(P-Atlas PA.4)_
- [E] **K12 Collection knowledge map — pans/zooms/clusters legibly, offline (PA.5d)** — on a saved collection with citation/similarity edges, the `TopAppBar` "Knowledge map" (Hub) action opens the full-screen map. Confirm: it **opens at the clustered overview** (≤12 super-nodes, not node-soup); **pinch-zoom expands** clusters into members at the tier boundaries (overview→mid→detail) and **fling-pan** is smooth (no jank/ANR — spot-check `dumpsys gfxinfo`) on a ~50- and a ~150-node collection; **tapping a paper node opens that paper**; the whole thing renders with **airplane mode ON** (no network error in logcat — it's app-drawn, no model/provider call); **rotate** preserves the map (the ViewModel re-supplies the scene; no re-derive flash, no crash); **dark/light** toggle recolours nodes/edges/labels live. Empty/sparse collections show the designed empty state. _v2.0.2 (emu 2026-06-26, `emulator-5554` Pixel_3a): the "Reasoning" collection's **Hub action opened the full-screen map**, which **opened at the clustered overview** — a single tertiary-coloured **cluster super-node** labelled with the highest-degree member's title truncated to 27 chars + "…" (the 2 similar papers formed one community). **Cluster-tap drilled in** (the review-fix `focusOn` zoomed+centred past the MID threshold → the cluster expanded to its members joined by a **dashed SIMILAR edge** — dashed-vs-solid styling correct). **Pan** (single-finger drag) moved the canvas. **Airplane mode ON → re-opened the map fresh** (forcing a new DB-only `sceneForCollection`): rendered identically with **zero network errors in logcat** — the app-drawn red line holds on hardware. **Rotate → landscape**: scene retained (VM survived config-change), re-fit, **no crash**, MainActivity stayed focused. **Dark mode**: background→near-black, super-node→light-lavender (dark tertiary), label→white — live recolour from `colorScheme`. **Honest caveats:** the only collection had **2 papers (1 cluster)**, so node-soup / the overview ≤12 cap / multi-cluster LoD were **not** exercised on-device (covered off-device by `GraphSceneLodTest`'s 300-node fixture); **multi-touch pinch** can't be driven via `adb input` so zoom was exercised via the cluster drill-in (the focal-zoom math is unit-tested in `KnowledgeMapViewportTest`); `CLUSTER_DRILL_ZOOM=2.6` spreads a 2-node cluster's members off-screen at the drill zoom (a **tuning note**, not a bug). uiautomator's accessibility tree was stale under Compose, so driven by screenshot coords._ _(P-Atlas PA.5d)_
- [E] **K13 Per-paper "View as map" + the "View as list" a11y fallback (PA.5d)** — the per-paper Connections screen's "Knowledge map" (Hub) action opens the same canvas for one paper's relations (reusing the PA.1 relation graph through `GraphSceneBuilder`). Confirm the **"View as list" toggle** swaps the canvas for a cluster-grouped list of the same papers, that **TalkBack reads the list** (every relation reachable without gestures — the non-spatial a11y path), and that the map's `contentDescription` announces the gesture affordances. _v2.0.2 (emu 2026-06-26, `emulator-5554`): on the "AIR: Adaptive Interleaved Reasoning" paper, the **Connections screen showed the Hub "View as map" action** (next to its "No citation data yet" empty state); tapping it **opened the per-paper map** (a cluster super-node centred on AIR — the per-paper `RelationGraphSource`→`GraphSceneBuilder` path renders). On the collection map, the **"View as list" toggle** swapped the canvas for a **cluster-grouped list**: a primary-coloured section header = the cluster label **truncated to 2 lines + ellipsis** (the review-fix `maxLines`/`Ellipsis`) over the two member paper rows, and the **toggle icon swapped** (List↔Hub). **Tapping a paper in the list opened that paper's detail** (the `onPaperClick` nav). **Caveat:** TalkBack itself was not driven (the `stateDescription` on the toggle + the fully-tappable list content are present in code; a full screen-reader pass is deferred to a focused a11y session)._ _(P-Atlas PA.5d)_
- [E] **K14 Raw-`svg` renders sanitized + safe (P-Share PS.1)** — an AI answer containing a ` ```svg ` block renders as a **real inline vector** (not an escaped code box), in **light + dark + airplane mode** (bundled assets, no network); a crafted **malicious** svg (with `<script>`, `onload=`, `<foreignObject>`, an external `href`/`url(http…)`) renders only the **benign shapes** with **no script execution and no network request** in logcat (the `SvgSanitizer` allowlist + the `blockNetworkLoads` WebView backstop); a non-svg ` ```svg ` fence stays an inert code box. _v2.0.2 (emu 2026-06-26, `emulator-5554`): asked Claude (cloud) "Draw a red circle as an inline SVG image, only the code"; the settled answer **rendered the ` ```svg ` block as a real inline red-circle vector** (via `SvgSanitizer` allowlist → `RichBlockWebView`), not an escaped code box — the on-device sanitise→render path works. **No network errors in logcat** during the WebView render (bundled assets only). The adversarial/malicious-svg neutralisation can't be driven by a real model and stays covered off-device by `SvgSanitizerTest` 14/0/0; dark + airplane follow the same bundled-asset path._ _(P-Share PS.1)_
- [E] **K15 Read-aloud speaks an answer cleanly (P-Share PS.2)** — a settled answer's **Read aloud** action (in the Ask sheet's per-answer row) speaks the prose through the system TTS, **offline / airplane mode** (no network, no AI key); rich blocks are voiced as captions ("Diagram"/"Equation"/"Image"), **not** raw LaTeX/Mermaid, and `[n]` citations aren't read as "bracket one"; the button toggles to **Stop** while reading and re-tap / sheet-dismiss / sending the app to the **background** all stop it; on an engine/voice-less device it no-ops gracefully (no crash). _v2.0.2 (emu 2026-06-26, `emulator-5554`): the **Read-aloud** action dispatched **offline TTS synthesis** — logcat showed `TextToSpeech: Setting up the connection to TTS engine…`, `GoogleTTSServiceImpl: Synthesis request for locale eng-USA`, and `TTS dispatch: en-us-x-iog-seanet-embedded` (the **embedded/offline** voice — no network) — and the action **toggled to a Stop icon** while speaking, auto-reverting on completion (the `speakingId` flow). For the Mermaid answer the spoken text is the **"Diagram." caption** (rich block voiced as a caption, not raw Mermaid). **Caveat:** the audio itself can't be heard over adb — verified via the synthesis-dispatch logs + the Stop-toggle + no-crash; the extraction + chunker stay golden-tested off-device (`SpeakableTextTest`, `TextToSpeechManagerTest`)._ _(P-Share PS.2)_
- [E] **K16 Per-bubble contextual actions (P-Share PS.3)** — under a settled assistant answer, the **inline action row** shows Copy · Read-aloud · Share and an **overflow ⋮** opens Pin-to-notes (paper scope) + Quote; **Copy** puts the markdown answer on the clipboard, **Quote** prepends a blockquote of it into the Ask input, **Pin** flips to "Added to notes", and Read-aloud/Share behave as before. A **user question** bubble shows a lone **Copy**; an **error** bubble shows no actions. The overflow menu opens/dismisses cleanly inside the modal sheet. _v2.0.2 (emu 2026-06-26, `emulator-5554`): under a settled assistant answer the **inline row** rendered **Copy · Read-aloud · Share · ⋮** and the **overflow** opened **Pin to notes · Quote in a follow-up · Export as image** (content-descs confirmed in the dump); the **user-question** bubble showed a lone **Copy**. **Quote** prepended a capped blockquote of the answer into the Ask input (`> ```mermaid graph LR n0{{"AI Healthcare Chatbots as…` — `quoteInto` whitespace-collapse + 200-char cap). The overflow opened/dismissed cleanly inside the modal sheet. (Copy→clipboard is code-verified; the adb clipboard service isn't readable here.)_ _(P-Share PS.3)_
- [E] **K17 Export an answer as an image (P-Share PS.4b)** — a rich answer's overflow **Export as image** renders it to a PNG **off-screen** and opens the OS share sheet; the PNG is the full, faithfully-rendered answer (light + dark), and the share works (FileProvider grant + chooser preview). _v2.0.2 (emu 2026-06-26, `emulator-5554`): on the app-drawn **Map relationships** Mermaid graph, Export-as-image produced a **1080×820 PNG** capturing the **complete** graph — centre hexagon + all 6 related-paper nodes with similarity scores + dashed edges, crisp, on the correct `surfaceContainerHigh` background; **dark mode** produced the same graph on a near-black bg with light text/borders (theme threaded through `RichTheme`). The chooser opened with a **working image thumbnail preview** + target list (Nearby Share/Print/Drive/Messages). **Three real bugs found+fixed this PR** (see the log row). **Caveat:** completing a share to a real target wasn't driven (no accounts on the emulator); the FileProvider grant + the rendered preview confirm the path. The off-device foundation (FileProvider config, capture geometry `RichRenderSignal.deviceHeightPx`) stays golden-tested (`RichHtmlTest`)._ _(P-Share PS.4b)_
- [ ] **K18 Save/share the paper + citations (P-Share PS.5)** — on the paper detail screen, the bar **overflow ⋮** opens **Share PDF · Copy reference · Copy BibTeX**. **Share PDF** opens the OS sheet with the already-downloaded PDF (`application/pdf`, FileProvider grant + chooser preview); when the PDF is **not** downloaded it falls back to sharing the arXiv PDF **link** as text with a "sharing the link instead" snackbar (no silent download → arXiv rate limiter untouched). **Copy reference** / **Copy BibTeX** put the formatted reference / the `@misc{…}` entry on the clipboard with a confirming snackbar; the BibTeX parses (escaped braces) and the reference reads `Authors (Year). Title. arXiv:id [class]. <link>`. The existing one-tap **Share** (title + abs link) and the Ask-sheet **Pin to notes** (PS.3, = "save answer to notes") are unchanged. Pure citation rendering is golden-tested off-device (`CitationTest` 8/0/0). _v2.0.2 (emu 2026-06-27, `emulator-5554`): on the "AI Healthcare Chatbots" paper, the bar **overflow ⋮** opened **Share PDF · Copy reference · Copy BibTeX** (text/icons confirmed). **Copy BibTeX** → snackbar **"BibTeX copied"**; **Copy reference** → **"Reference copied"** (clipboard content stays golden-tested — the adb clipboard service isn't readable). **Share PDF — link fallback** (no local PDF): the OS sheet opened **"Sharing link"** with `https://arxiv.org/pdf/2606.27302v1` as **text** — no file, no download. After **Read PDF** fetched `2606.27302v1.pdf` into `files/pdfs/`, **Share PDF** opened **"Sharing 1 file"** with the real **`2606.27302v1.pdf`** (`application/pdf` via the new `files-path pdfs/` FileProvider — grant works, no SecurityException). Both paths verified._ _(P-Share PS.5)_
- [ ] **K19 Whole-conversation export (P-Share PS.6)** — in the Ask sheet, once a settled answer exists, the header **share icon** opens a menu: **Share as text · Export Markdown file · Export as PDF**. **Share as text** opens the OS sheet with the conversation Markdown (prior behavior). **Export Markdown file** writes a `.md` and opens the sheet (`text/markdown`, FileProvider grant + chooser preview); the file content matches the on-screen turns (You/Assistant + Sources, scope `# header`). **Export as PDF** opens the **system print dialog** → Save as PDF / print produces a **paginated** PDF on a **light** background (regardless of app dark mode), with math/diagrams/tables **rendered** (not raw `$$`/mermaid source) and multi-page content flowing across pages; a no-rich-block conversation prints as clean text. The capture WebView is removed after the job (no leak). Pure Markdown + rich-HTML rendering stay golden off-device (`ConversationMarkdownTest`, `RichHtmlTest`); the print pipeline is genuinely device-bound. _v2.0.2 (emu 2026-06-27, `emulator-5554`): in a resumed multi-turn Ask conversation, the header **"Export or share this conversation"** icon opened **Share as text · Export Markdown file · Export as PDF**. **Share as text** → OS sheet, conversation Markdown starting `# AI Healthcare Chatbots…` (the scope header). **Export Markdown file** → OS sheet **"Sharing 1 file"** `conversation_1782533672563.md` (`text/markdown`, FileProvider) + a "Preparing Markdown…" toast. **Export as PDF** → the **system print dialog** (`com.android.printspooler`), **ISO A4**, **2 pages** (native pagination), **light** background (PRINT_THEME forces light regardless of app theme), rendering the multi-turn **You/Assistant** structure + a **fully rendered Mermaid relation graph** (nodes/weights/edges, not raw source) + a **rendered red-circle SVG** flowing across the A4 page break — the host-driven readiness wait rendered the async KaTeX/Mermaid/SVG before printing. (Completing Save-as-PDF to a file not driven; the print preview confirms pagination + rendering.)_ _(P-Share PS.6)_

- [x] **K20 Qwen-ONLY provider resolution (the 2026-07-03 user-reported bug)** — with Gemma **absent** (deleted/parked, not merely deprioritized) AND **no cloud keys** AND Nano unavailable, only `Qwen3-0.6B.litertlm` downloaded: per-paper Ask routes **on-device** and streams a grounded answer — **no `ask_not_configured`**; the sheet shows "On-device — nothing leaves your device"; Settings-usable and the Ask gate agree. Run with prefer-on-device **OFF** (keys-present + OFF would re-mask via silent cloud routing) and ON. Off-device pins: `AppModuleResolverWiringTest` (real `@Provides` composition, Qwen-only → `Resolved(ON_DEVICE)`), `ResolverReadinessSourceStructuralTest` (hand-enumeration = red build). _v2.0.3-dev (emu 2026-07-03, `emulator-5554`): **reproduced the user's exact bug on the pre-fix build** — Gemma parked out of `files/models/`, AI-keys prefs parked, Qwen (614 MB, SHA-verified) the only engine → firing a Summarize preset showed **"No AI provider is set up yet…"** (screenshot-matched the user's report). **Post-fix APK, same device state:** the same Summarize preset resolved **on-device** — the sheet flipped to **"On-device — nothing leaves your device."** — and Qwen **streamed a complete 1504-char grounded answer** (Sources · 1; the summary correctly described the paper's stateful link-tracker monitor + the 93%→47% gradual-attack-evasion result; XNNPack cache built at 574 MB with the preflight passing at 1.1 GB free). **Bonus find #1 (real bug, fixed this PR):** with the disk at 88% (752 MB free), Qwen's first `Engine.initialize()` **SIGABRT-crashed the whole app** writing its XNNPack cache (`no space left on device` in the tombstone; `installd` then purged Gemma's 788 MB cache) → `EngineCachePreflight` now refuses first-init gracefully (`AppError.Storage`) instead of the native crash. **Bonus find #2 (→ PA.6 follow-up):** Qwen3's **`<think>…</think>` reasoning monologue renders to the user** before the real answer (K10's "verbose thinking-out-loud style", now diagnosed precisely — fix via Qwen3's `/no_think` soft switch in `QwenEngine.promptOf`). **Bonus find #3 (→ backlog):** the mid-stream crash left a **ghost empty assistant bubble** (0-char `incomplete` row) rendered in the resumed sheet._ _**REAL DEVICE (2026-07-04): the reporter confirmed Qwen now works on the original device that surfaced the bug** — the Ask sheet resolves on-device and answers; K20 closed `[x]`._ _(P-Atlas readiness hotfix)_

- [ ] **K21 On-device UX refinements (PA.6 follow-up)** — (a) **concurrent downloads:** start Gemma + Qwen downloads in one settings visit → **two distinct progress notifications** (ids 4201/4202), each surviving the other's completion; (b) **completion feedback:** with the app backgrounded, a finished download posts the non-ongoing "%s ready — ask any paper in your library" notification (4203/4204), and in-app the settings snackbar fires on the Downloading→Ready edge; (c) **tier-pinned Test:** with both models present, the Qwen card's Test visibly exercises Qwen (slow ~0.6B stream) and its result renders inside that card — delete Qwen and its Test errors rather than silently streaming Gemma; (d) **no `<think>` leak:** a fresh Qwen answer starts with the answer, not a reasoning monologue (`/no_think` + stream filter; off-device: `ThinkBlockFilterTest` 8/8). _Partially observed (emu 2026-07-03): the refreshed AI-providers surface renders end-to-end — new intro/privacy copy, per-card descriptions, per-card Test buttons; the **Qwen card's pinned Test returned "Connection OK" rendered inside that card** (the pin exercised the real Qwen engine on its warm cache); prefer-chips render single-line (Auto · Gemma · Qwen light) after the short-name fix this PR. Remaining for a device session: concurrent-download notifications, backgrounded completion notification + snackbar edge, delete-Qwen→Test-errors, and a fresh no-think answer._ _(PA.6 follow-up)_

## M. arXiv HTML reader & network (Phase P-HTML) _(SPEC-P-HTML)_
> CI covers the security/network spine off-device: `HtmlSanitizer` adversarial corpus + fixed-point
> (PH.1), `AllowedHosts`/interceptor + the `PdfDownloader` rate-limit retrofit + the no-direct-`newCall`
> structural guard (PH.2). Hardware proves the retrofit doesn't regress PDF now, and (PH.4+) real
> native+ar5iv HTML rendering, offline reading, native-MathML fidelity, and the fallback chain.
- [ ] **M1 PDF rate-limit retrofit — no regression (PH.2)** — opening a paper's PDF still works on device; the dedicated `@ArxivClient` client + `rateLimiter.acquire()` (cache-miss only) adds the ≥3s spacing without a perceptible stall on a normal open, and a second rapid arXiv action (open a PDF right after a sync/search) **visibly spaces** rather than racing; a **cached** PDF re-opens **instantly** (no spacing); the `AllowedHosts` gate never blocks a legitimate `arxiv.org`/`ar5iv` fetch. Off-device: `PdfDownloaderTest` (cache-hit zero-acquire, cache-miss acquire-once, error mapping), `AllowedHosts(Interceptor)Test`, `NoDirectNewCallStructuralTest` all green. _(P-HTML PH.2)_
- [E] **M2 Native HTML render, end-to-end (PH.4 — PRIMARY gate)** — on a **recent (≥2024) paper**, the paper-detail "Read HTML" button opens the reader and the **native `arxiv.org/html/<id>`** edition renders: reflowable body text, headings/sections, figures shown as the figcaption placeholder (images are PH.5), legible in **both light and dark** (theme vars applied). No Typekit/CDN fetch (no flash of a webfont; `blockNetworkLoads` backstop). _(P-HTML PH.4)_
- [ ] **M3 ar5iv fallback + honest banner (PH.4)** — on a **pre-2023 paper** (native 404s), the reader falls back to **`ar5iv.labs.arxiv.org`** and renders, with the persistent **"community ar5iv conversion"** banner shown at the top and **announced by TalkBack** (`liveRegion`). _(P-HTML PH.4)_
- [E] **M4 Airplane-mode render from cache — zero egress (PH.4 — red-line)** — open a paper's HTML once online, enable **airplane mode**, re-open → it renders **from the `HtmlStorage` cache** with **no network egress** (confirm via `dumpsys`/proxy: no requests). A never-cached paper in airplane mode degrades gracefully (error state + reachable "Read PDF instead"), not a crash. _(P-HTML PH.4)_
- [E] **M5 Native-MathML fidelity stress sample (PH.4 → drives PH.8 go/no-go)** — a math-heavy paper renders **display equations, matrices, stretchy delimiters, and sub/superscripts** correctly via the **native Chromium WebView MathML** on **Pixel_3a_API_34** AND on an **API-26/27 AVD with an old System WebView** (the minSdk floor). Record a per-AVD verdict; broad failure on the floor makes **PH.8** (bundled offline MathML fallback) non-optional. _(P-HTML PH.4; needs the API-26/27 old-WebView AVD — see HUMAN.md)_
- [ ] **M6 Fallback-to-PDF + sane back-stack (PH.4)** — a paper with **no HTML at either source** auto-routes to the **PDF viewer** (one-shot, no flicker back to a dead HTML screen); the always-present **"Read PDF instead"** toolbar action works from any state; **Back from the PDF returns to paper detail** (the HTML route was popped `inclusive`), never to an empty HTML screen. _(P-HTML PH.4)_
- [ ] **M7 Cross-reference + external-link confirm (PH.4)** — tapping a **bibliography/cross-paper cite** that targets **another arXiv paper** opens it in-app (`arxiver://paper`); an **external link** raises the **confirm dialog** and only opens in the browser on explicit confirm (**never auto-opens**); an in-doc **anchor** cite scrolls within the document. _(P-HTML PH.4)_
- [ ] **M8 TalkBack reads the reader (PH.4 — a11y)** — the reader's WebView content is reachable/readable by **TalkBack**; the toolbar **back** and **"Read PDF instead"** carry labels; the ar5iv banner announces once. _(P-HTML PH.4)_
- [ ] **M-PH5-1 Figures render via `data:` with both network blocks armed (PH.5)** — on a real signed build, a figure-laden paper shows its figures, while `blockNetworkLoads=true` AND `blockNetworkImage=true` AND `baseUrl=null` (proves the `data:`-images-exempt-from-network-blocks design in situ; no virtual origin). _(P-HTML PH.5)_
- [ ] **M-PH5-2 Airplane-mode figures from cache — zero egress (PH.5 — red line)** — re-open a previously-cached figure-laden paper in **airplane mode**: all inlined figures render from disk (`index.html`) with **no network egress** (confirm via `dumpsys`/proxy). _(P-HTML PH.5)_
- [ ] **M-PH5-3 Negative egress + cache re-gate (PH.5 — red line)** — a deliberately-injected absolute external `<img src=https://…>` triggers **no network request** (sanitizer strip + transform host-reject + `blockNetworkLoads`); a hand-tampered cached body with an external host is **dropped + re-fetched** (`assertNoExternalHost` re-gate). _(P-HTML PH.5)_
- [ ] **M-PH5-4 Cap ratification (PH.5 — PROVISIONAL numbers)** — open a genuinely **figure-heavy / survey paper** AND a paper with a single **near-`IMAGE_MAX_BYTES`** image; confirm `loadDataWithBaseURL` renders without **truncation** and memory/scroll are acceptable on the low-RAM **API-26** target. Record measured `index.html` size + peak heap; **ratify or retune** `IMAGE_COUNT_CAP`/`IMAGE_MAX_BYTES`/`IMAGE_TOTAL_BYTES_BUDGET`/`IMAGE_FETCH_DEADLINE_MS` in HUMAN.md. _(P-HTML PH.5)_
- [ ] **M-PH5-5 Over-cap / fetch-failure degrade (PH.5)** — a `>IMAGE_COUNT_CAP` paper and a paper whose images fail mid-fetch both show **figcaption placeholders** (never broken-image glyphs); placeholder text is TalkBack-readable. _(P-HTML PH.5)_
- [ ] **M-PH6-1 Rotation + dark-mode toggle mid-read (PH.6)** — position restored to within a screenful (anchor-exact where an anchor stands); NO flash-to-top (conceal-reveal); the 500 ms reveal failsafe never leaves the reader blank; repeat on a multi-MB `data:`-inlined figure-heavy paper at the API-26 floor. _(P-HTML PH.6)_
- [ ] **M-PH6-2 Phase-2 figure reload holds the position (PH.6)** — first online open, scroll mid-read within the 30 s window: position held exactly (anchor payload) despite content-height inflation; a TOC/cite jump followed by the phase-2 reload re-targets the ANCHOR; ratify `JUMP_SETTLE_MS`=2500 against the real smooth-scroll duration. _(P-HTML PH.6)_
- [~] **M-PH6-3 TOC jumps on device (PH.6)** — section/figure/table rows scroll correctly (smooth, dismiss-then-act); the bibliography row lands on the first bib entry; the delayed re-apply never double-jumps after a user scroll; sheet + nested LazyColumn drag interplay on a 100+ row TOC. _(P-HTML PH.6)_
- [ ] **M-PH6-4 TalkBack pass (PH.6)** — `cd_toc` announced; group headers navigate as headings; rows read labels (never raw ids); "Jumped to …" announced on a TOC tap; heading navigation works INSIDE the reader WebView (LaTeXML h2–h6 survive sanitization). _(P-HTML PH.6)_
- [E] **M-PH6-5 Airplane-mode cross-session resume (PH.6)** — kill the app, re-open a previously-read paper offline → cache hit + sidecar restore to the last position, zero egress. _(P-HTML PH.6)_
- [ ] **M-PH6-6 Probe/restore timing truths (PH.6)** — load-reset scroll-event ordering vs `onPageFinished` (tick suppression correct); layout drift on a 4 MB+ `data:` body before/after the guarded delayed re-apply; stale probes generation-dropped. _(P-HTML PH.6)_
- [ ] **M-PH6-7 Exit-path persistence (PH.6)** — back-nav and "Read PDF instead" within ~1 s of scrolling still persist the last settled position (`onCleared` flush); process-kill loses at most the stretch since the last scroll-idle probe. _(P-HTML PH.6)_
- [~] **M-PH7-1 Find on a real offline paper (PH.7)** — highlights + "n of m" correct on a multi-MB `data:`-inlined paper; counts settle only on `isDoneCounting` for large documents; **dark-theme legibility of the fixed Blink match markers** over the reader's dark CSS (not embedder-controllable — the only lever is the reader palette; backlog row pending this verdict). _(P-HTML PH.7)_
- [ ] **M-PH7-2 Find × PH.6 reload interplay (PH.7)** — theme-toggle/phase-2 reload with the find bar open: highlights re-issue once, painted under the conceal-reveal gate they appear on reveal, the match scroll wins over the delayed re-apply (no viewport yank), and a late FindListener callback from the OLD document never updates the new bar (generation drop observable). Record whether the re-issued find visibly scrolls to the first match (ratifies "find wins the scroll"; fallback = clear find on reload). _(P-HTML PH.7)_
- [~] **M-PH7-3 Selection→Ask end-to-end (PH.7)** — long-press → "Ask about this" (after Copy, floating toolbar anchored to the selection) → sheet opens with the "> excerpt" prefill → edit → Send → the cloud ConfirmCard shows the excerpt **verbatim**; a second selection in the same session applies its new quote (consume-once-by-id); the rare dead-tap (selection collapsed between click and read) dismisses quietly. _(P-HTML PH.7)_
- [ ] **M-PH7-4 TalkBack pass (PH.7)** — the floating-toolbar Ask item reachable by swipe navigation + announced (no platform contract — if it fails, the toolbar Ask icon is the guaranteed path and the PROCESS_TEXT escalation goes to HUMAN.md); find announces "Match n of m"/"No matches found"; next/prev/close labeled; IME focus lands in the find field. _(P-HTML PH.7)_
- [ ] **M-PH7-5 Selection mechanics on the floor (PH.7)** — stock + custom selection ActionMode render correctly inside the Compose AndroidView on API 26; IME opening for the find field fires no spurious scroll tick (no probe-saved shifted position); Android 14+ mouse/DeX dropdown lacks the Ask item — confirm + record as the known platform limitation. _(P-HTML PH.7)_
- [~] **M-PH7-6 Reader-hosted Ask parity (PH.7)** — pin-to-notes lands in the paper's notes with the snackbar; share/export work; cross-ref opens the paper; the session resumes the SAME conversation the detail screen shows; the top-bar Ask works with no selection. _(P-HTML PH.7)_
- [ ] **M-PH5-6 First-paint latency + no starvation (PH.5)** — text + math paint promptly on first download (two-phase), figures pop in after; the **30s deadline** stops a pathological survey paper from holding the shared ≥3s slot indefinitely, and a **concurrent FollowSync still progresses** during a figure-heavy open. _(P-HTML PH.5)_

## Q-PT. Agentic in-chat tool-use (Phase P-Tools) _(SPEC-P-TOOLS)_
> CI covers the pure/loop/registry/migration/disclosure pieces: `ChatToolLoopTest`, `ToolRegistryTest`,
> `ToolPackageNoOkHttpStructuralTest`, `Migration4To5Test`/`Migration5To6Test`, `ChatRepositoryTest`,
> `ChatPreviewBuilderTest`, provider SSE tool-block tests. What needs a live cloud key + hardware:
- [ ] **Q-PT1 Library search, live (PT.1)** — a cloud turn with the **Library** chip on: the model calls `search_my_library`, the inline activity bubble shows "Searched your library: …" (neutral, `egress=false`), and the answer cites saved papers as prose `arXiv:<id>`. With the chip off, no tool attaches (byte-identical to a pre-tools turn).
- [ ] **Q-PT2 arXiv search/get/import round-trip, live (PT.2)** — a cloud turn with the **Web search** chip on: `search_arxiv` (fragmented `input_json_delta`/`functionCall` SSE reassembles), `get_paper`, and `import_to_library` each run; each activity bubble shows the accent-tinted "… (left your device)" wording (`egress=true`), and the confirm sheet discloses "sends your query to arXiv, a third party."
- [ ] **Q-PT3 Import → reader loop-closer (PT.2)** — after `import_to_library` of a fresh id, the paper appears in the Library AND opening it lands in the P-HTML reader / PDF by the same (un-versioned) id — no re-fetch, no new nav.
- [ ] **Q-PT4 Two-gate consent + per-message stickiness (PT.2)** — Library-on / Web-off honors library-yes-web-no (no arXiv egress); flipping a chip between two consecutive sends attaches/omits the class accordingly; both chips survive process death (persisted columns). A hallucinated `search_arxiv` in a web-off turn is refused with no egress.
- [ ] **Q-PT5 Rate-limit spacing on a multi-call turn (PT.2)** — a 3–4-call external turn spaces ≥3s between arXiv fetches (serial through the shared limiter); no burst; a concurrent FollowSync still progresses.
- [ ] **Q-PT6 TalkBack on the composer deck** — the Library/Web-search chips announce their selected state + label; the egress activity bubbles read their "left your device" text.
- [ ] **Q-PT7 Semantic Scholar search, live (PT.3)** — a cloud turn with the Web-search chip on: `search_semantic_scholar` returns S2 hits (incl. non-arXiv), the activity bubble reads **"Searched Semantic Scholar (left your device)"** (NOT arXiv), and the confirm sheet discloses **"sends your query to Semantic Scholar"**. An arXiv-keyed hit (`importable`) then imports via `import_to_library` + opens in the reader; a non-arXiv hit does not import.
- [ ] **Q-PT8 S2 host-gate now fires + CitationSync unaffected (PT.3)** — S2 traffic reaches `api.semanticscholar.org` through the `@ArxivClient` interceptor (allowed); the nightly `CitationSyncWorker` still spaces ~1.2s/call and completes (no ≥3s over-throttle from the client move).
- [ ] **Q-PT9 Optional S2 BYOK key (PT.3)** — Settings → the "Semantic Scholar" card: entering a key persists it (masked, never shown again, survives restart); with a key set the S2 request carries `x-api-key`; clearing it removes the header. The free tier (no key) still returns results. Key never appears in a backup export.
- [ ] **Q-PT10 chemRxiv search, live (PT.4)** — a cloud turn with the Web-search chip on: `search_chemrxiv` returns chemistry preprints; the activity bubble reads **"Searched chemRxiv (left your device)"** (NOT arXiv/Semantic Scholar), the confirm sheet discloses **"sends your query to chemRxiv, a third party,"** the model cites results by DOI in prose, and **no import is offered** for a chemRxiv (DOI-keyed) hit.
- [ ] **Q-PT11 chemRxiv live-GET envelope + host-gate (PT.4)** — the one thing CI can't reach: confirm the **live `/engage/chemrxiv/public-api/v1/items` response actually matches the DTOs** (real hits parse, not silently-empty from a renamed key); chemRxiv traffic reaches `chemrxiv.org` through the `@ArxivClient` interceptor (allowed); an off-host asset-PDF **redirect is blocked per-hop**; polite-mutex ~1.2s spacing holds under a concurrent arXiv/S2 turn.
- [ ] **Q-PT12 P-Tools phase-closer full loop (CHECKPOINT device gate)** — one agentic session exercising all four external hosts (arXiv + S2 + chemRxiv) **and** the local library: two-gate consent honored, per-host activity bubbles render correctly, ≥3s arXiv spacing under a multi-call turn, and the **import→reader loop-closer** (search arXiv → import → opens in the P-HTML reader by id) works end-to-end.

## Q-PS. First-class multi-source papers (Phase P-Sources) _(SPEC-P-SOURCES)_

- [ ] **Q-PS1 chemRxiv import → store → organize round-trip, live (PS.1)** — a cloud turn with the Web-search chip on: `search_chemrxiv` returns a hit with `importable:true`; the model calls `import_to_library(source:chemrxiv, doi:…)`; the paper appears in the **Library** with a **chemRxiv source badge**, opens to a detail screen showing the badge + a **Source: chemRxiv** metadata row + the DOI (no "arXiv ID" row, no "Read HTML" or "Send to Claude" button), and can be tagged / added to a collection / found by on-device search like any paper.
- [ ] **Q-PS2 chemRxiv PDF reads in-app (PS.1) — the reading payoff** — opening the imported chemRxiv paper's **Open PDF** downloads and renders its real chemRxiv PDF in the in-app viewer (filename `_`-sanitized from the `chemrxiv:<doi>` id); airplane-mode re-open serves it from cache with no egress.
- [ ] **Q-PS3 chemRxiv PDF fetch spacing + host-gate (PS.1)** — the chemRxiv PDF fetch reaches its host through the `@ArxivClient` interceptor and self-spaces on the **~1.2s polite** slot; a **concurrent arXiv PDF/Atom fetch still spaces ≥3s** (the per-host reversal holds — arXiv is never sped up, chemRxiv never eats arXiv's 3s slot).
- [ ] **Q-PS4 (device-only) chemRxiv `pdfUrl` CDN redirect posture (PS.1)** — the one thing CI can't reach: does the chemRxiv `pdfUrl` resolve same-host, or 302 to an off-allowlist `assets.chemrxiv.org` CDN? **Graceful degrade now shipped:** on any failed PDF load the viewer shows "Open in browser" (`canonicalUrl()` → `doi.org/<doi>`), so a fail-closed chemRxiv PDF is never a dead spinner — verify this degrade renders + the browser opens the paper. **Then capture the real `pdfUrl` host:** if it's `assets.chemrxiv.org` (or a `chemrxiv.org` 302 → that CDN), the in-app render needs that ONE exact sub-domain allowlisted (never a wildcard) — a **Co-Founder host decision** logged in HUMAN.md. Confirm the exact host from a device network trace before allowlisting.
- [ ] **Q-PS5 backup round-trip on device (PS.1)** — export a library containing a chemRxiv paper → the JSON carries its real chemRxiv URL + `"origin":"chemrxiv"` (no `arxiv.org/pdf/chemrxiv`), and importing it on a fresh install restores the paper (origin + real PDF URL byte-unchanged) alongside its tags/notes/collection; a pre-P-Sources **v1** backup file still imports (arXiv rows, PDF URL re-synthesized).
- [ ] **Q-PS6 organize/search parity (PS.1)** — a chemRxiv paper is **semantically searchable** (it embeds — `source=MANUAL` passes the embedding gate) and appears in hybrid `search_my_library` results, collections, and the citation-neighbors surface, indistinguishable from an arXiv paper except for its badge/source metadata.
- [ ] **Q-PS7 bioRxiv/medRxiv OA import → in-app PDF read, live (PS.2) — the reading payoff** — a cloud turn with the Web-search chip on: `search_semantic_scholar` returns a bioRxiv/medRxiv hit whose `openAccessPdf.url` host is `www.biorxiv.org`/`www.medrxiv.org` → `importable:true`; the model calls `import_to_library(source:biorxiv|medrxiv, doi:…)`; the paper lands in the Library with the correct source badge and its **Open PDF** downloads + renders the real bioRxiv/medRxiv PDF in-app; airplane-mode re-open serves from cache with no egress. **Device unknown CI can't reach:** whether the app's real `Arxiv/<ver>` UA actually gets the PDF (a UA-less/datacenter fetch may hit a 403/challenge) — confirm a live render on hardware; if it fails-closed, the graceful external-open (Q-PS8) is the floor.
- [ ] **Q-PS8 arbitrary-host / doi.org OA hit stays read-only (PS.2)** — an S2 bio/medRxiv (or any) hit whose `openAccessPdf.url` host is **not** allowlisted (e.g. `doi.org`, a publisher host) comes back `importable:false`; the model cites the DOI/URL for external-open and `import_to_library` on it errors cleanly — the paper **never renders in-app** and no `papers` row is created. Confirms the host-gate fails closed on device (the interceptor backstop + the search-time gate agree).
- [ ] **Q-PS9 bio/med PDF fetch spacing + host-gate (PS.2)** — a bioRxiv/medRxiv PDF fetch reaches its host through the `@ArxivClient` interceptor and self-spaces on the **~1.2s polite** slot; a **concurrent arXiv PDF/Atom fetch still spaces ≥3s** (the per-host reversal holds — arXiv is never sped up, bio/med never eat arXiv's 3s slot). Mirrors Q-PS3 for the two new hosts.

## P-Reader2. Universal PDF full-text + elevated reading (Phase P-Reader2) _(ROADMAP P-Reader2)_

> PFT.5 promotes full-text body search to **PDFs** via `pdfbox-android` (FULL bundle, offline) in the isolated
> `:core:pdf` module. The extractor + gate + arbitration are CI-proven; the rows below are the device confirmations
> CI can't run — the sharpest being extraction QUALITY on real two-column arXiv PDFs and the no-egress guarantee.

- [ ] **PR2-A2a PDF text extraction quality on real arXiv PDFs (PFT.5.2)** — open several real papers' PDFs (a
  dense **two-column** paper, a **ligature/math-heavy** one, a **scanned** one) and dump `PdfBodyTextExtractor.extract`
  output (debug hook or the resulting body chunks): the two-column + ligature bodies read as **legible prose** (not
  column-interleaved or CID-mangled); the scanned/image-only one returns **""** (→ gated out, not indexed). Confirms
  the extractor + the FULL font bundle work on device, not just the tiny CI fixture.
- [ ] **PR2-A2b `sortByPosition` A/B ratification (PFT.5.2)** — extract the SAME two-column PDF with `sortByPosition`
  **false vs true** and compare reading-order coherence; ratify the shipped `DEFAULT_SORT_BY_POSITION` (currently
  `false` — content-stream order, arXiv-pdflatex-friendly) or flip it. Record the winner + why.
- [ ] **PR2-A2c pdfbox no-egress on device (PFT.5.2)** — packet-inspect a full open→extract cycle: pdfbox opens **zero
  sockets** (all fonts/glyphlists load from the bundled assets; no font/network fetch). The structural source
  tripwire (`PdfboxNoNetworkStructuralTest`) + this device row together are the "zero egress" evidence (no single
  unit test can prove it, since pdfbox runs on `java.net`, not the gated OkHttp client).
- [ ] **PR2-A5a PDF full-text goes live end-to-end (PFT.5.7)** — open a **non-arXiv** (chemRxiv/bioRxiv) PDF — which
  has no HTML edition, so PDF is the only body source — then in Explore → Library search a **body-only phrase** from
  it → it surfaces under **"Also found in full text"** and the coverage caption count increments. Re-opening does not
  re-embed (the `OK:<model>` marker). An **arXiv** paper opened in BOTH readers stays HTML-derived (the PDF path
  defers — no clobber). Confirms the trigger + arbitration on device.
- [ ] **PR2-A5b one-time id-backfill covers the pre-existing corpus (PFT.5.7)** — with PDFs downloaded **before** this
  build (no `.pdf.id` sidecar), let one unmetered worker pass run → the id-backfill writes `.pdf.id` for the papers it
  can match and `backfillPdf` indexes them, so the existing library becomes full-text-searchable **without** re-opening
  each. (Verifiable on the emulator, which already holds pre-PFT.5 downloaded PDFs.)
- [ ] **PR2-A5c release-build (R8) extraction — MOOT while `isMinifyEnabled = false`.** No R8 stripping today, so
  pdfbox's reflection-heavy font loading is not at risk; the `com.tom_roush.**` / `org.bouncycastle.**` keep-rules are
  committed (inert) so a future re-enable is safe. Re-check extraction on a minified release **only if** minification
  is turned back on.
- [ ] **PR2-C1a smart-invert night render preserves figure hue (PR.UX.1)** — open a paper with a **colour figure**
  (e.g. a plot with red/green/blue traces or a heat-map) in the PDF reader and toggle night mode. Under the new
  smart-invert the traces keep their **hue family** (red stays red-ish, blue stays blue-ish) on a soft-dark page —
  NOT the old plain-negation look where red→cyan / blue→orange. Black body text renders soft-light (~230), the page
  soft-dark (~18), neither a blinding pure white nor pure black. A/B against a pre-PR.UX.1 build to confirm the win.
- [ ] **PR2-C2a DPI-aware render is crisp + memory-stable (PR.UX.2)** — open a text-dense PDF on a hi-DPI device and
  confirm glyphs are **sharp** (no upscale blur the old flat-1080 render showed on a 1440px screen); pinch/scan a
  figure to confirm fine detail holds. Then **fast-fling through a long PDF** (50+ pages) and watch memory in
  Android Studio's profiler: page bitmaps are freed as items scroll off (the `DisposableEffect` recycle) — the heap
  should plateau, not climb unbounded. No OOM on a low-RAM device (the heap-derived width cap should engage there).
- [ ] **PR2-C2b render-resolution change keeps reading position (PR.UX.2)** — scroll to mid-document, then rotate or
  fold/unfold to change the container width (re-rasterise at a new resolution). The reading position must be
  **preserved** (the item aspectRatio, hence layout height, is width-independent) — confirms the P-Read offset
  invariant holds across a resolution change.
- [ ] **PR2-C3a pinch + double-tap zoom feels right and coexists with scroll (PR.UX.3)** — at 1× a single-finger drag
  **scrolls** the document normally. **Pinch** to zoom (1–4×): the point under the fingers stays pinned (coordinate-
  preserving), no jarring first-frame scroll. While zoomed, a single-finger drag **pans** (clamped to the page edges,
  never a gap). **Double-tap** toggles 1↔2.5× centred on the tap. Zoom back to 1× and confirm scroll resumes.
- [ ] **PR2-C3b zoom leaves the reading position untouched (PR.UX.3)** — scroll to a known page, note the "Continue
  reading" position, pinch-zoom + pan around, then background/reopen: the durable position is **unchanged** (zoom is
  a draw-only transform; neither zoom nor pan writes a reading-position row). Zoom level itself does **not** persist
  across reopen (resets to 1×).
- [ ] **PR2-C4a jump-to-page navigates and persists the deliberate jump (PR.UX.4)** — with a multi-page PDF, tap the
  **page pill** (bottom-centre, now persistent) → the Slider dialog opens seeded at the current page. Drag to a far
  page → **Go**: the reader lands there. Background/reopen → the "Continue reading" shelf + resume now point at the
  **jumped-to** page (a deliberate jump persists, unlike the restore jump). Cancel leaves position unchanged.
  TalkBack announces the pill as "Jump to page".

## R-FT. Full-text body search (Phase P-FullText) _(SPEC-SEARCH §8)_

> HTML-first v1 (PFT.1–PFT.3): body text is extracted from the already-persisted reader HTML, chunk-indexed as
> `source_kind='body'`, and surfaced in local Find as a distinct "Also found in full text" section. The engine +
> DAO + indexing logic are CI-proven (Robolectric); these rows are the on-device confirmations CI can't run.

- [E] **R-FT1 body indexing populates on reader-open** — open a paper in the **HTML reader** (native or ar5iv). A
  background body-index job runs once (fire-and-forget; no UI jank while reading). Then in **Explore → Library**,
  search for a **distinctive phrase that appears only in that paper's body** (not its title/abstract) → the paper
  surfaces under **"Also found in full text."** Re-opening the same paper does **not** re-embed (the `.bodyindex`
  sidecar short-circuit). _2026-07-14 (emu 5554): opening a fresh arXiv paper (2606.27287) in the HTML reader fired
  the fire-and-forget body-index — coverage grew **2→3** distinct body papers, 49 `source_kind='body'` chunks written
  with one `embedded_at`, `.bodyindex` stamped `bge-small-en-v1.5-q8`; opening two more (2606.23643/2606.27330) grew
  it 3→5 (each got 53/61 chunks), and re-opening 2606.27287 ~4× never re-embedded (single `embedded_at` held). The
  **body-only phrase "placement is randomized by sampling"** (verified absent from every title/abstract; present in
  body ordinals 9–10 via the exact app FTS `MATCH`) surfaced 2606.27287 under **"Also found in full text."** **One
  real finding (see HUMAN §1, 2026-07-14):** the body leg's exclude set is `keyword ∪ raw top-30 semantic KNN`
  (`SearchViewModel` L232, `SEMANTIC_LEG_K=30` unfiltered) — so a body-only paper that is a **weak** semantic
  neighbor **below the display cut** is hidden from BOTH the main list AND the full-text section. Proven decisively:
  the phrase surfaced 2606.27287 **only after** temporarily removing its `paper_embeddings` row (restored byte-exact).
  Body leg + section + no-re-embed all work; the exclude aggressiveness is the flagged product question._ **FIX SHIPPED
  same-day (Co-Founder GO):** the body leg now dedupes against the DISPLAYED hits (keyword ∪ gated `fused`), not the
  raw top-30 KNN. **Re-verified end-to-end on emu 5554:** the identical search now surfaces 2606.27287 under "Also
  found in full text" **with its embedding intact** (no delete hack) — see HUMAN §1 2026-07-14 + the new
  `SearchViewModelTest` regression guard._
- [E] **R-FT2 honest section + coverage caption** — the "Also found in full text" rows have full parity (tap opens,
  swipe saves, long-press multi-selects); the main results are unaffected (no "Full text" badge noise); a
  query-independent caption reads **"Full text covers the N papers you've opened"** with N = the count of
  body-indexed papers (grows as you open more papers in the reader). _2026-07-14 (emu 5554): the caption rendered
  **"Full text covers the 3 papers you've opened"** (matching the DB body-paper count exactly) and live-grew as N
  climbed to 5; main results carried only their **"semantic"** badge (no "Full text" badge); the full-text row was a
  plain paper row with **no per-row badge**. Row-parity (tap/swipe/long-press) is the same `LocalHit`→`PaperListItem`
  composable as main results (code-covered), not separately driven this pass._
- [ ] **R-FT3 model-bump self-heal (optional)** — after an embedding-model change (or a forced re-index), the
  worker's model-mismatch wipe drops stale body chunks and the filesystem-driven backfill re-indexes the
  reader-opened papers over subsequent unmetered runs — full-text coverage recovers rather than collapsing to 0.
- [ ] **R-FT4 body FTS is fast enough at scale (soft)** — with a few hundred reader-opened papers indexed, typing a
  common term keeps the **main results** instant (the body leg is off the traced path) and the "Also found in full
  text" section fills within a beat. (If it ever lags, candidate-gating is the tracked fix before large growth.)
- [E] **R-FT5 TalkBack** — the "Also found in full text" section header and the coverage caption are announced;
  section rows read like any paper row. _2026-07-14 (emu 5554): both the section header ("Also found in full text")
  and the coverage caption render as plain `Text` nodes present in the accessibility tree (announceable); section
  rows are the same paper-row composable as the main list. Aural TalkBack pass not driven._

## S-Read. Reading continuity + "Continue reading" shelf (Phase P-Read) _(SPEC-UI Today · SPEC-DATA §2)_

> HTML-first design: the PH.6 `.position` sidecar stays the HTML restore source-of-truth; a v17 `reading_positions`
> table is a shelf projection + the PDF viewer's new durable resume. The store/DAO/heartbeat logic is CI-proven
> (Robolectric); these are the on-device confirmations CI can't run. Provisional constants to ratify from real
> reading behavior are flagged.

- [E] **S-Read1 PDF cold-kill resume** — open a paper's **PDF**, scroll several pages in, **force-stop** the app,
  reopen the same PDF → it restores to roughly where you left off (not page 1). A merely-opened PDF you never
  scrolled does **not** create a shelf row (see S-Read3). A version bump (new PDF) soft-misses to the top, keeping
  the old row for the shelf. _2026-07-14 (emu 5554): scrolling ~9 pages of a multi-page PDF (2606.23643 "TailorMind")
  wrote `(surface=pdf, page_index=8, fraction=0.52, finished=0)`; **`am force-stop`** then cold-reopen restored to
  **page 9** (the References page — deep, not page 1), matching the durable row that survived the kill. Never-scrolled
  PDF is symmetric to the S-Read3 HTML open-no-scroll (PDF viewer writes only on scroll); the version-bump soft-miss
  sub-leg not separately driven._
- [E] **S-Read2 the shelf + surface-correct resume** — open a paper in the **HTML reader**, scroll partway, press
  back → in **Today** a **"Continue reading"** section appears at the top with that paper + a "N% · HTML edition"
  position line; tapping it reopens the **HTML** reader restored near your position. Repeat starting from a **PDF** →
  the row reads "PDF" and tapping resumes the **PDF viewer**. _2026-07-14 (emu 5554): a genuine HTML scroll wrote
  `(2606.27287, html, fraction=0.0285→0.1411)`, and Today showed **"Continue reading" → 14% · HTML edition"** at the
  top; tapping the card reopened the **HTML** reader restored near the position (abstract body, not page-1 title). The
  PDF variant rendered **"52% · PDF"** for 2606.23643. Shelf live-updates on return (3%→14% seen). Note: the initial
  am-start VIEW deep-link spawned a **separate task** so the first return didn't recompose Today — a test artifact of
  the deep-link path, not a product bug (in-app nav + cold-start both refresh correctly)._
- [E] **S-Read3 honesty (the anti-PA.3 core)** — a paper you **opened but didn't scroll** never appears; a **TOC/
  citation jump** (no real scrolling) never adds it; a paper you **scrolled to the end and dwelled on** (sustained)
  drops off; a paper you mark **library-status "read"** drops off. Reopening a "finished" paper and scrolling back
  up makes it reappear. _2026-07-14 (emu 5554): ALL four legs confirmed by direct `reading_positions` inspection.
  **Open-no-scroll:** opening 2606.27330's HTML reader (HTML fetched + rendered) wrote **zero** rows. **TOC jump:**
  jumping to section "3 Task Decomposition" updated only the PH.6 `.position` sidecar (anchor `S3`) — the shelf
  `reading_positions` stayed empty (a jump changes where you'd resume TO, never puts the paper on the shelf).
  **Finished:** two consecutive ≥0.95 probes (0.9961→1.0) flipped `finished=1` and the shelf query then excluded it
  (`NOT EXISTS finished=1`); **scrolling back up to 0.90 reset the streak → `finished=0` → it reappeared.**
  **Library-read:** tapping the "Read" status chip set `library_entries.status='read'` and the shelf query dropped it._
- [E] **S-Read4 cross-surface furthest-progress** — read a paper to ~80% in **HTML**, then open its **PDF** and glance
  at page 1 (~3%) → the shelf shows the paper **once**, at **80% · HTML**, and resumes HTML (the recent low glance
  never buries the real progress). _2026-07-14 (emu 5554): 2606.23643 held BOTH a `pdf` row (0.52) and an `html` row
  (0.0134); the shelf query (`GROUP BY paper_id, MAX(fraction)`) represented it **once**, by the higher-progress PDF
  row (52% · PDF) — the low HTML glance neither duplicated nor buried it. (Surfaces are inverted vs. the spec's
  example — 52% PDF > 1.3% HTML here — but the MAX-fraction furthest-progress + appears-once logic is identical.)_
- [~] **S-Read5 provisional thresholds to ratify** — from real reading, sanity-check: the low **progress floor**
  (~0.02–0.03, "opened ≠ reading"), the **sustained-dwell finished** floor/streak (~0.95, two probes), the **recency
  window** (~30 days), and the **shelf cap** (~5). Tune in `ReadingProgressRepository`/`HtmlReaderViewModel` if reality
  differs. _2026-07-14 (emu 5554): the **0.02 floor** and **0.95×2 finished** thresholds behaved sensibly under real
  scrolling — a 4-swipe scroll landed 0.0285 (just above floor, honestly "reading"); a 3-swipe glance landed 0.0134
  (below floor, correctly NOT shelf-eligible on its own); finished needed exactly two ≥0.95 probes. Both feel about
  right on the emulator, but **recency (30d) and shelf-cap (5) were not stress-tested**, and the numbers still want
  ratification from natural real-device reading — keeping this `[~]` rather than closing it._
- [E] **S-Read6 TalkBack** — the "Continue reading" header and each row are announced in **position language**
  ("Resume {title}, scroll position 34%, HTML edition") — never "34 percent read"; equal in strength to the visual.
  _2026-07-14 (emu 5554): the shelf card's `content-desc` read exactly **"Resume {title}, scroll position 14%, HTML
  edition"** (and **"…, scroll position 52%, PDF"** for the PDF card) — position language, never "percent read"; the
  "Continue reading" header is an announceable `Text` node. Both surfaces verified in the a11y tree._

## P-PC5. Pinned sections + rename (P-Chat PC.5) _(ROADMAP P-Chat)_

- [ ] **P-PC5-1 Pin/unpin** — pinning a chat floats it into a **Pinned** section header above **Recent**; the row **animates** across the boundary (not a teleport); unpin returns it to Recent; an all-recent list shows no section header.
- [ ] **P-PC5-2 Rename (both surfaces)** — the row overflow **Rename** and the ChatSessionScreen TopAppBar **Edit** both open a prefilled dialog; a new name shows immediately in the list AND **live** on an already-open ChatSessionScreen TopAppBar; a **blank** submit clears back to the derived label.
- [ ] **P-PC5-3 Overflow delete still undoable** — Delete moved into the overflow menu still fires the "Chat deleted" Undo snackbar (the PC.3 delayed-commit path).
- [ ] **P-PC5-4 Upgrade + persist** — on a v3→v4 upgraded device, pin + rename **persist across an app restart**.
- [ ] **P-PC5-5 TalkBack** — pin/unpin (verb labels), the overflow (`chat_more_actions`), the Rename item, and the dialog field are all announced.

## O-PC3. Chat tab + promoted recents (P-Chat PC.3) _(ROADMAP P-Chat)_

- [ ] **O-PC3-1 Chat tab present + selectable** - a 4th bottom-nav tab "Chat" shows, is selectable, lights on selection, survives a rotation; cross-tab Back returns to Chat (per-tab saved back stack); the 4 labels fit at 1.3x font scale on a small phone.
- [ ] **O-PC3-2 Promoted recents render** - sessions list with scope chip, snippet (latest non-empty message; a session whose only reply was an empty/errored turn shows the user question or no snippet; orphaned-target rows show the fallback label + no snippet), relative time; tapping a row resumes its conversation.
- [ ] **O-PC3-3 Delete + undo (delayed commit)** - delete a chat -> row hides instantly + "Chat deleted" snackbar with Undo; tap Undo within the window -> the row returns; let the window elapse -> the chat is gone after relaunch. **Navigate off the Chat tab immediately after deleting -> the deletion still commits** (application-scope survival).
- [ ] **O-PC3-4 Empty-state CTA** - with zero sessions the empty state shows "Browse your library" -> routes to the Library tab.
- [ ] **O-PC3-5 TalkBack** - a row announces label + snippet + scope + time as one item; the delete button is announced separately with its cd; the Undo snackbar action is reachable.

## N-PC2. Explore merge - Search + Browse (P-Chat PC.2) _(ROADMAP P-Chat)_

- [ ] **N-PC2-1 Resting taxonomy** - cold-open Explore (Library scope, blank query) shows the category taxonomy under the pinned field; the keyboard does NOT auto-open; groups expand/collapse; follow toggles work; tapping a category opens its listing and Back returns to Explore.
- [ ] **N-PC2-2 Today CTA -> taxonomy** - with zero follows and last-used scope = arXiv, tapping the Today "Explore categories" CTA lands on Explore **Library scope showing the taxonomy** (the `?reset=true` forces scope 0 + blank query), not the arXiv intro.
- [ ] **N-PC2-3 3-tab bar + label fit** - the bottom bar renders exactly 3 tabs (Today . Explore . Library); the bar is VISIBLE on Explore (the base-route matcher) and Explore is highlighted selected; "Explore" label not truncated at 1.3x font scale.
- [ ] **N-PC2-4 Tab-switch state survival** - query, results, filters, and the Library|arXiv scope survive tab switches and a rotation.
- [ ] **N-PC2-5 IME gate** - on the Library scope, pressing the keyboard "Search" does NOT fire an arXiv network submit (inert); on the arXiv scope it does.
- [ ] **N-PC2-6 arXiv search unchanged** - an arXiv query still queues through the >=3s limiter ("searching arXiv..."), paginates, and shows structured filters; no throttle errors surface raw.

## M-PC1. Full-screen conversation route (P-Chat PC.1) _(ROADMAP P-Chat)_

- [ ] **M-PC1-1 Composer/IME with adjustResize** — on ChatSessionScreen the keyboard must never bury the composer; check for a navbar-height gap above the keyboard (double inset consumption: Scaffold padding + consumeWindowInsets + imePadding). **Also sanity-check existing inputs** (search field, routine setup, Ask sheet) — `adjustResize` is activity-wide and new in PC.1.
- [ ] **M-PC1-2 Long-conversation scroll UX** on the route, incl. scroll position across a tab switch away/back — **gates the recorded LazyColumn + pinned-composer decision**.
- [ ] **M-PC1-3 Cross-ref back-pop** — tap an arXiv ref in an answer → paper detail → back pops into the intact conversation (no dismissal, unlike the sheet hosts).
- [ ] **M-PC1-4 VM-reuse regression** — history → session A → back → session B shows B's transcript (the deleted sheet host's bug must stay dead).
- [ ] **M-PC1-5 Fork + process-death restore** — "New conversation" → one turn → kill the app → relaunch → the same forked session continues (no second fork; `boundSessionId` restore).
- [ ] **M-PC1-6 Resume without flash** — history → a long session: a brief spinner then turns; the empty hint must not flash (presets are standing chrome and stay). A single first-composition frame of default state is possible by construction — check it is imperceptible.
- [ ] **M-PC1-7 TalkBack** on ChatSessionScreen actionables (back, New conversation, export menu, composer).
- [ ] **M-PC1-8 Read-aloud stops when covered** — share sheet / print dialog over the activity stops speech (ON_STOP observer on a full screen; pre-existing behavior worth one look).

## L. Feedback, selection, swipe & background tasks (v2.0-alpha / Phase UX2) _(SPEC-UI §4/§4a/§5)_
> CI covers the pure/logic pieces: `FeedbackControllerTest`, `SelectionStateTest`,
> `SwipeablePaperRowTest` (a11y-action builder), `OrganizeViewModelTest` (tri-state/idempotence),
> `FilteredPapers`/`CategoryFeed` save paths, `BackgroundTaskMonitorTest` (state→task + cancel
> routing), `DownloadNotificationsTest` (channel/ForegroundInfo). What needs hardware:
- [E] **L1 Elevated feedback** — the app-level snackbar shows above the bottom bar with real elevation, a working dismiss "✕", longer dwell for action-bearing messages, and both actions reachable (Undo **and** "Add to…"). _v2.0.2 (emu 2026-06-23): "Saved to library" snackbar carries both **Undo** and **Add to…**; "Paper dismissed" carries Undo; action-bearing messages dwell longer (≈8s)._
- [E] **L2 Bulk Organize (the #1 ask)** — multi-select in Library/collection/tag/Search/feed → "Add to collection or tag" files every selected paper; tri-state chips read correctly across a mixed-membership selection; inline new-collection/new-tag adds all; tapping a full (✓) target removes all. _v2.0.2 (emu 2026-06-23): long-press → selection mode → selected 2 Library papers (one already in "Reasoning", one not) → "Add 2 papers to…" sheet → tapping Reasoning filled the gap → both now in the collection._
- [E] **L3 Swipe everywhere** — swipe-right = save, swipe-left = remove/dismiss on Today/Search/CategoryFeed/Filtered/Library; TalkBack exposes the same actions; swipe is inert in selection mode (no fight with long-press). Save-in-place rows snap back; triaged/removed rows leave the list. _v2.0.2 (emu 2026-06-23) on Today: swipe-right → "Saved to library" (row snaps back); swipe-left → "Paper dismissed" (row leaves, list advances), both with Undo. TalkBack custom-action equivalents are CI-covered (`SwipeablePaperRowTest`); the full per-list swipe matrix continues under V6._
- [~] **L4 Background tasks sheet** — Settings → "Background activity" shows live progress for the Gemma download, follow-sync and embedding/indexing; cancel actually stops the worker; retry re-enqueues. _v2.0.2 (emu 2026-06-23): the sheet opens from Settings and renders its empty state ("Nothing running right now"). Live progress + cancel/retry not yet caught — workers complete too fast on a warm emulator to observe; will be exercised against the long Gemma download in **V3**. Monitor state→task + cancel routing is CI-covered (`BackgroundTaskMonitorTest`)._
- [E] **L5 Foreground-service download** — the ~2.59 GB Gemma download runs as a foreground service with an ongoing local progress notification, survives backgrounding/Doze, completes + SHA-verifies. **Local-only** — confirm no network beyond the pinned model URL (no telemetry). _v2.0.2 (emu 2026-06-26, **V3**): `dumpsys` confirmed a `SystemForegroundService` `isForeground=true` (dataSync type) with an **ongoing, non-dismissible** progress notification (`android.title="Downloading on-device model"`, `android.progress` climbing, channel `arxiver_background`, flags FOREGROUND|ONGOING|NO_CLEAR); the download completed + **SHA-256-verified** (final 2.59 GB file). Doze-survival not separately exercised._
- [E] **L6 POST_NOTIFICATIONS (Android 13+, the S20)** — the permission is requested when the on-device download is first triggered; **denying it still completes the download**, only the notification is suppressed (graceful degradation). _v2.0.2 (emu 2026-06-26, **V3**): tapping Download first raised the system **"Allow Arxiver to send you notifications?"** prompt; granting it let the foreground progress notification show and the download proceed. The deny-still-completes branch not separately exercised._
- [E] **L7 Relevance thumbs (Phase P4.2)** — each Today inbox row shows a thumb-up/down pair. Tapping thumb-up fills the up icon (tap again to clear); thumb-down fills the down icon **but keeps the paper in the list** (soft "less like this", unlike swipe-dismiss which removes it). TalkBack announces state-aware labels ("More like this" / "Marked more like this, tap to clear", and the down equivalents). The vote persists across app restart, and on the next scoring pass a thumb-up promotes similar papers / a thumb-down demotes them. No snackbar on a tap (the icon state is the confirmation). Zero egress. _(Toggle logic + reactive surfacing are CI-covered: `TodayViewModelTest`.)_ _2026-07-10 (emu): voted rows exposed the state-aware TalkBack label "Marked more like this, tap to clear" (vs "More like this" unvoted) and "Less like this"; tapping a filled thumb-up cleared it — `paper_feedback` thumb rows 60→59 and the content-desc reverted — with no snackbar. Cross-restart persistence + next-pass promotion/demotion covered by B2a._

## H. Success criteria rollup _(PRD §7)_
- [E] **H1** New user can install, follow 2, and triage within 3 min of first launch (§7.1 — gated on A1/A2/B1). _v2.0.2 (emu 2026-06-23): fresh install → onboarding → followed 2 categories → "Start reading" → Today populated with fresh papers → triaged via swipe, all within the ~3-min rate-limiter budget (which is network-bound, so emulator timing is representative)._
- [ ] **H2** Hybrid search returns relevant results < 300ms, fully offline (§7.2 — C3/D2).
- [ ] **H3** A configured routine receives a well-formed payload and produces a useful run, no app-side errors (§7.3 — G2).
- [E] **H4** Zero crashes in normal flows; CI green on every release commit (§7.4 — A5; CI half already holds). _v2.0.2 (emu 2026-06-23): the A5 crash-free half holds on the emulator; the CI half holds (green gate). Ask/Claude surfaces ride V4/V5._

---

## Verification log

| 2026-07-14 | P-FullText §R-FT + P-Read §S-Read (post-#165, debug, **emulator** `emulator-5554` Pixel_3a API 34) | **Two whole phases device-verified in one session; one real product finding.** Installing the post-P-Read debug APK over the on-device **v16** install ran the **live v16→v17 migration on real data** — `user_version=17`, `reading_positions` created with the exact P-Read.1 schema, **0 rows** (honest empty shelf), no crash, no data loss (the coherent 65-label ranker harness + Gemma/Qwen/bge models all survived). **P-FullText §R-FT:** opening papers in the HTML reader fired the fire-and-forget body-index (coverage grew **2→3→5** distinct body papers as I opened 2606.27287/2606.23643/2606.27330; 49/53/61 body chunks; `.bodyindex` sidecar stamped; re-opens never re-embedded). A **provably body-only** phrase surfaced the reader-opened paper under **"Also found in full text"** with the caption **"Full text covers the N papers you've opened"** (N matched the DB); main results carried only their "semantic" badge, the full-text row no badge. **Finding (→ HUMAN §1):** the body leg excludes `keyword ∪ raw top-30 semantic KNN` (unfiltered) — so a body-only paper that's a **weak** semantic neighbor **below the display cut** is hidden from BOTH sections; proven decisively (the paper surfaced only after temporarily removing its `paper_embeddings` row, restored byte-exact). **P-Read §S-Read:** genuine HTML scroll wrote a shelf row and Today showed "Continue reading → N% · HTML edition"; tap resumed the correct surface restored near position; **PDF durable cold-kill resume** (scroll→`page_index=8`→`force-stop`→reopen restored to page 9); **cross-surface furthest-progress** (one card, MAX fraction); the full **honesty invariant** — open-no-scroll/TOC-jump write no row, sustained ≥0.95×2 marks finished→drops off→scroll-up reappears, library-"read" drops off; **TalkBack** content-descs in position language ("scroll position N%, {surface}"). Harness restored (2606.27287 status back to `to_read`, embedding restored, `paper_feedback` untouched). Cleared `[E]`: R-FT1/2/5, S-Read1/2/3/4/6; S-Read5 `[~]` (thresholds sane, recency/cap not stressed); R-FT3/R-FT4 still `[ ]` (model-bump + at-scale FTS need a model change / hundreds of papers). |
| 2026-07-12 | P-Ambient PA.1b (#145, debug, **emulator** `emulator-5556`) | **The ambient digest, verified end-to-end on device.** Installing the PA.1b APK ran the **live migration v14→v16** (`user_version=16`, 303 inbox rows `digested_at`-backfilled NULL). The opt-in toggle rendered to spec ("Requires Wi-Fi sync; nothing leaves your device") and enabled cleanly. Because `cmd jobscheduler run` can't target WorkManager's periodic job on this API, a throwaway build (embedNow un-suppressed) forced a non-suppressed pass → **the digest posted**: "89 new likely-relevant papers", a `BigTextStyle` body of the top-3 by score (all LLM-agents papers — the coherent profile's real top rows) + "…and 86 more", on the `arxiver_digest` IMPORTANCE_DEFAULT channel; **exactly 89 rows stamped `digested_at`** (exactly-once, matching the eligibility query). Tap opened the app (warm→last screen; cold→Today by construction — Today-forcing rides PA.2). Throwaway reverted + shipped APK reinstalled + `digested_at` reset; the 65-label harness is intact. Residual for a real device: lockscreen-hidden render (VISIBILITY_PRIVATE is set; emulator has no lock) + the API-33 deny path. |
| 2026-07-11 | post-#141 (debug, **emulator** `emulator-5556`) | **B1g SSRN card + detail leg (continuation).** Opened the source sheet, scrolled to confirm all **8** sources render with correct captions (chemRxiv/RS/SSRN/**Preprints.org**/**PsyArXiv** "Papers open in browser"; bio/med lag hint). Picked **SSRN** → SSRN-specific field vocab (Engineering/Social Sciences); "governance" → ONE submit → live **Top 25** (real SSRN papers via OpenAlex, short cards with **no abstract**). Opened the first hit → detail rendered the **SSRN** badge, **"No abstract available from SSRN"**, and **"Open in browser" (no PDF spinner)** + Connections (DOI 10.2139/ssrn.1148386). Cleaned up the one bare `papers` row the view persisted; ranker harness untouched (65 labels). B1g → `[E]` (only the save/multi-select-external + aural-TalkBack legs remain). |
| 2026-07-11 | p5 QW1+P5.5 (PR #141, debug, **emulator** `emulator-5556`) | **P5.4 coherent-profile readout + B1g multi-source search.** Replaced the uninformative diffuse profile with a realistic **coherent MODERATE** one via `run-as sqlite3` (interest = LLM-agents/reasoning/RL, 30 positives = 24 saves + 6 thumb-up; hard-ish negatives = vision/image/diffusion, 35 = 15 thumb-down + 20 dismiss; class-interleaved past-millis timestamps, FK-checked). One Re-index worker pass fitted a **healthy calibration** (a=48.7, λ=0.3) and the debug card read **Overall/Rich AUC 0.89 [0.81–0.97] · P@10 1.00** — advisory NO-GO-direction for P5.4 (the existing ranker already nails the top-10). Then **B1g**: the source chip opened the "Search source" radio sheet to spec (arXiv ★; chemRxiv/RS/SSRN "Papers open in browser"; bio/med lag hint); chemRxiv picked → chemRxiv-specific field vocab, results cleared, no auto-search; "catalysis" → ONE submit → live **Top 25** OpenAlex results; **airplane mode**: sheet opened offline (local registry, zero network), submit degraded to an honest "You're offline" + Retry. Harness profile left intact (the coherent 65-label set kept as fuel); airplane restored off. |
| 2026-07-10 | p5.3 merged (#138) debug (**emulator** `emulator-5556`) | **Discovery + PDF + thumbs legs, same session.** Unified follow directory (PE.4): arXiv is a peer row beside all 7 preprint sources; followed bioRxiv **Bioinformatics** and SSRN **Engineering** + **All of SSRN** → one sync landed **20 bioRxiv papers** in Today (B1a). "Following" sheet grouped by source with a working whole-source unfollow (B1c). Both arXiv follows driven to `empty_sync_streak=4` surfaced the muted **"No new papers recently"** hint on exactly those rows (B1d). A **bioRxiv PDF rendered in-app** (watermark visible, no Chrome bounce, no host block) — the device leg the local-box probe couldn't cover (B1e). Thumb rows exposed state-aware TalkBack labels and a working toggle-clear (`paper_feedback` 60→59), no snackbar (L7). Test follows deleted afterward; the 60 synthetic thumb labels intentionally kept as harness fuel. |
| 2026-07-10 | p5.3 merged (#138) debug (**emulator** `emulator-5556`, API 34 x86_64, 6 GB AVD) | **P5 device session — the whole ranking pipeline exercised end-to-end on hardware, plus a live 3→14 migration.** Upgrading the June-23 v2.0.2 install (DB v3) ran the ELEVEN chained migrations live: `user_version=14`, `relevance_model` present + EMPTY (the never-seeded invariant on real hardware), all data intact, no crash. Built a real 60-label profile through the actual UI (40 thumb-ups on agents/LLM papers, 20 thumb-downs on vision/video — scripted taps on the real P4.2 buttons), then one worker pass did embed → eval → calibrate → persist → score: calibration FITTED (a=1.504) and Today showed **"Likely relevant" = exactly 10 rows** with a score-tied 11th correctly excluded (B2c); a real swipe-dismiss stamped `score_at_label` byte-identical to the on-screen score; pass-2's refit tripped the **a≤0 guard → exact-0.55 fallback → section honestly vanished** (every score < 0.50). The debug card rendered per-segment numbers with an honest title-only "insufficient data" (B2b). Vision cluster demoted to the score floor (B2a demotion leg). **Airplane + force-stop + relaunch: the scored feed rendered fully offline** — the CHECKPOINT P5 "airplane-mode ranks" red-line leg. Two honest caveats for the real-device pass: AUC 0.50 reflects the deliberately-diffuse synthetic profile, and the fitted↔null flip-flop at the guard boundary is designed honesty (hysteresis → P5 ledger). Emulator restored (airplane off). |
Newest first. One entry per device session; absolute dates.

| Date | Build | Observed |
|---|---|---|
| 2026-07-04 | post-#87 debug (**emulator** `emulator-5554`, API 34 x86_64) | **P-HTML close-out session — the reader verified end-to-end on hardware; the M5 verdict closes PH.8 as no-go.** **M2 [E]:** a 2026 paper's NATIVE HTML edition rendered end-to-end (title/affiliations/abstract/reflow, full PH.7 chrome; minor LaTeXML "footnotemark" text artifact — the paper's own markup). **M-PH6-3 [~]:** the TOC sheet rendered groups + depth-indented real labels; tapping "2.3 Threat Model" landed the section EXACTLY at the viewport top (bibliography row + double-jump legs not exercised). **M-PH7-1 [~]:** find bar swapped in, live count "1 of 246"→"3 of 246" on next-taps, active match ORANGE / inactive YELLOW, auto-scroll to match (dark-theme marker legibility not checked). **M-PH7-3 [~]:** long-press → floating toolbar (Copy·Share·Select all·Read aloud·⋮) → **"Ask about this" in the overflow** → the sheet opened, RESUMED the paper's existing conversation (the PA.6-era turns, incl. the pre-/no_think Qwen answer — historical data as expected, plus the ghost empty bubble = the recorded backlog item, seen live) → the input held **"> sequence"** — blockquote-framed, NOT auto-sent (cloud-confirm Send leg not exercised). **M-PH7-6 [~]:** same-conversation resume proven by the visible history. **M5 [E — the PH.8 gate]:** the Attention paper (1706.03762, now NATIVE — arXiv's backfill covers it, so the ar5iv leg M3 stays [ ] pending a genuinely uncovered paper) rendered its math via **native Chromium MathML: the softmax display equation with a true fraction + radical + scripts, inline √d̄ₖ and a stacked 1/√dₖ — adequate and legible** (cosmetic: the margin equation number wraps). Render of the large native doc took ~60s on emulated x86 (sanitize+transform CPU-bound; arm64 hardware is faster — noted, not a defect). **M4 [E]:** airplane ON + force-stop + relaunch → cached HTML rendered with zero egress. **M-PH6-5 [E]:** still offline, scrolled deep → probe persisted → force-stop → reopen → **restored deep in the Introduction** (sidecar → restore funnel across process death). Device restored (airplane off). **PH.8 verdict → no-go** (see ROADMAP decision log): System WebView is Play-updatable on every API-26+ device, so the frozen-old-WebView cohort is a museum piece already served by the never-strand PDF fallback — a bundled math engine buys negligible reach for real cost. |
| 2026-07-04 | post-#83/#84 (**real device** — the reporter's phone) | **K20 closed on real hardware.** The user who reported the Qwen-only "No AI provider is set up yet" bug updated past the PA.6 hotfix and confirmed **Qwen now works** on the same device — on-device resolution + a streamed answer where the bug originally reproduced. This is the tier-alone real-device leg the 2026-07-03 emulator session couldn't substitute for; K20 `[E]`→`[x]`. HUMAN.md §2 re-verify row resolved. |
| 2026-07-03 | v2.0.3-dev (PA.6 hotfix branch, **emulator** API 34 x86_64, 6 GB AVD) | **K20 — Qwen-only resolution: user bug reproduced pre-fix, fixed post-fix, decode observed end-to-end.** Recreated the reporter's exact device state on `emulator-5554` (Gemma parked out of `files/models/`, `arxiver_ai_keys.xml` parked, Nano unavailable, Qwen 614 MB SHA-verified the ONLY engine). **Pre-fix (installed build):** firing a Summarize preset → **"No AI provider is set up yet…"** — a screenshot-match of the user report. **Post-fix APK (same state):** the same preset resolved **on-device** ("On-device — nothing leaves your device.") and Qwen **streamed a complete grounded 1504-char answer** (Sources · 1; correctly summarized the paper's stateful link-tracker monitor, 93%→47% evasion) with the full action row + follow-up chips. **Three real finds along the way:** (1) at 88% disk, Qwen's first `Engine.initialize()` **SIGABRT-crashed the app** writing its ~574 MB XNNPack cache → `EngineCachePreflight` ships in this PR (graceful `AppError.Storage`); (2) Qwen3's **`<think>` monologue renders to the user** before the answer → `/no_think` fix in the PA.6 follow-up; (3) the crash left a **ghost empty assistant bubble** in the resumed sheet → backlog. Device restored after the run (Gemma + keys back). Real-device re-verify requested from the reporter (HUMAN.md §2). |
| 2026-06-27 | v2.0.2 (debug, **emulator** API 34 x86_64, 6 GB AVD) | **Device-verification sweep K11/K12/K13/K18/K19 — one real bug found+fixed (K11).** Cleared the pending checks on `emulator-5554` after building+installing the current `main` (PS.5+PS.6). **K18 (PS.5):** the paper-detail bar **overflow ⋮** → Share PDF · Copy reference · Copy BibTeX; Copy actions show "BibTeX/Reference copied"; **Share PDF link-fallback** shared `https://arxiv.org/pdf/2606.27302v1` as text (no PDF → no download), and after Read-PDF cached the file, **Share PDF** shared the real `2606.27302v1.pdf` (`application/pdf` via the new `files-path pdfs/` FileProvider, grant works). **K19 (PS.6):** the Ask header **"Export or share this conversation"** menu → Share-as-text (Markdown w/ `# header`), Export Markdown file (`conversation_*.md` via FileProvider), and **Export as PDF → the system print dialog** (ISO A4, **2 pages**, **light** theme, a fully-rendered Mermaid graph + a rendered SVG flowing across the page break — host-driven readiness rendered async content before printing). **K12/K13 (PA.5d, already `[E]`):** re-confirmed — the per-paper Knowledge map opens, pans, and the **"View as list"** a11y toggle renders the focus paper + 7 relations; the map **renders in airplane mode** (app-drawn, no network). **K11 (PA.4) — positive adoption + a real bug:** with prefer-on-device ON + Gemma, a comparison prompt routed **on-device** and Gemma **positively rendered a valid GFM table** (`| Setting | Observation | …`) — the `TABLE::`→table adoption never observed before. But the same answer **leaked a raw `::TABLE`/`~|~` block**: Gemma opened a *second* table with the **closing** fence and the body had no `TABLE::`, so `StructuredTableTransform`'s `if(!body.contains(OPEN)) return body` guard returned early and the sentinel reached the user. **Fixed:** gate on any sentinel + tolerant fence (salvage a stray `::TABLE` block) + a final scrub; 4 new unit tests (24/0/0). **Re-verified live after reinstall:** Gemma re-triggered the exact bug and the fix **salvaged the stray block into a second valid GFM table** — persisted clean, zero `~|~`/`::TABLE` (vs the raw leak pre-fix). Keys app-Settings-only (never touched); device state restored (prefer-on-device reset, airplane off). |
| 2026-06-26 | v2.0.2 (debug, **emulator** API 34 x86_64, 6 GB AVD) | **P-Share PS.4b image capture + device-verification sweep (K14/K15/K16/K17, K11).** Built PS.4b (Export-as-image) and verified the P-Share surfaces on `emulator-5554`. **K17 export — three real bugs found+fixed on device:** (1) the PS.4a JS render-signal (`arxiver://rendered`) **never fired in an off-screen WebView** — an invisible/occluded WebView produces no frames, so `requestAnimationFrame` callbacks never run, and at `alpha=0` Chromium throttles its JS **timers** to a standstill too (the on-screen `arxiver://height` direct-`location.href` pings did fire, proving JS ran). Pivoted to a **host-driven** capture: the WebView renders at `alpha=0.004` (imperceptible but non-zero so the compositor keeps producing frames → the async Mermaid/fonts render actually completes), and the **Kotlin host owns the timing** — polls host-initiated `evaluateJavascript` for "fonts loaded + every Mermaid `<svg>` present", then snapshots; nothing depends on the renderer's throttled page timers/rAF. (2) The first capture **clipped the last node** — `body.getBoundingClientRect().height` (264 css) under-reports because the Mermaid `<svg>` **overflows its body box** (svg bottom = 272); fixed by measuring the **max bottom of any element**. (3) The share-sheet **image preview** hit a FileProvider `SecurityException` — `EXTRA_STREAM`+`FLAG_GRANT_READ_URI_PERMISSION` grants the target but **not the chooser's preview process**; fixed by also setting **`ClipData`**. Result: a **1080×820 PNG** of the full Map-relationships graph (light + dark), chooser preview + targets render. **K14** ` ```svg `: Claude's "red circle" svg rendered as a **real inline vector** (sanitised), no network in logcat. **K15** read-aloud: **offline TTS synthesis dispatched** (`GoogleTTSServiceImpl: Synthesis request` / `TTS dispatch: …seanet-embedded`) + Stop-toggle. **K16** actions: inline **Copy·Read·Share·⋮** + overflow **Pin·Quote·Export-image** + user-bubble lone Copy; **Quote** prepended a capped blockquote into the input. **K11** Gemma STRUCTURED [~]: Gemma E2B decoded **on-device** (`tflite: XNNPack … gemma-4-E2B-it.litertlm`), grounded list answer with `[1]`, **no `TABLE::`/`~|~` leak** — but a positive `TABLE::`→table render wasn't observed (Gemma favoured lists; two comparison trials interrupted). Keys are app-Settings-only (Claude+Gemini already connected from prior sessions; never touched). |
| 2026-06-26 | v2.0.2 (vc7, **emulator** API 34 x86_64, 6 GB AVD, on-device Qwen) | **P-Atlas PA.3 — light Qwen tier verified end-to-end (K10).** On the warm 6 GB AVD (light-eligible at 5931 MB), the **PA.3b UI** surfaced a **"Qwen 0.6B light"** on-device card with a Download button (below the downloaded-Gemma card). Download ran a foreground-service fetch that landed **exactly `614,236,160` bytes** at `files/models/light/Qwen3-0.6B.litertlm` (SHA-verified). **Dir-isolation confirmed on-device:** Gemma (`files/models/…`) + Qwen (`files/models/light/…`) **coexist** — the download did not purge Gemma (PA.3a's separate-dir `deleteStaleSiblings` catch holds). The card flipped to **Test/Delete**; the **Preferred engine** row rendered per-ready-tier chips (`Auto · Gemma · Qwen 0.6B light`). With **prefer-Qwen** selected, a per-paper Ask routed **on-device** ("On-device — nothing leaves your device", no cloud confirm) and **Qwen streamed a real grounded answer in ~20 s** (referencing the paper's digital-twins / elderly-cognitive-assistance / LLM / I-CONECT content) — **non-zero tokens, not blank** (the CPU build has no F2 zero-token trap). PLAIN prose, no table (correct for 0.6B); a more verbose reasoning style than Gemma. **Bug found+fixed (PA.3 follow-up PR):** the Qwen Download button reused the shared `ai_ondevice_download` string and mislabeled the size as "~2.6 GB" (Gemma's) → added a per-model `ai_ondevice_download_light` ("~0.6 GB") + a `downloadLabelRes` param on `ModelCard`. Cleared `[E]` K10. |
| 2026-06-26 | v2.0.2 (vc7, **emulator** API 34 x86_64, on-device Gemma) | **P-Atlas PA.2 — on-device STRUCTURED richness ladder, table validity (PA.0a, K9).** With prefer-on-device ON and Gemma ready, on the "Prompt Injection… Single and Multi-Injection Settings" paper, the comparison question "**Compare the single-injection and multi-injection attack settings**" routed **on-device** — the sheet read "**On-device — nothing leaves your device**" with **no cloud privacy-confirm** (the reliable on-device tell), so the new `OutputRichness.STRUCTURED` system addendum was in play. Gemma streamed (~20–40 s, emulated x86 CPU) a grounded answer that ended in a **valid, correctly-rendered 2×2 Markdown table** — `| Setting | Observation |` with **Single Injection** ("reliably improves rankings when résumé quality is homogeneous and few inject `[1]`") and **Multi-Injection** ("effectiveness diminishes as more inject, collapsing when widespread `[1]`") rows — rendered as a real bordered table, **not raw pipes**, in **Standard** mode (the STRUCTURED exemplar nudge alone drove it). **Grounding held:** cited `[1]`, honestly noted the abstract "does not provide a direct comparison," then tabled only what it could legitimately synthesize (no fabricated table); **no LaTeX/Mermaid** (STRUCTURED forbids them). This confirms the research prediction (~2B tables valid with a 1-shot exemplar) on our actual Gemma build and that the nudge doesn't override grounding. The RAG-budget half (the ~50-token nudge drops no chunk at the 4096 on-device window) is a headless CI test (`ChatContextAssemblerTest`). **Env note:** run on `emulator-5554` only after another local Claude session's `dev.helpinghand.companion` agent (which had been auto-foregrounding and hijacking input) vacated it for its own emulator (`HH_verify_api34`/5556); always `adb -s emulator-5554` with two devices up. |
| 2026-06-26 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **P-Atlas PA.1 — app-drawn relation graph (the deterministic-artifact keystone).** On paper **arXiv:2606.23643** (TailorMind), the Ask preset row surfaced a **"Map relationships"** chip (last preset). Tapping it inserted a new chat turn with **no provider call**; the persisted assistant content (read from `chat_messages`) was a valid, deterministic Mermaid graph: `graph TD` with a hexagon center `n0{{"TailorMind: Towards Preference-Aligned M…"}}` (label capped + escaped), rounded external-neighbour nodes (`n1("Semantic Browsing…")` … `n6`), and dotted similarity edges carrying cosines (`n0 -.->|0.84| n1`, `|0.80| n2`, …) — the live `VectorIndex.topK`/`neighborsFor` + builder pipeline. It routed to the offline `RichBlockWebView` and rendered, **no crash**. **Model-independence proven:** with **airplane mode ON** (Wi-Fi disabled, ✈ in the status bar) a second `Map relationships` tap generated + rendered another graph with no network error — the artifact needs no AI provider and works fully offline, on every tier. Builder/insert/VM covered by unit tests (`RelationGraphBuilderTest` 7/0/0, `ChatRepositoryTest.insertArtifactTurn`, `AskViewModelTest` no-provider + empty-state). **Legibility follow-up (same day):** the first cut used `graph TD`, which fans a star out horizontally and squished it to phone width (cramped/faint). Switching the builder to **`graph LR`** makes it tall + narrow — re-verified on TailorMind: the center (left) and its 5 neighbours (right) render as **clearly readable** titled nodes with the cosine on each edge (0.84/0.80/0.75/0.74). Single-paper legibility resolved; collection-scale level-of-detail + a pannable canvas remain **PA.5**. |
| 2026-06-26 | v2.0.2 (vc7, **V3 6 GB AVD**, API 34 x86_64) | **V3 — on-device Gemma chat verified end-to-end.** Bumped the Pixel_3a AVD to `hw.ramSize=6144` (host has 32 GB) → "Device memory: 5931 MB", clearing the 4 GB Gemma floor. The On-device card + **Download** now show (the **≥4 GB** complement of the J8 hidden-branch). Tapping Download raised the **POST_NOTIFICATIONS** prompt (L6); granting it ran a **foreground-service** download with an ongoing progress notification (L5) that completed and **SHA-256-verified** a **2.59 GB** `gemma-4-E2B-it.litertlm` (J5). The engine loaded on **`backend: CPU`** with **no `TF_LITE_PREFILL_DECODE … Skipping`** (the F2 zero-token signature is gone on the standard build); **Test connection → "Connection OK"** (J6). `TierSelector` flipped "Recommended: Cloud" → "**Recommended: Gemma (downloaded)**". With **Prefer on-device when ready** ON, the Summarize + Key-contributions presets streamed **grounded, `[1]`-cited markdown answers from Gemma — not blank bubbles** (the F2 S20 fix holds; K3/K5 on-device legs), with **no "what leaves the device" confirm** (K4 on-device leg), and **Key contributions ran in airplane mode** with no network error (J7 — on-device is fully offline). On-device decode is slow on the emulated x86 CPU (tens of seconds/answer) but correct; arm64/NPU throughput still wants the S20. **Bug found+fixed:** the Download button advertised "~1.9 GB" (stale label from the F2 swap off the 1.87 GB `-web` build) → corrected to "~2.6 GB". |
| 2026-06-26 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **P-Rich R3d.4 — vision "Summarize with figures" preset (the flagship vision tier).** With Claude configured (cloud, vision-capable) and paper **arXiv:2606.23643** (TailorMind) having a downloaded 16-page PDF, the Ask sheet's preset row surfaced a **"Summarize with figures"** chip as the last preset — **gating confirmed** (it appears only because the resolved provider is vision-capable AND a local PDF exists). Tapping it opened the **page-pick stepper**: "Page 1 of 16" (1-based, default 1), the **−** correctly **disabled at the floor**, +/− incremented the label (→ "Page 4 of 16"), TalkBack content-descs "Previous page"/"Next page" present. "Use this page" rendered the page network-free and parked on the cloud **privacy confirm**, which disclosed exactly `[Attached image: page 4 of arXiv:2606.23643 — image/jpeg, ~124 KB]` — **1-based page label** (the VM converts to 0-based once — m1), mime + ~KB, **never the base64 bytes** — with the **SYSTEM prompt unchanged** (text path byte-identical). Send fired a **real Claude vision call** that returned a **page-specific** answer ("## Page 4 Summary … There are **no figures, plots, or tables on this page** — it is entirely mathematical formulations and algorithmic descriptions"), correctly reporting the actual visual composition of page 4 — content the abstract excerpt `[1]` does not contain, so it is **only obtainable by analyzing the attached page image**, proving vision works end-to-end. Markdown rendered (## heading, **bold**); no crash in logcat. SB1 (action-time re-check) + SB2 (disclosure assertion) covered by `AskViewModelTest`; the on-device/non-vision-provider hidden-chip leg is unit-only (emulator has no ready Gemma). |
| 2026-06-24 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **P-Rich R4 — save/export/share (text/Markdown).** A settled answer shows a **Share** button beside Pin; the sheet header shows a **Share this conversation** icon (once a real answer exists). Tapping the header share opened the **OS share chooser above the modal sheet**, with `EXTRA_TEXT` = the full Markdown transcript: `# <paper title>` header, `## You` / `## Assistant` turns, verbatim bodies (LaTeX `$$…$$`, Markdown tables, a ` ```mermaid ` block), and the `— Exported from Arxiver` footer — **no `FOLLOWUPS::` sentinel leaked** (stripped on persist + defensively in the serializer). Per-answer Share renders in the action row in all three Ask hosts (paper/collection/history). **Known (documented) limitations confirmed:** resumed-history turns carry inline `[n]` with no `### Sources` block (citations are live-only, not persisted); markdown is shared as `text/plain`. No FileProvider added (image/PDF → R4.2). |
| 2026-06-24 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **P-Rich R3b.2 — follow-up suggestion chips.** **Standard:** after a "How does QoR-compact perform?" answer, two heuristic chips appeared below Sources/Pin — "What are the limitations or weaknesses?" + "How does this compare to related work?" ("Explain the method" correctly de-duped because the question began "how does…"). **Tapping** the limitations chip re-entered `ask()` — the privacy preview's new `USER` turn ended with exactly `Question: What are the limitations or weaknesses?`. **Cloud-Max:** asking produced a long, structured answer (incl. a rendered **Mermaid** summary diagram — modes × R2 composing) followed by **paper-specific model-generated chips** ("How was the exhaustive five-item subset selection conducted…?", "What are the known limitations or failure modes of QoR-compact?"); the raw `FOLLOWUPS::` sentinel was **stripped from the bubble and the persisted history** (no leak in either). Chips render only on the latest assistant turn. |
| 2026-06-24 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **P-Rich R3b — 3-mode depth dial.** The Ask sheet shows a **Quick · Standard · Max** segmented control (Standard selected by default) between the preset row and the notes toggle. Selecting **Max** and asking a question → the cloud **privacy preview** prepended the Max directive to the `USER` turn verbatim: *"Give a thorough, detailed answer: synthesize across the excerpts and surface nuances and edge cases. Use a Markdown table or a Mermaid diagram when it genuinely clarifies."* (the cloud-only rich nudge present), with the `SYSTEM:` prompt **unchanged**. Standard (default) adds no directive — byte-identical to pre-R3b (unit-tested + matches prior R3a/R3c Standard previews); Quick's terse directive + Max-on-device omitting the nudge are unit-tested. No crash; mode applies to presets too (preset × mode grid). |
| 2026-06-24 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **P-Rich R3c — Ask preset library.** The Ask sheet shows a **horizontally-scrollable preset chip row** above the input (Summarize · Key contributions · Explain method · …). Tapping **Key contributions** on the SPIRAL paper ran the grounded path → the cloud privacy preview showed `Question: List the main contributions…` with the **`SYSTEM:` prompt unchanged** (golden-safe), and after confirm produced a structured, `[1]`-cited "Main Contributions / Claims of SPIRAL" answer. **Scope filter verified:** the paper sheet offered paper-only presets (Explain method); the **collection** ("Reasoning") chat dropped them, showing only Summarize/Key contributions/Compare related (BOTH-scope). Chips are standard M3 `AssistChip`s (theme-aware; dark covered by R3a's verified dark sheet) and disabled while a turn streams. |
| 2026-06-24 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **P-Rich R3a — cross-refs + pin-to-notes.** A grounded Claude answer to the "Against Proxy Optimization" paper rendered `arXiv:2606.23597` as a **tertiary-tinted tappable link** (visibly distinct from `[n]` citations) — in **light AND dark** theme. **Tapping it dismissed the Ask sheet and navigated to the paper detail** (`ArxivId.parse` → `onPaperClick`), no crash. **Pin to notes:** the "📌 Pin to notes" button on a settled answer flipped to a disabled "✓ Added to notes" in-sheet on tap — an in-sheet confirmation added because the app-wide snackbar renders *behind* the modal Ask sheet. Native render path fully verified; WebView path covered by `RichHtmlTest` (`arxiver://paper/<id>` emission, math/Mermaid-protected, malformed-id rejection) + the proven R1/R2 `arxiver://` interception. Collection-scope chat passes no `onPinAnswer` → pin hidden (structural). |
| 2026-06-23 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **P-Rich R2 — diagrams (Mermaid).** Asked a paper to draw its method pipeline as a Mermaid flowchart: the answer routed to the WebView and Mermaid rendered a **real top-down flowchart** (styled nodes Input Frames → Object Detector → Bounding Boxes → Polycepta Module → Object Representations, with arrows) — not raw ` ```mermaid ` text. Verified in **light** theme, then re-rendered in **dark** theme (dark nodes / light text, matching `themeVariables`), and **still renders in airplane mode** (offline, bundled `mermaid@10.9.3`). Math (R1) + plain (R0) answers unaffected. |
| 2026-06-23 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **P-Rich R1 — math (KaTeX).** Asked a paper for a display LaTeX equation: the answer routed to the sandboxed offline `RichBlockWebView` and rendered a real **display equation** (`Ŝ = Σ_{k∈Q} wₖxₖ` with hat/summation/subscripts) plus **inline math** in a themed table and section headings (`Ŝ`, `Q`, `xₖ`, `k∈Q`) — not raw `$$…$$`. **Offline confirmed:** with airplane mode ON, re-opening the session still renders the math (bundled KaTeX, `blockNetworkLoads`, no network). Plain answers still use the native renderer; the WebView's only callbacks are the safe `arxiver://cite\|height` link intents. |
| 2026-06-23 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **P-Rich R0 — rich AI output (markdown + citations).** AI answers now render as **markdown** in the Ask sheet (verified live: headings, **bold**, _italic_, numbered lists, and a real bordered **GFM table** — previously raw `**`/`##`). `[n]` renders as a colored tappable citation that opens a **"Sources" expander** showing the cited excerpt (e.g. `[1] arXiv:2606.23633 — …`). The system prompt now invites markdown (seen in the "what leaves the device" preview); the inbox-grounding fix + key-header-only preview still hold. **Bug fixed (real, total Gemini breakage):** Gemini Test failed even with a valid `AIza…` key — the pinned model `gemini-2.0-flash` is **deprecated → HTTP 404**; swapped to `gemini-2.5-flash` (J3). |
| 2026-06-23 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **Cloud chat & RAG end-to-end (BYOK).** Connected a real Anthropic key → Test "Connection OK" (J2); default persists across relaunch (J4); both keys survive relaunch (J1 software-keystore). Per-paper Ask on an embedded paper streamed a grounded answer citing `[1]` (K2/K3), with the "what leaves the device" confirm showing key-is-header-only (K4); Summarize returned a grounded markdown summary (K6); collection KB chat synthesized across both papers `[1]`+`[2]` (K7); chat history listed/resumed/survived-restart/deleted (K8); cleared keys → `NotConfigured` "configure a provider" (K5). bge model "ready", 28 papers embedded (C1/C2/B2). RAM-floor `<4 GB` branch hides Gemma download (J8); Nano "not supported" non-flagship branch (J10). **Findings:** (1) Gemini key (`AQ.…`) is the wrong type → "Couldn't reach the provider" (J3) — needs an `AIza…` key; (2) **bug fixed** — per-paper Ask on an inbox (un-embedded) paper sent no context (the `ScopeIndexer` Paper branch was a no-op) → now indexes the paper on open + awaits it before retrieval. |
| 2026-06-23 | v2.0.2 (vc7, **emulator** API 34 x86_64) | **First autonomous emulator verification session.** Local env set up (build under pinned JDK 17, adb-driven UI + screenshots). Cleared `[E]`: A5 (crash-free sweep, 8 screens), I2/I3/I4 (phantom row, bottom-strip on Today+Detail, add-to-collection + survives upgrade), L1/L2/L3 (elevated feedback, bulk Organize, swipe both directions), F2 (deep link + share-in), H1, H4. L4 partial (sheet renders; live-progress deferred to V3). Re-confirmed PDF render (light) + Settings shows "Model ready — 28 papers embedded" (pre-evidence for C1/C2/B2, formal in V2). **Bug fixed:** `ArxiverApplication.onCreate` launched fire-and-forget IO coroutines (taxonomySeeder.seed / ensurePeriodicSync) with no `CoroutineExceptionHandler`; a DB-teardown race threw `IllegalStateException: Illegal connection pointer` uncaught → flaked 4 Robolectric suites locally (passed on CI's slower runners). Added a logging CEH → deterministic green. Version reconciled 1.2.2/vc6 → 2.0.2/vc7. |
| 2026-06-14 | v1.2.1 (vc5) | Sync spinner now clears (I1 ✅ — refresh icon shown). Bottom blank strip above the nav bar **still present** on Today (and Paper Detail) — divider fix wasn't the cause. Root-caused to nested-Scaffold inset double-apply → fixed in v1.2.2. |
| 2026-06-14 | v1.2.0 (vc4) | **First fully-working device run.** Onboarding → Today, fetching new articles (B1), PDF light+dark (F1), tagging (F3), search (F4) all verified. Bugs found → fixed in v1.2.1: Today sync spinner never stops (I1), phantom empty row in lists + detail (I2/I3), and no way to add a paper to a collection (I4). |
| 2026-06-14 | v1.1.1 (vc3) | "Start reading" crashed again; **crash-reporter dialog worked** (A4 ✅) and the copied trace root-caused the DataStore/protobuf R8 casualty → fixed by disabling minification (ROADMAP UX.9, ships v1.2.0). Onboarding still never reached Today. |
| 2026-06-13 | v1.1.0 (vc2) | "Start reading" crashed; reopening showed onboarding again. Root-caused (statically, from the R8 mapping) to Hilt-multibinding stripping → R8 compat mode + keep rules (ROADMAP UX.8). |
| 2026-06-11 | v1.0.0 (vc1, self-IDed 0.1.0) | First signed release published; no device smoke recorded. |
