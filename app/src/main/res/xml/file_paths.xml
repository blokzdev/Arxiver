<?xml version="1.0" encoding="utf-8"?>
<!--
  Scoped FileProvider paths for user-initiated shares only (no upload; per-share read grant).
  - PS.4: the cache's ask_exports/ dir (user-exported answer/diagram PNGs).
  - PS.5: the files/pdfs/ dir (already-downloaded paper PDFs, shared via the OS sheet).
-->
<paths>
    <cache-path
        name="ask_exports"
        path="ask_exports/" />
    <files-path
        name="pdfs"
        path="pdfs/" />
</paths>
